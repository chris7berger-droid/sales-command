SALES COMMAND — HANDOFF DOCUMENT v29
Generated: March 21, 2026
====================================================

HOW TO USE THIS DOCUMENT
Start every new Claude thread with:
"Continue building Sales Command. Here is the handoff doc v29 and the architecture doc."
Upload this file + HDSP_CommandSuite_Architecture.docx

====================================================
COMPLETED THIS SESSION (v29)
====================================================

REMEMBER ME CHECKBOX — COMPLETE ✅
- Added "Remember me" checkbox to Login.jsx (checked by default)
- When checked: sets sessionStorage flag; session persists across browser restarts
- When unchecked: no flag; App.jsx calls checkRememberMe() on load,
  signs out if flag missing (browser was closed and reopened)
- No Supabase client reinitialization — uses sessionStorage flag pattern
  to avoid breaking onAuthStateChange listeners

LOGIN INPUT STYLING — COMPLETE ✅
- Input backgrounds changed from C.linenCard to C.linenDeep for contrast
- Added WebkitAppearance: "none" to prevent browser white default styling

WORK TYPE DROPDOWN — COMPLETE ✅
- Replaced pill-button grid in CallLogDetail with compact dropdown
- Trigger button shows "X selected" or "Select work types…"
- Scrollable dropdown (max 240px) with checkboxes
- Selected items highlighted with dark background in dropdown
- Selected tags shown below trigger with dark background, teal text, × to remove
- Click-outside-to-close via useRef + mousedown listener

CALL LOG → PROPOSAL/INVOICE NAVIGATION — COMPLETE ✅
- CallLogDetail fetches linked proposals and invoices on mount
- Proposals fetched via call_log_id; invoices fetched via job_number → job_id
- Displayed as clickable dark cards with status pill and dollar amount
- Clicking a proposal navigates to Proposals page and auto-opens that proposal
  (via initialProposal.openId flow in App.jsx → Proposals.jsx)
- Clicking an invoice navigates to Invoices page
- New props: onNavigateProposal, onNavigateInvoice wired through
  CallLog.jsx → App.jsx

WTC CARD DATA FIX — COMPLETE ✅
- ProposalDetail WTC query expanded from minimal (id, work_type_id, locked,
  created_at) to full financial fields + work_types(name) join
- WTC cards now show actual work type name (not raw ID)
- WTC cards show calculated dollar total in dark badge with teal text
- Added calcWtcTotal() helper to compute labor + materials + travel − discount
- Summary total now computed as sum of all WTC totals (not stale proposals.total)
- WTCs reload after editing via wtcReload counter pattern

FMT$ DECIMAL FIX — COMPLETE ✅
- Added maximumFractionDigits: 0 to fmt$ in utils.js
- Prevents sub-cent decimals from calculation precision bleeding through
- Affects all dollar displays app-wide

====================================================
STYLE RULES (IMPORTANT — READ EVERY SESSION)
====================================================

1. Dollar amounts on cards: dark badge (C.dark background, borderRadius: 6,
   padding: "3px 10px") with teal text (C.teal). NOT full-card dark backgrounds.
2. Cards and modals: standard brown/linen background (C.linen or C.linenCard).
   Full dark backgrounds only for summary panels.
3. Selected tags/pills (work types, etc.): C.dark background with C.teal
   border and text.
4. Never use white backgrounds — inputs should use C.linenDeep or similar.

====================================================
DATA INTEGRITY RULES (IMPORTANT — READ EVERY SESSION)
====================================================

1. When fetching proposal_wtc for display, ALWAYS join work_types(name) and
   include financial fields: regular_hours, ot_hours, burden_rate,
   ot_burden_rate, markup_pct, materials, travel, discount, size
2. Summary total must be computed from live WTC data, not proposals.total
3. After any change to Proposals.jsx or CallLogDetail.jsx, verify:
   - WTC cards show work type name (not ID)
   - WTC cards show calculated dollar total
   - Summary total = sum of all WTC totals
4. fmt$ must use maximumFractionDigits: 0 — never show sub-cent precision

====================================================
NEXT TASKS (in priority order)
====================================================

1. End-to-end regression test on production (scmybiz.com):
   create customer → log call → create proposal → build WTC →
   generate PDF → send → customer signs → status = Sold →
   download signed PDF

2. SC-58: Security hardening (RLS audit, leaked password protection)

3. Backlog:
   SC-55: Goal Setting Flow
   SC-56: Multi-tenant email settings
   SC-62: Attachments on Call Log detail

====================================================
PRODUCTION ENVIRONMENT
====================================================

Production URL: https://www.scmybiz.com
Vercel project: sales-command (chris7berger-droid)
GitHub repo: chris7berger-droid/sales-command (branch: main)
Auto-deploys on every push to main

Vercel env vars set:
  VITE_SUPABASE_URL
  VITE_SUPABASE_ANON_KEY

====================================================
SUPABASE COLUMN REFERENCE (verified — do not guess)
====================================================

table: team_members
  columns: id, name (NOT full_name), email, phone, role, auth_id, active

table: proposals
  columns: id, total (NOT total_price), approved_at (NOT accepted_at),
           status, created_at, customer, intro_completed, attachments_added,
           recipients_assigned, wtc_verified, call_log_id, proposal_number,
           signing_token

table: call_log
  columns: id, display_job_number, stage, bid_due, follow_up, created_at,
           jobsite_address, customer_name, sales_name, job_number, job_name,
           is_change_order, parent_job_id, co_number, co_standalone,
           jobsite_city, jobsite_state, jobsite_zip,
           billing_address, billing_city, billing_state, billing_zip,
           billing_address_same, customer_id

table: customers
  columns: id, name, customer_type, first_name, last_name, phone, email,
           contact_phone, contact_email, billing_same, billing_name,
           billing_phone, billing_email, business_address, business_city,
           business_state, business_zip

table: proposal_wtc
  columns: id, proposal_id, work_type_id (INTEGER 1-40), burden_rate,
           ot_burden_rate, tax_rate, prevailing_wage, regular_hours,
           ot_hours, markup_pct, materials (jsonb), size, unit,
           sales_sow, field_sow (jsonb), sub_areas (jsonb),
           travel (jsonb), discount, discount_reason, locked, created_at,
           start_date (date), end_date (date)

table: work_types
  columns: id, name

table: proposal_signatures
  columns: id, proposal_id, signer_name, signer_email, signed_at,
           ip_address, pdf_url

table: invoices
  columns: id, job_id, job_name, status, amount, discount, sent_at, due_date

table: job_work_types
  columns: id, call_log_id, work_type_id

====================================================
SUPABASE STORAGE
====================================================

bucket: signed-proposals
  - Public bucket: ON
  - Files named: signed-proposal-{proposal_id}-{timestamp}.pdf

====================================================
SUPABASE AUTH CONFIG
====================================================

Site URL: https://www.scmybiz.com
Redirect URLs: https://www.scmybiz.com
Edge Function send-proposal: "Verify JWT with legacy secret" = OFF

====================================================
FILE LOCATIONS
====================================================

- src/pages/WTCCalculator.jsx
- src/pages/CallLog.jsx
- src/pages/Proposals.jsx
- src/pages/PublicSigningPage.jsx
- src/pages/Home.jsx
- src/pages/Login.jsx
- src/pages/Invoices.jsx
- src/components/CallLogDetail.jsx
- src/lib/supabase.js
- src/lib/auth.js
- src/lib/utils.js
- src/lib/tokens.js
- src/App.jsx
- vercel.json (project root)

====================================================
SIGNING FLOW ARCHITECTURE (SC-30)
====================================================

Proposal Detail → Send Proposal → ProposalPDFModal (Send tab)
→ send-proposal Edge Function → Resend email → customer gets link
→ /sign/:token → PublicSigningPage.jsx → customer types name
→ jsPDF generates branded PDF → signed-proposals bucket
→ pdf_url saved to proposal_signatures
→ proposals.status = Sold
→ call_log.stage = Sold
→ Proposal Detail shows Download Signed PDF button

====================================================
NAVIGATION ARCHITECTURE
====================================================

App.jsx manages page state via setActive("pageName"):
- CallLog → Proposals: onNewProposal sets initialProposal.job, switches to proposals
- CallLog → Proposal detail: onNavigateProposal sets initialProposal.openId,
  Proposals.jsx load().then finds matching proposal and sets sel
- CallLog → Invoices: onNavigateInvoice switches to invoices page
- Home → CallLog: setActive("calllog") with optional setBidDueFilter

====================================================
WORKFLOW RULES
====================================================

1. NEVER use drag and drop for files
2. ALWAYS provide code as text in chat — never as downloads
3. NEVER give half-baked solution mid-figuring-it-out
4. Always grep before edits
5. Python scripts via python3 << PYEOF heredoc
6. Git commit after every script succeeds
7. Ask for screenshot before assuming error cause
8. One step at a time
9. git push + handoff doc at session end

====================================================
BACKLOG
====================================================

SC-55: Goal Setting Flow
SC-56: Multi-tenant email domain settings
SC-58: Security hardening
SC-62: Attachments on Call Log detail

====================================================
END OF HANDOFF v29
====================================================
