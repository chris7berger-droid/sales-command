SC_Handoff_v65 — April 10, 2026
Session: Fix invoice delete bug

===============================================================================
WHAT WAS DONE
===============================================================================

1. Fixed invoice delete not removing invoices from the list
   - File: src/pages/Invoices.jsx
   - Root cause: handleDelete() and handleVoidConfirm() used hard DELETE,
     which RLS silently blocked. The load() query also had no deleted_at
     filter, so even soft-deleted rows would appear.
   - Fix: Both delete paths now set deleted_at instead of hard-deleting.
     The load() query now filters with .is("deleted_at", null).
   - Invoice lines are left intact (belong to the soft-deleted invoice).

2. Wired up soft-delete for proposals + deleted_at filter everywhere
   - ProposalDetail.jsx: handleDelete now sets deleted_at instead of
     hard-deleting proposals, signatures, and WTCs.
   - Invoice link checks in handleDelete and handlePullBack now exclude
     soft-deleted invoices (.is("deleted_at", null)).
   - Added .is("deleted_at", null) filter to ALL proposal and invoice
     queries across: Proposals.jsx, Home.jsx, SalesDash.jsx, Managers.jsx,
     Customers.jsx, CallLogDetail.jsx.

===============================================================================
COMMITS
===============================================================================

f5b0e19 Fix invoice delete: use soft-delete and filter deleted_at in list query
1a0e895 Wire up soft-delete for proposals; add deleted_at filter to all queries

===============================================================================
DEPLOY
===============================================================================

Pushed to main → Vercel auto-deploy.

===============================================================================
NOTES FOR NEXT SESSION
===============================================================================

- .env.prod is sitting untracked in the repo root — do not commit it.
