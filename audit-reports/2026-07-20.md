# Sales Command Security Audit — 2026-07-20

## Summary
_pending_

## Top 3 Urgent
_pending_

## Findings by Severity

### Critical

- **B6** — **`sql/rollback_drop_public_read_policies.sql:4` rebuilds the 2026-04-26 anti-pattern**
  - Evidence: `CREATE POLICY "tenant_config_public_read" ON public.tenant_config FOR SELECT TO anon USING (true); CREATE POLICY "team_members_public_read" ON public.team_members FOR SELECT TO anon USING (active = true);`
  - Live in-repo rollback script that exactly re-creates the two policies the 2026-04-26 incident named as the cause. An operator running this as "rollback" during an outage instantly re-opens the incident with one paste — every tenant's `stripe_customer_id`/`stripe_subscription_id`/`monthly_billing_goal`/`license_number`/`default_burden_rate` and every active team_member's `name/email/phone/auth_id/role` exposed to the publishable anon key. Carry-over from 2026-06-28, unchanged.
  - Spec: `CLAUDE_RLS.md` 2026-04-26 anti-pattern, OWASP A01.
  - Fix: delete the file. Correct rollback is `git revert` of the dropping migration commit in `command-suite-db`, not re-creating incident policies.

- **B11** — **`team_members` write policies allow self-elevation across roles**
  - `sql/rls_identity_tables.sql:64`
  - Evidence: `CREATE POLICY "team_members_update" ON public.team_members FOR UPDATE TO authenticated USING  (tenant_id = public.get_user_tenant_id()) WITH CHECK (tenant_id = public.get_user_tenant_id());`
  - Any authenticated tenant member can UPDATE any team_members row in their tenant — including flipping their own `role` from Sales to Admin/Manager. `is_admin_or_manager()` keys off `team_members.role`, so this is a one-step privilege escalation that unlocks every role-gated policy downstream. Sibling INSERT (line 60) and DELETE (line 69) share the same defect. Carry-over from 2026-06-01/2026-06-28 — unfixed for four cycles now; the single most weaponizable gap in the codebase.
  - Spec: `CLAUDE_RLS.md` role-gating, OWASP-ASVS V8.2.2, OWASP A01.
  - Fix: add `AND public.is_admin_or_manager()` to USING + WITH CHECK on `team_members_insert/_update/_delete`. If sales reps must still edit their own non-role profile fields, add a SECURITY DEFINER `update_my_profile()` RPC that whitelists non-role columns.

### High

- **A4** — **`invite-user` logs full magic-link `action_link` via the `linkData` object**
  - `supabase/functions/invite-user/index.ts:128`
  - Evidence: `console.log("generateLink result:", { linkData, linkErr: linkErr?.message });`
  - `linkData.properties.action_link` is a single-use magic-link that grants a session for `email`. Persisting it to Edge Function logs turns every invite into a takeover credential for that user for anyone with log-read access. Carry-over from 2026-06-28 A4 (same line).
  - Spec: OWASP-ASVS V7.1.1, OWASP A09.
  - Fix: `console.log("generateLink result:", { hasLink: !!linkData?.properties?.action_link, linkErr: linkErr?.message });`

- **B10** — **`tenant_config_update` lacks `is_admin_or_manager()` gate**
  - `sql/rls_identity_tables.sql:94`
  - Evidence: `CREATE POLICY "tenant_config_update" ON public.tenant_config FOR UPDATE TO authenticated USING  (id = public.get_user_tenant_id()) WITH CHECK (id = public.get_user_tenant_id());`
  - Any tenant member (Sales Rep included) can mutate `stripe_customer_id`, `stripe_subscription_id`, `monthly_billing_goal`, `yearly_billing_goal`, `license_number`, `default_burden_rate`/`_ot_burden_rate`/`_tax_rate`, `apps[]`. `default_burden_rate` flows into every WTC calc — a rep can change company-wide pricing. Pairs with D3 (`Settings.handleSave` also has no client role gate on the main form). Carry-over.
  - Spec: `CLAUDE_RLS.md` role-gating, OWASP A01, OWASP A05.
  - Fix: add `AND public.is_admin_or_manager()` to USING + WITH CHECK.

- **B13** — **`proposal_signatures_delete` allows any tenant member to wipe legal signature evidence**
  - `sql/rls_child_tables.sql:261`
  - Evidence: `CREATE POLICY "proposal_signatures_delete" ON public.proposal_signatures FOR DELETE TO authenticated USING (tenant_id = public.get_user_tenant_id());`
  - Any authenticated tenant member can DELETE any signature row — `signer_name`, `signer_email`, `ip_address`, `signed_at`, `pdf_url` — while leaving the proposal in status='Sold'. Destroys the forensic/legal record of the customer's electronic signature (persisted per E-SIGN Act intent) with no audit trail. Carry-over.
  - Spec: OWASP-ASVS V7.1, OWASP A09, E-SIGN Act non-repudiation.
  - Fix: drop the DELETE policy entirely (signatures should be append-only). If admins genuinely need pull-back, gate with `is_admin_or_manager()` AND write an `audit_log` row via SECURITY DEFINER trigger.

- **B-db_hardening_get_user_tenant_id** — **`sql/db_hardening_phase1.sql:12` redefines `get_user_tenant_id()` as broken and hijack-prone** (NEW)
  - Evidence: `CREATE OR REPLACE FUNCTION public.get_user_tenant_id() RETURNS uuid AS $$ SELECT id FROM public.tenant_config LIMIT 1; $$ LANGUAGE sql SECURITY DEFINER STABLE;`
  - Two defects in one function: (1) SECURITY DEFINER without `SET search_path` — an attacker able to create objects earlier in the search_path can hijack `tenant_config` resolution and pivot into a superuser context; (2) the body returns the first tenant unconditionally. If this file is re-run after `rls_identity_tables.sql:129` (the correct `auth.uid()`-aware body verified at line 129 with `AND active = true` at line 134), it SILENTLY re-breaks tenant isolation across every downstream policy — every authenticated user resolves to the same "first tenant" and sees all their data. Both the correct and broken definitions coexist in-repo; load order in `command-suite-db` decides which wins.
  - Spec: `CLAUDE_RLS.md` tenant scoping, OWASP-ASVS V5.2.6, CWE-1188.
  - Fix: delete Section 1 of this file (the old helper). If kept, replace with the auth-aware body from `rls_identity_tables.sql:129` AND append `SET search_path = public`.

- **D2** — **`PublicInvoicePage` leaks internal cost basis via `proposal_wtc(*)` wildcard**
  - `src/pages/PublicInvoicePage.jsx:51`
  - Evidence: `.from("invoice_lines").select("*, proposal_wtc(*, work_types(name))").eq("invoice_id", inv.id);`
  - `invoices` select at line 40 was fixed (D1 — explicit column list + column-level anon GRANT). But the sibling `invoice_lines` fetch still requests `proposal_wtc(*)`. Under the anon viewing-token RLS, the response ships `regular_hours`, `ot_hours`, `burden_rate`, `ot_burden_rate`, `markup_pct`, `tax_rate`, `materials` (jsonb cost basis w/ unit costs), `travel`, `discount`, `sales_sow`, `sub_areas` — every internal cost and margin field — to every customer's browser. Visible in the Network tab; a competitor with a valid invoice link can reverse-engineer the entire cost structure. `invoice_lines.*` also exposes `tenant_id`. Carry-over from 2026-06-01 and 2026-06-28 — unchanged.
  - Spec: `CLAUDE.md`, `CLAUDE_RLS.md`, OWASP-ASVS V5.1.3, OWASP A01.
  - Fix: narrow to `.select("id, invoice_id, billing_pct, amount, description, proposal_wtc:proposal_wtc_id(id, work_types(name))")` and add `REVOKE SELECT ON public.proposal_wtc FROM anon; GRANT SELECT (id, work_type_id) TO anon;` in command-suite-db so a hand-crafted REST query cannot widen.

- **D-PayAppDetailModal-money** — **`PayAppDetailModal.jsx` money handlers still have no role gate**
  - `src/components/PayAppDetailModal.jsx:233` (`handleSaveLines`), `:264` (`handleDeletePayApp`)
  - Evidence (233): `async function handleSaveLines() { ... await supabase.from("billing_schedule_pay_app_lines").update({...}); await supabase.from("billing_schedule_pay_apps").update({...}); await supabase.from("invoices").update({ amount, retention_amount }).eq("id", payApp.invoice_id); }` — component imports no `teamMember` prop and defines no `canManage/canDelete` symbol (grep confirmed).
  - Any authenticated user can hit these three money-writing UPDATEs from the visible buttons OR from devtools. `handleDeletePayApp` (:264) hard-deletes the pay app and soft-deletes the linked invoice with the same absence of gating. RLS on `billing_schedule_pay_apps`/`_lines` lives in `command-suite-db` and cannot be verified here, but even correct tenant-scoped RLS with no role gate lets any Sales Rep alter billed amounts post-send or delete a sent invoice. Companion `NewPayAppModal.jsx:133` mints these rows with the same gap. Carry-over from 2026-06-28.
  - Spec: `CLAUDE.md` Data Integrity Rule 6, `CLAUDE_RLS.md` role-gating, OWASP-ASVS V4.1.3.
  - Fix: accept `teamMember` prop, compute `canManage = ["Admin","Manager"].includes(teamMember?.role)`, gate the Save/Delete buttons AND each handler entry; add matching RLS UPDATE/DELETE on `billing_schedule_pay_apps` + `_lines` + role-restricted UPDATE on `invoices.amount`/`retention_amount` in `command-suite-db`.

- **C-reset-password** — **`reset-password` references undeclared `origin` — throws ReferenceError AND inverts the anti-enumeration oracle**
  - `supabase/functions/reset-password/index.ts:66`
  - Evidence: `const isSchedule = origin.includes("schmybiz") || origin.includes("schedulecommand");` — `origin` is never declared in this Deno scope (no browser globals), verified across lines 1-65.
  - Behavior: the "user not found" branch (:57) returns `{ success: true }` — the anti-enumeration path. The "user exists" branch reaches :66, throws ReferenceError, gets caught at :145 and returns `{ error: "origin is not defined" }` with 500. Result: an unauthenticated attacker can now enumerate registered users by inspecting response body/status (500 = exists, 200 = doesn't). Also breaks Schedule Command branding — the branding branch is unreachable. Carry-over from 2026-06-28 (unfixed).
  - Spec: OWASP-ASVS V3.2.3, OWASP A01, CWE-204.
  - Fix: `const origin = req.headers.get("origin") || req.headers.get("referer") || "";` before the try-block, AND replace `error: error.message` in the catch with a generic success/500 response that preserves the anti-enumeration invariant.

- **C-qb-void-invoice** — **`qb-void-invoice` missing admin/manager gate on QB audit-trail mutation** (severity ↑ from MEDIUM)
  - `supabase/functions/qb-void-invoice/index.ts:63`
  - Evidence: `const caller = await authenticateCaller(sb, req, SUPABASE_SERVICE_ROLE_KEY); if (!caller.ok) return unauthorizedResponse(caller.status, corsHeaders); const { invoiceId, reason, action: voidAction } = await req.json();`
  - Any authenticated tenant user (including a Sales Rep) can void or delete any of their tenant's QB invoices. Voiding is an irreversible money mutation with audit/tax consequences and belongs to admin/manager. `qb-auth` correctly uses `requireAdminOrManager` for mutating actions — same pattern needed here. Carry-over (promoted to HIGH: irreversible QB write, unusual for a rep role).
  - Spec: OWASP-ASVS V4.1.3, OWASP A01, CWE-863.
  - Fix: replace `authenticateCaller` with `requireAdminOrManager` from `_shared/tenantAuth.ts`.

- **C-create-billing-session** — **`create-billing-session` missing admin/manager gate on subscription/payment-method mutation** (severity ↑ from MEDIUM)
  - `supabase/functions/create-billing-session/index.ts:57`
  - Evidence: `const caller = await authenticateCaller(supabase, req, SUPABASE_SERVICE_ROLE_KEY); if (!caller.ok) return unauthorizedResponse(caller.status, getCors(req)); if (caller.isServiceRole) return unauthorizedResponse(403, getCors(req));`
  - Any tenant member can initiate a Stripe Checkout, change subscribed apps, or mutate `tenant_config.stripe_customer_id`. Non-admins triggering `action:'portal'` also receive a Stripe Customer Portal session URL — payment methods, cancellation, plan changes. This is admin/manager territory (financial control). Carry-over (promoted).
  - Spec: OWASP-ASVS V4.1.3, OWASP A01.
  - Fix: swap `authenticateCaller` for `requireAdminOrManager`.

- **F-PayAppDetailModal-cascade** — **`handleSaveLines` updates `invoices.amount` but never propagates to `invoice_lines.amount`**
  - `src/components/PayAppDetailModal.jsx:249`
  - Evidence: `if (payApp?.invoice_id) { await supabase.from("invoices").update({ amount: Math.round(gross * 100) / 100, retention_amount: ret, }).eq("id", payApp.invoice_id); }` — no accompanying UPDATE against `invoice_lines`.
  - `PublicInvoicePage.jsx:224` renders `l.amount` from `invoice_lines` as the per-line "Line Total"; `Invoices.jsx` InvoiceDetail does the same. After any pay-app % edit, the customer's public page and the printable PDF show stale per-line dollars while the invoice header total is fresh. Canonical Data Integrity Rule 6 trap inverted — the save recomputes the upstream source but fails to cascade to the downstream stored copy that's rendered. Carry-over from 2026-06-28.
  - Spec: `CLAUDE.md` Data Integrity Rule 6.
  - Fix: after the invoices UPDATE, look up `invoice_lines` by `(invoice_id, billing_schedule_line_id)` and UPDATE each row's `amount` + `billing_pct` from the just-saved pay-app lines.

- **C16 / C-qb-record-payment-service-role** — **`qb-record-payment` service-role branch skips per-invoice tenant scoping**
  - `supabase/functions/qb-record-payment/index.ts:103`
  - Evidence: `if (!caller.isServiceRole && invoice.tenant_id !== caller.tenantId) { return unauthorizedResponse(403, corsHeaders); }`
  - When caller presents `SUPABASE_SERVICE_ROLE_KEY`, the tenant check is skipped. The webhook path currently passes `invoiceId` from `session.metadata.invoice_id` — a Stripe-signed but originally client-supplied value at Payment Link creation. Any future SSRF or misuse that lets a caller reach `qb-record-payment` with the service key, plus knowledge of another tenant's `invoiceId`, can create phantom QB Payment rows against arbitrary tenants. Carry-over.
  - Spec: OWASP-ASVS V13.2.1, CWE-345.
  - Fix: remove the isServiceRole bypass — derive tenant from `invoice.tenant_id` unconditionally; require the webhook to POST `{invoiceId, expectedTenantId}` and assert equality.

### Medium

- **B-history_locker-active** — **`archive.get_user_tenant_id()` omits `AND active = true` (two copies)** (NEW)
  - `sql/history_locker_phase1.sql:76` and `sql/rls_cleanup_and_remaining.sql:389`
  - Evidence (:76): `CREATE OR REPLACE FUNCTION archive.get_user_tenant_id() RETURNS uuid AS $$ SELECT tenant_id FROM public.team_members WHERE auth_id = auth.uid() LIMIT 1; $$ LANGUAGE sql SECURITY DEFINER STABLE SET search_path = public, archive;`
  - `public.get_user_tenant_id()` at `rls_identity_tables.sql:134` correctly filters `AND active = true`, so a deactivated employee immediately loses access. The archive equivalent (both copies) does NOT — a deactivated team_member still resolves to their old tenant and can continue reading `archive.legacy_records` (imported customer/job history in `raw_data jsonb`). Deactivated-user cleanup is incomplete for the archive schema.
  - Spec: OWASP-ASVS V8.2.3, OWASP A01.
  - Fix: add `AND active = true` to the WHERE in both `archive.get_user_tenant_id()` bodies (history_locker_phase1.sql:80 and rls_cleanup_and_remaining.sql:393).

- **B-archive-grant-all** — **`GRANT ALL` on archive schema to `anon`** (NEW)
  - `sql/history_locker_phase1.sql:118-122`
  - Evidence: `GRANT USAGE ON SCHEMA archive TO anon, authenticated; GRANT ALL ON ALL TABLES IN SCHEMA archive TO anon, authenticated; ALTER DEFAULT PRIVILEGES IN SCHEMA archive GRANT ALL ON TABLES TO anon, authenticated;`
  - Blanket `GRANT ALL` to `anon` on every current AND future archive table. RLS is enabled on `archive.import_batches` and `archive.legacy_records` with authenticated-only policies, so anon is currently denied by default. But: (1) any new archive table added without immediate `ENABLE ROW LEVEL SECURITY` becomes anon-readable/writable in the same deploy; (2) `anon` gets INSERT/UPDATE/DELETE grants no anon flow needs. Defense-in-depth is missing.
  - Spec: OWASP-ASVS V4.1.3 (least privilege), OWASP A01.
  - Fix: split to `GRANT USAGE ... TO anon, authenticated;` then `GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA archive TO authenticated;` only.

- **B-proposal_wtc-invoice_lines-delete** — **`proposal_wtc_delete` and `invoice_lines_delete` lack role gates on money rows** (NEW)
  - `sql/rls_child_tables.sql:199` (proposal_wtc), `:304` (invoice_lines)
  - Evidence (:199): `CREATE POLICY "proposal_wtc_delete" ON public.proposal_wtc FOR DELETE TO authenticated USING (tenant_id = public.get_user_tenant_id());`
  - Any tenant member can DELETE proposal_wtc rows (locked pricing snapshots feeding `calcWtcPrice` — deleting them triggers the `calcWtcPrice(null) → 0` trap that has already caused two 2026 incidents) or invoice_lines (per-line billed dollars the customer sees on `PublicInvoicePage`). Same pattern class as B13 but on money rows.
  - Spec: `CLAUDE.md` Data Integrity Rule 6, `CLAUDE_RLS.md` role-gating.
  - Fix: gate both DELETE policies with `AND public.is_admin_or_manager()`. Consider also `proposal_recipients_delete` and `customer_contacts_delete`.

- **B-authenticated-deleted_at** — **`proposals_select` and `invoices_select` don't filter `deleted_at`** (NEW)
  - `sql/rls_core_tables.sql:138` (proposals), `:185` (invoices)
  - Evidence (:138): `CREATE POLICY "proposals_select" ON public.proposals FOR SELECT TO authenticated USING (tenant_id = public.get_user_tenant_id());` — no `AND deleted_at IS NULL`.
  - Soft-deleted rows remain visible to every tenant member unless the client remembers `.is('deleted_at', null)`. CLAUDE.md's Column Reference explicitly documents `deleted_at (timestamptz, NULL = active)` for both tables. Any new list view, RPC, or ad-hoc data pull that forgets the filter accidentally shows deleted rows and — for UPDATE paths — can revive them. Same defect on the authenticated side that B2/B3 flag on the anon side.
  - Spec: `CLAUDE.md` soft-delete contract, OWASP A01.
  - Fix: add `AND deleted_at IS NULL` to the USING clause of `proposals_select` and `invoices_select`; provide an admin-gated view (`proposals_including_deleted`) for the recovery UI.

- **C-qb-record-payment-status** — **`qb-record-payment` records phantom payment when invoice status is not Paid** (NEW)
  - `supabase/functions/qb-record-payment/index.ts:141`
  - Evidence: `if (!invoice.qb_invoice_id) { return new Response(JSON.stringify({ error: "Invoice not synced to QB yet" }), { ... status: 400 }); }`
  - Handler proceeds to create a QB Payment for any invoice with a `qb_invoice_id`, without asserting `invoice.status === 'Paid'` or `paid_at` is set. Any tenant user can invoke this against an unpaid invoiceId and corrupt QB AR reporting until an operator manually reconciles. No admin gate either.
  - Spec: OWASP-ASVS V4.1.3, OWASP A04.
  - Fix: guard `if (invoice.status !== 'Paid' || !invoice.paid_at) return 400 "invoice not paid";` and require admin/manager for direct (non-service-role) invocations.

- **C-qb-record-payment-net** — **`qb-record-payment.netAmount` ignores retention, over-records payment in QB**
  - `supabase/functions/qb-record-payment/index.ts:153`
  - Evidence: `const netAmount = (invoice.amount || 0) - (invoice.discount || 0);`
  - `send-invoice:161` charges the customer `amount - discount - retention_amount` via Stripe. `qb-record-payment` posts `amount - discount` to QB, over-recording payment by the retention amount on retention-bearing invoices. Real cash-vs-books mismatch. Carry-over from 2026-06-28.
  - Spec: OWASP A04 (insecure design).
  - Fix: `const netAmount = (invoice.amount||0) - (invoice.discount||0) - (invoice.retention_amount||0);` (mirror `send-invoice:161`).

- **C-invite-user-listusers** — **`invite-user` uses `auth.admin.listUsers()` — cross-tenant enumeration + silent pagination bug**
  - `supabase/functions/invite-user/index.ts:102`
  - Evidence: `if (createErr.message.includes("already been registered")) { const { data: { users } } = await supabase.auth.admin.listUsers(); const existing = users?.find((u: any) => u.email === email); ... }`
  - Returns the first page (default 50) of `auth.users` across ALL tenants. On a real prod instance with >50 users the duplicate-detection silently misses matches past page 1 — `authId` ends up undefined and the team_members row is never linked. Also enables cross-tenant email enumeration. Carry-over.
  - Spec: OWASP-ASVS V4.2.2, OWASP A01.
  - Fix: `supabase.auth.admin.getUserByEmail(email)`; then verify the resulting team_members row is in the caller's tenant.

- **C-reset-password-listusers** — **`reset-password` uses the same listUsers-with-no-pagination anti-pattern**
  - `supabase/functions/reset-password/index.ts:55`
  - Evidence: `const { data: { users } } = await supabase.auth.admin.listUsers(); const userExists = users?.find((u: any) => u.email === email.toLowerCase());`
  - Defeats the no-enumeration property on instances with >50 users. O(all users) per reset call — cheap DoS vector on multi-tenant deploys. Carry-over.
  - Spec: OWASP-ASVS V4.2.2, OWASP A05.
  - Fix: `supabase.auth.admin.getUserByEmail(email.toLowerCase().trim())`.

- **C-deactivate-user-count** — **`deactivate-user` counts `call_log` cross-tenant**
  - `supabase/functions/deactivate-user/index.ts:119`
  - Evidence: `.from("call_log").select("id", { count: "exact", head: true }).eq("sales_name", member.name);`
  - Runs on service_role (RLS bypass) with only `sales_name` filter. Two tenants with a "John Smith" get combined counts, leaking tenant B's job volume to tenant A. Carry-over.
  - Spec: OWASP-ASVS V4.2.1, OWASP A01.
  - Fix: `.eq("tenant_id", callerTenantId)`.

- **C-qb-search-customers** — **`qb-search-customers` has no admin/manager gate on paid QB API queries**
  - `supabase/functions/qb-search-customers/index.ts:88`
  - Evidence: `const caller = await authenticateCaller(sb, req, SUPABASE_SERVICE_ROLE_KEY); if (!caller.ok) return unauthorizedResponse(caller.status, corsHeaders); if (caller.isServiceRole) return unauthorizedResponse(403, corsHeaders);`
  - Any tenant user (including inactive-invitee) can spin QB API queries. Intuit throttles per-realm; sustained abuse from one user can lock out real ops. Exposes customer directory data to any tenant employee. Carry-over.
  - Spec: OWASP-ASVS V11.1.3.
  - Fix: `requireAdminOrManager` OR rate-limit per-user with a low cap.

- **C-extract-sov** — **`extract-sov` has no rate limit on paid Anthropic API calls**
  - `supabase/functions/extract-sov/index.ts:159`
  - Evidence: `const anthropic = new Anthropic({ apiKey: ANTHROPIC_API_KEY }); const resp = await anthropic.messages.create({ model: "claude-opus-4-7", max_tokens: 16000, ... });`
  - Any tenant user can trigger Opus 4 calls (16k output tokens) as long as they hold a legitimate `billing_schedule.pdf_url`. No per-user/tenant cap. Anthropic billing at ~$0.15-0.30/call, quickly reaches real dollars on abuse. Carry-over.
  - Spec: OWASP-ASVS V11.1.3, OWASP A05.
  - Fix: in-memory rate limit keyed by `user.id` + daily per-tenant DB counter cap; cache by SHA-256 of PDF bytes.

- **C-proposal-signed-rate** — **`proposal-signed` leaks token-validity oracle via distinct error codes + no rate limit**
  - `supabase/functions/proposal-signed/index.ts:79`
  - Evidence: `if (msg.includes("ALREADY_SIGNED")) return jsonResp(409, { error: "ALREADY_SIGNED" }, corsHeaders); if (msg.includes("INVALID_TOKEN")) return jsonResp(403, { error: "Forbidden" }, corsHeaders); if (msg.includes("INVALID_SIGNER_NAME")) return jsonResp(400, { error: "INVALID_SIGNER_NAME" }, corsHeaders);`
  - Distinct 409/ALREADY_SIGNED vs 403/INVALID_TOKEN lets an unauthenticated attacker distinguish valid-but-consumed signing tokens from invalid ones. `signing_token` is a UUID (brute-force impractical), but the oracle still reveals which proposals have been signed. Public endpoint has no rate limit. Carry-over.
  - Spec: OWASP-ASVS V11.1.4, OWASP A07, CWE-204.
  - Fix: return identical 403 for both ALREADY_SIGNED and INVALID_TOKEN (log the distinction server-side); add per-IP rate limit.

- **C-proposal-signed-error** — **`proposal-signed` returns raw `error.message` to unauth callers**
  - `supabase/functions/proposal-signed/index.ts:144`
  - Evidence: `} catch (error) { console.error("proposal-signed error:", (error as Error).message); return new Response(JSON.stringify({ error: (error as Error).message }), { ... });`
  - Any thrown exception (JSON parse, network to Resend, Postgres constraint text) returned verbatim to the anonymous caller. Combines badly with the token oracle above. Carry-over.
  - Spec: OWASP A05, OWASP-ASVS V7.4.1.
  - Fix: `return jsonResp(500, { error: "Sign failed" }, corsHeaders);`.

- **C-invite-user-sender** — **`invite-user` passes body-supplied `senderEmail` to Resend without verified-domain gate**
  - `supabase/functions/invite-user/index.ts:153`
  - Evidence: `body: JSON.stringify({ from: senderEmail || "noreply@salescommand.app", to: email, subject: \`${senderName || "Your team"} invited you to Sales Command\`, ... });`
  - `send-invoice`/`send-pay-app`/`send-proposal` all gate `senderEmail` against `VERIFIED_DOMAINS`; `invite-user` does not. Unverified From is either rejected by Resend (invite silently fails) or, if the domain is verified but the caller isn't the rightful sender, allows within-domain spoofing for phishing. Carry-over.
  - Spec: OWASP A08.
  - Fix: mirror the `VERIFIED_DOMAINS.includes(senderDomain) ? senderEmail : noreply` pattern from `send-invoice:279`.

- **C9** — **Broad `error.message` leakage in terminal catch across 19 functions**
  - `supabase/functions/qb-sync-invoice/index.ts:456` (representative; also `send-invoice:551, send-pay-app:504, qb-record-payment:193, qb-void-invoice:195, qb-search-customers:123, qb-link-customer:149, qb-create-job:317, qb-auth:196, invite-user:191, delete-user:120, deactivate-user:130, deactivate-payment-link:99, create-billing-session:210, send-proposal:220, extract-sov:194, reset-password:145, check-orphan-users:33/60, proposal-signed:144`)
  - Evidence: `return new Response(JSON.stringify({ error: error.message }), { headers: { ...corsHeaders, "Content-Type": "application/json" }, status: 500, });`
  - Postgres error text (column/constraint names), Stripe errors (price/customer IDs), QB errors (realm-side codes) leak to callers. `send-pay-app` uses `sanitizeProviderError` — the helper exists and is unused elsewhere. Carry-over.
  - Spec: OWASP A05, OWASP-ASVS V7.4.1.
  - Fix: hoist `sanitizeProviderError` to `_shared/errors.ts` and route every terminal catch through it.

- **C10** — **`send-invoice` returns raw Stripe/Resend response bodies to caller**
  - `supabase/functions/send-invoice/index.ts:349` (Stripe), `:467` (Resend main)
  - Evidence (:349): `if (!stripeRes.ok) { return new Response(JSON.stringify({ error: \`Stripe error: ${stripeData.error?.message || "Unknown"}\` }), { ... status: 500 }); }`
  - Resend rejection payloads include internal message IDs and the From address; Stripe errors surface price-id/customer-id fragments. `send-pay-app` has `sanitizeProviderError` — not applied here. Carry-over.
  - Spec: OWASP A05.
  - Fix: reuse the `sanitizeProviderError` helper on both response bodies before returning to caller.

- **F1** — **Proposal Summary Total fallback uses stale `proposals.total`**
  - `src/components/ProposalDetail.jsx:1299` (moved from :1195)
  - Evidence: `const sov = sovContractSum != null && sovContractSum > 0 ? sovContractSum : null; const fallback = parseFloat(p.total) || 0; const value = sov != null ? sov : fallback;`
  - Data Integrity Rule 2 forbids this — the summary total MUST be computed from live WTC data via `calcWtcPrice()`. `proposals.total` is only refreshed by `handleSave`/`handleLock`/`toggleWtcLock`; if a WTC is edited without re-save the summary shows a stale number. Line moved once more since 2026-06-28 (1195 → 1299) but the code is unchanged. The live WTC breakdown block below (1310-1363) already computes from `calcWtcBreakdown` — same expression can be reused.
  - Spec: `CLAUDE.md` Data Integrity Rule 2.
  - Fix: `const fallback = wtcs.length ? wtcs.reduce((s,w) => s + calcWtcPrice(w, undefined, usesExactPricing(p)), 0) : (parseFloat(p.total)||0);`

- **A1** — **Hardcoded Supabase URL fallback in public client**
  - `src/lib/supabasePublic.js:16`
  - Evidence: `import.meta.env.VITE_SUPABASE_URL || "https://pbgvgjjuhnpsumnowuym.supabase.co"`
  - A misbuilt non-prod deploy silently routes to prod. Also masks the failure mode where the app should fail loud on a missing env var. Carry-over.
  - Spec: OWASP-ASVS V14.2.1.
  - Fix: drop the fallback; throw on missing env at module load.

- **A2** — **Hardcoded publishable-key fallback in public client**
  - `src/lib/supabasePublic.js:20`
  - Evidence: `import.meta.env.VITE_SUPABASE_ANON_KEY || "sb_publishable_LL6wYC7kJDPQfGYeKAaLaWKoK"`
  - Rotation hazard; baked-in fallback ships in the bundle. After a key rotation, misconfigured deploys keep working against the old key rather than failing loud. Carry-over.
  - Spec: OWASP-ASVS V14.2.1.
  - Fix: remove the literal; require the env var.

- **A1b** — **Hardcoded URL fallback on Login forgot-password path**
  - `src/pages/Login.jsx:59` (moved from :58)
  - Evidence: `` `${import.meta.env.VITE_SUPABASE_URL || "https://pbgvgjjuhnpsumnowuym.supabase.co"}/functions/v1/reset-password` ``
  - Same shape as A1 on the auth surface. Carry-over.
  - Fix: import the shared supabase client and drop the literal.

- **A2b** — **Hardcoded publishable-key fallback on Login (DIFFERENT literal than A2 — drift persists)**
  - `src/pages/Login.jsx:62` (moved from :61)
  - Evidence: `apikey: import.meta.env.VITE_SUPABASE_ANON_KEY || "sb_publishable_v7XktVvkAlX7y5f6xoFjng_AaLaWKoK"`
  - The literal here (`v7XktVvkAlX7y5f6xoFjng_…`) still differs from the literal in `supabasePublic.js:20` (`LL6wYC7kJDPQfGYeKAaLaWKoK`) — one is definitely stale/orphan and an operator rotating one location would think the job was done. Exact rotation hazard this finding has been calling out for multiple cycles. Carry-over.
  - Spec: OWASP-ASVS V14.2.1.
  - Fix: remove both literals; require the env var and import the configured client.

- **E-react-router** — **`react-router-dom@7.13.1` has 7 published advisories; version has slipped further from patched line**
  - `package-lock.json:2638` (pulls in react-router 7.13.1 via react-router-dom 7.13.1)
  - Evidence: `"react-router": "7.13.1"` — GHSA-49rj-9fvp-4h2h HIGH 8.1 (turbo-stream RCE), GHSA-8646-j5j9-6r62 HIGH 8.0 (RSC XSS via javascript: redirect), GHSA-8x6r-g9mw-2r78 HIGH 7.5 (__manifest DoS), GHSA-rxv8-25v2-qmq8 HIGH 7.5 (single-fetch DoS), plus moderates for open-redirect, prerender-XSS, CSRF on PUT/PATCH/DELETE document requests.
  - Reachability: nil in this app. Every advisory code path is a Remix server-mode feature (turbo-stream, single-fetch, unstable RSC, __manifest, document-request revalidation). This repo is a Vite SPA using BrowserRouter + Routes only. Advisory count grew from 5→7 since 2026-06-28 and version is now 2 minors behind the patched line — pin risk keeps growing while the exploit surface stays closed. Carry-over.
  - Spec: OWASP A06.
  - Fix: `npm install react-router-dom@^7.16.0`; verify SPA routing post-upgrade.

- **E1** — **`ws@8.19.0` carries HIGH memory-exhaustion DoS + uninit-memory disclosure (dev-only)**
  - `package-lock.json:3025` (transitive via vite HMR)
  - GHSA-96hv-2xvq-fx4p HIGH 7.5 memory-exhaustion DoS (fixed in 8.21.0) + GHSA-58qx-3vcg-4xpx MODERATE uninit-memory disclosure (fixed in 8.20.1). Only used by vite's HMR WebSocket during `vite dev`; not shipped to prod. Same version as 2026-06-28 (no upgrade).
  - Spec: OWASP A06.
  - Fix: `npm i vite@^8.0.16` which will pull `ws` to ≥8.21.0; verify with `npm ls ws`.

- **E3** — **`vite@8.0.10` has HIGH `server.fs.deny` bypass on Windows (dev-only)**
  - `package-lock.json:2934` (devDependency)
  - GHSA-fx2h-pf6j-xcff HIGH 7.5 fs.deny bypass via Windows alternate paths + GHSA-v6wh-96g9-6wx3 MODERATE launch-editor NTLMv2 hash disclosure via UNC path. Both require the Vite dev server running on Windows. Vercel builds on Linux. Carry-over from 2026-06-28.
  - Spec: OWASP A06.
  - Fix: bump devDependency to `vite@^8.0.16`.

- **E2** — **`pdf-lib@1.17.1` unmaintained (~4 yrs stale) on pay-app PDF path**
  - `package-lock.json:2480`
  - Last release May 2022. Sole PDF engine on `src/lib/payAppPdf.js` (which processes financial data emailed to customers). No open CVE but a parser/renderer bug found upstream will not be patched. Carry-over.
  - Spec: OWASP A06.
  - Fix: consolidate pay-app PDF generation onto `jspdf` (already used in `src/lib/invoicePdf.js` / `src/lib/sovPdf.js`) so the security-critical PDF surface has one maintained library.

### Low

- **C-stripe-webhook-reclaim** — **`stripe-webhook` 'reclaim' branch can double-invoke `qb-record-payment`**
  - `supabase/functions/stripe-webhook/index.ts:101`
  - Evidence: `// 'claimed' (or unreadable): a prior delivery did not finish → RE-PROCESS. console.log("Re-processing previously-claimed Stripe event:", event.id);`
  - If a prior delivery reached the qb-record-payment fire-and-forget (line 172) but crashed BEFORE `markDone()` at :262, Stripe retries. Claim row is 'claimed', we re-process — invoking another qb-record-payment. QB Payment API is not idempotent on the SC side; a duplicate Payment is created. `qb-record-payment` does not check `invoice.qb_payment_id` before creating. Carry-over.
  - Spec: OWASP A04.
  - Fix: before payment update at :131, check `invoice.qb_payment_id`; if set, skip. Better: `qb-record-payment` itself early-returns if `invoice.qb_payment_id` already populated.

- **C-send-invoice-sender** — **`send-invoice` sender-notification `to` = body-supplied `senderEmail` without ownership check**
  - `supabase/functions/send-invoice/index.ts:520`
  - Evidence: `body: JSON.stringify({ from: fromAddress, to: senderEmail, subject: \`Invoice Sent — #${invoiceId} (${customerName})\`, ... });`
  - Verified-domain gate at :503 restricts the DOMAIN of `senderEmail`; the local-part is never checked against caller's `team_members.email`. Any caller can supply any address on `hdspnv.com`/`scmybiz.com`/`schmybiz.com`/`salescommand.app` and have the invoice-metadata notification (recipient list, amount, breakdown) sent there. Exfil vector inside a compromised tenant. Carry-over.
  - Spec: OWASP A01.
  - Fix: resolve `senderEmail` from `team_members WHERE auth_id = caller.userId` (`send-pay-app:218` pattern).

- **C-check-orphan-error** — **`check-orphan-users` leaks Postgres `error.message` from cron-gated handler**
  - `supabase/functions/check-orphan-users/index.ts:33`
  - Evidence: `if (error) { console.error("Failed to query orphan users:", error.message); return new Response(JSON.stringify({ error: error.message }), { status: 500 }); }`
  - Cron-gated (limited scope), but raw error.message from `rpc('get_orphan_auth_user_count')` — SQL/hints/column details — is returned. Low-risk; defense-in-depth. Carry-over.
  - Spec: OWASP A05.
  - Fix: return generic `"orphan-user check failed"`, keep `console.error`.

- **C-deactivate-payment-link-cors** — **`deactivate-payment-link` catch drops CORS headers**
  - `supabase/functions/deactivate-payment-link/index.ts:99`
  - Evidence: `} catch (error) { console.error("deactivate-payment-link error:", error.message); return new Response(JSON.stringify({ error: error.message }), { headers: { "Content-Type": "application/json" }, status: 500, }); }`
  - Missing `...corsHeaders` — the browser hides the 5xx body. UI can't surface state; operator retries unnecessarily or thinks deactivation succeeded. Every other catch in the file spreads corsHeaders. Carry-over.
  - Spec: OWASP-ASVS V14.5.3.
  - Fix: `headers: { ...corsHeaders, "Content-Type": "application/json" }` (and route error.message through `sanitizeProviderError`).

- **C18** — **`_shared/cors.ts` omits `Access-Control-Allow-Methods`**
  - `supabase/functions/_shared/cors.ts:57`
  - Evidence: `return { "Access-Control-Allow-Origin": resolveAllowedOrigin(req, extraOrigins), "Access-Control-Allow-Headers": allowHeaders, };`
  - Preflight responses from every function ship no Allow-Methods header. Browsers default to permitting the simple set (GET/POST/HEAD) but strict security configurations fail preflight for non-simple content types. File's own comment acknowledges this is filed as C18. Carry-over.
  - Spec: OWASP-ASVS V14.5.3.
  - Fix: add `"Access-Control-Allow-Methods": "POST, OPTIONS"` in the returned headers object.

- **A5** — **`stripe-webhook` logs full recipient email on every payment notification**
  - `supabase/functions/stripe-webhook/index.ts:49`
  - Evidence: `console.log("Email to", to, ":", res.status);`
  - Customer email persisted to Edge Function logs on every paid-invoice notification. Concentrates payer PII outside DB's tenant-scoped access controls. Carry-over.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09.
  - Fix: `console.log("Email dispatched:", res.status);` — drop the recipient value.

- **A-send-pay-app-log** — **`send-pay-app` logs recipient email on every send** (NEW)
  - `supabase/functions/send-pay-app/index.ts:99`
  - Evidence: `console.log("send-pay-app invoked", { payAppId, invoiceId, recipientEmail });`
  - Same class as A5, in a file changed since the last audit and not previously flagged. Aggregates customer email PII in the log store.
  - Spec: OWASP-ASVS V8.3.4, OWASP A09.
  - Fix: drop `recipientEmail`; keep `{ payAppId, invoiceId }`.

- **A-invite-user-log** — **`invite-user` invocation log includes invitee + inviter names/emails** (NEW)
  - `supabase/functions/invite-user/index.ts:88`
  - Evidence: `console.log("invite-user invoked", { email, name, teamMemberId, senderEmail, senderName });`
  - Every invite writes invitee email/name and inviter email/name to Edge Function logs — a queryable list of every user account and who invited them, outside the RLS-protected `team_members` table.
  - Spec: OWASP-ASVS V8.3.4, OWASP A09.
  - Fix: `console.log("invite-user invoked", { teamMemberId, hasName: !!name, hasSender: !!senderEmail });`

- **E-dompurify** — **`dompurify@3.4.1` (transitive via jspdf) has 8 advisories; HTML-to-PDF path unused** (NEW)
  - `package-lock.json:2009` (optionalDependency of jspdf 4.2.1)
  - Advisories against ≤3.4.10 (IN_PLACE bypasses, config pollution, template XSS — moderate/low). Reachability: dompurify runs only from jspdf's `.html()`/`fromHTML`/`svg`/`addSvg` codepaths; grep of `src/` shows zero invocations of those. Dead code path in this app.
  - Spec: OWASP A06.
  - Fix: no direct action required; will resolve on next `npm i jspdf@latest`. If HTML-to-PDF paths are ever added, pin `dompurify ≥3.4.11`.

## Diff vs last audit
_pending_

## Coverage
_pending_
