# Sales Command Security Audit — 2026-07-26

## Summary
_pending_

## Top 3 Urgent
_pending_

## Findings by Severity
### Critical

- **B11** — **`team_members` write policies still allow tenant self-elevation (seed unfixed)**
  - `sql/rls_identity_tables.sql:64`
  - Evidence:
    ```
    CREATE POLICY "team_members_update" ON public.team_members
      FOR UPDATE TO authenticated
      USING  (tenant_id = public.get_user_tenant_id())
      WITH CHECK (tenant_id = public.get_user_tenant_id());
    ```
  - Commit `37fa00f` ("Closes S12: team_members self-elevation lockdown deployed") only touched `docs/BACKLOG.md` (1 file, 1 insertion — verified via `git show --stat`). The prod fix landed in `command-suite-db` migration `20260722120000_team_members_role_lockdown.sql`; the seed in **this** repo was NOT updated. `grep -r is_admin_or_manager sql/` returns zero matches. Any authenticated tenant member seeded from this file can UPDATE any team_members row in their tenant, including their own `role` — Sales → Admin/Manager in one PostgREST call. This unlocks every downstream role-gated policy (tenant_config, invoice deletes, manager dashboards). Fourth consecutive audit cycle unfixed in-repo; prod is fixed but the repo/seed divergence is itself a landmine — any fresh deploy or seed re-run re-introduces the vulnerability.
  - Spec: `CLAUDE_RLS.md` role-gating convention, OWASP A01, OWASP-ASVS V8.2.
  - Fix: add `AND public.is_admin_or_manager()` to USING and WITH CHECK on `team_members_insert/_update/_delete` in this seed to mirror the `command-suite-db` migration. Or delete the vulnerable policies from this seed and depend solely on the canonical migration.

### High

- **A4** — **`invite-user` logs full magic-link `action_link` via the `linkData` object**
  - `supabase/functions/invite-user/index.ts:128`
  - Evidence: `console.log("generateLink result:", { linkData, linkErr: linkErr?.message });`
  - `linkData.properties.action_link` is a single-use magic-link sign-in credential. Logging the whole object writes the live link to Supabase function logs; anyone with log-read access can claim the account before the invitee ever opens the email. Carry-over from 2026-06-01 / 2026-06-28, still at line 128.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09.
  - Fix: `console.log("generateLink result:", { hasLink: !!linkData?.properties?.action_link, linkErr: linkErr?.message });`

- **B10** — **`tenant_config_update` lacks `is_admin_or_manager()` gate**
  - `sql/rls_identity_tables.sql:94`
  - Evidence:
    ```
    CREATE POLICY "tenant_config_update" ON public.tenant_config
      FOR UPDATE TO authenticated
      USING  (id = public.get_user_tenant_id())
      WITH CHECK (id = public.get_user_tenant_id());
    ```
  - Any authenticated tenant member (not just admin/manager) can UPDATE their `tenant_config` row — stripe_customer_id, stripe_subscription_id, subscription_status, monthly_billing_goal, yearly_billing_goal, license_number, default_burden_rate, default_tax_rate, apps[]. Pairs with D3 (Settings.jsx has no client role gate either). A Sales Rep can rewrite tenant-wide pricing defaults or point stripe_customer_id at an attacker-controlled Stripe customer. Carry-over.
  - Spec: `CLAUDE_RLS.md` role-gating, OWASP A01, OWASP A05.
  - Fix: add `AND public.is_admin_or_manager()` to both USING and WITH CHECK. Also fix D3.

- **B13** — **`proposal_signatures` DELETE policy permits audit-trail destruction by any tenant member**
  - `sql/rls_child_tables.sql:261`
  - Evidence:
    ```
    CREATE POLICY "proposal_signatures_delete" ON public.proposal_signatures
      FOR DELETE TO authenticated
      USING (tenant_id = public.get_user_tenant_id());
    ```
  - Any authenticated tenant member can DELETE any signature row — including signatures already recorded from real customer signings. Wipes legal/forensic record (signer_name, signer_email, signed_at, ip_address, pdf_url) while leaving the proposal in Sold. Non-repudiation broken. Carry-over.
  - Spec: OWASP-ASVS V7.1 (audit log integrity), OWASP A09.
  - Fix: signatures should be append-only. Drop the DELETE policy outright, or gate with `AND public.is_admin_or_manager() AND signed_at IS NULL`.

- **B-history-locker-anon-grant** — **`history_locker_phase1.sql` grants ALL to anon on the archive schema + default privileges landmine** (NEW)
  - `sql/history_locker_phase1.sql:119`
  - Evidence:
    ```
    GRANT USAGE ON SCHEMA archive TO anon, authenticated;
    GRANT ALL ON ALL TABLES IN SCHEMA archive TO anon, authenticated;
    GRANT ALL ON ALL SEQUENCES IN SCHEMA archive TO anon, authenticated;
    ALTER DEFAULT PRIVILEGES IN SCHEMA archive GRANT ALL ON TABLES TO anon, authenticated;
    ```
  - The archive schema (import_batches, legacy_records — imported customer/job history including contact info, addresses, dollars, raw_data jsonb) has ALL DML granted to anon. RLS with tenant-scoped policies filters live SELECTs today, BUT: (1) sequences do NOT respect RLS — `nextval('archive.import_batches_id_seq')` is callable by anon and can consume IDs. (2) `ALTER DEFAULT PRIVILEGES` means any FUTURE archive table gets `ALL TO anon` by default; a maintainer who forgets `ENABLE ROW LEVEL SECURITY` on a new archive table creates instant anon full-read of imported history. Least-privilege violated as a persistent landmine.
  - Spec: least privilege, `CLAUDE_RLS.md`, OWASP-ASVS V1.4.1.
  - Fix: replace with `GRANT SELECT, INSERT, DELETE ON archive.import_batches, archive.legacy_records TO authenticated;` (no anon, no ALL, no ALL SEQUENCES). Drop the `ALTER DEFAULT PRIVILEGES … TO anon` lines. If anon truly needs archive access, add explicit per-table grants next to their RLS policies.

- **D2** — **`PublicInvoicePage` joins `proposal_wtc(*)` — leaks burden_rate, markup_pct, materials cost basis to anon**
  - `src/pages/PublicInvoicePage.jsx:51`
  - Evidence: `.from("invoice_lines").select("*, proposal_wtc(*, work_types(name))").eq("invoice_id", inv.id);`
  - `proposal_wtc` columns include burden_rate, ot_burden_rate, tax_rate, regular_hours, ot_hours, markup_pct, materials (jsonb with unit costs), sub_areas, travel, discount, sales_sow. Every customer viewing an invoice sees the full internal cost basis + margin in their Network tab. D1 (invoices select(\*)) was fixed via explicit column list + column-level REVOKE/GRANT, but D2's sibling `proposal_wtc` leak was left open. Carry-over since 2026-06-01.
  - Spec: `CLAUDE.md`, `CLAUDE_RLS.md`, OWASP-ASVS V5.1.3, OWASP A01.
  - Fix: narrow to `proposal_wtc(id, work_types(name), locked_line_total)`; use the server-computed locked total instead of running `calcWtcPrice` client-side. Follow with `REVOKE SELECT ON public.proposal_wtc FROM anon; GRANT SELECT (id, work_type_id, locked_line_total) TO anon;`.

- **C-requireAdminOrManager-case** — **`requireAdminOrManager` role check is case-mismatched — gate rejects every real admin** (NEW)
  - `supabase/functions/_shared/tenantAuth.ts:83`
  - Evidence:
    ```
    const role = tm?.role;
    if (role !== "admin" && role !== "manager") {
      return { ok: false, status: 403, reason: "Admin or manager role required" };
    }
    ```
  - DB stores `team_members.role` PascalCase (`"Admin"`, `"Manager"`, `"Sales Rep"`) — verified via `src/pages/Home.jsx:105` `["Admin","Manager"].includes(displayRole)`, `SalesDash.jsx:89`, and every other client role check. The gate compares lowercase strings, so every real user fails — the only consumer (`qb-auth` action ∈ {exchange, refresh}) 403s every admin/manager. Two consequences: (1) QB OAuth exchange/refresh from the browser is broken — no admin can connect/refresh QB via the UI. (2) The gate cannot be trusted for any future function; anyone reading the codebase would assume it enforces the intended role. A defense-in-depth control that exists in name only.
  - Spec: OWASP-ASVS V4.1.3, CWE-1288.
  - Fix: `if (role !== "Admin" && role !== "Manager")` — match DB casing. Or normalize both sides: `const r = String(role||"").toLowerCase(); if (r !== "admin" && r !== "manager")`. Add a smoke test exercising qb-auth exchange with a seeded Admin.

- **C-reset-password** — **`reset-password` references undeclared `origin` — ReferenceError leaked verbatim, Schedule Command branding still broken**
  - `supabase/functions/reset-password/index.ts:66`
  - Evidence: `const isSchedule = origin.includes("schmybiz") || origin.includes("schedulecommand");` — verified lines 1-65: `origin` is never declared, imported, or read from `req.headers`.
  - Same as 2026-06-28 — not fixed. In Deno Edge there is no `globalThis.origin`; the reference throws ReferenceError, the outer catch stringifies `error.message` back to the caller (leaks the broken-code identifier), and the function silently fails to send. SCC users still never get SCC-branded reset emails.
  - Spec: OWASP A05, OWASP A09, OWASP-ASVS V5.1.5, V7.4.1.
  - Fix: `const origin = req.headers.get("origin") || "";` at function top. Collapse the catch to return `{ error: "Internal error" }` with a server-logged correlation id.

- **C-qb-auth** — **`qb-auth` has `verify_jwt = false` at Supabase platform layer**
  - `supabase/config.toml:14`
  - Evidence: `[functions.qb-auth]\nenabled = true\nverify_jwt = false`
  - Platform-level JWT gate off. The handler does invoke role/tenant checks, but every other admin function relies on the platform gate as defense in depth. Combined with the case-mismatch above (which effectively disables the app-layer admin gate), a single regression to `authenticateCaller` becomes an anonymous cross-tenant OAuth mint. Carry-over.
  - Spec: Supabase production checklist, OWASP-ASVS V4.1.5.
  - Fix: set `verify_jwt = true`; keep in-handler role checks as additional defense.

- **C-invite-user-sender** — **`invite-user` passes caller-supplied `senderEmail` to Resend `From` with no verified-domain gate**
  - `supabase/functions/invite-user/index.ts:153`
  - Evidence: `from: senderEmail || "noreply@salescommand.app",`
  - Unlike `send-invoice`/`send-pay-app`/`send-proposal` (which gate `From` against `VERIFIED_DOMAINS`), invite-user does neither a domain gate NOR a caller-ownership check. `senderEmail` from `req.json()` used verbatim. Tenant-self admins can spoof colleagues within a verified domain. Carry-over.
  - Spec: OWASP-ASVS V5.1.3, CWE-345.
  - Fix: resolve senderEmail server-side from `team_members.email WHERE auth_id = user.id`; ignore body-supplied senderEmail.

- **C16** — **`qb-record-payment` service-role path trusts caller-supplied `invoiceId`**
  - `supabase/functions/qb-record-payment/index.ts:86`
  - Evidence: `const { invoiceId } = await req.json(); ... if (!caller.isServiceRole && invoice.tenant_id !== caller.tenantId) ...` — the service-role branch SKIPS the tenant check.
  - For user callers the gate is correct; for the service-role path (stripe-webhook) there is no cross-validation that the Stripe event was about this invoice. Combined with C-stripe-webhook-reclaim (below), an at-least-once redelivery duplicates the QB Payment. Any future internal service-role caller becomes a phantom-payment primitive. Carry-over.
  - Spec: OWASP-ASVS V13.2.1, CWE-345.
  - Fix: `stripe-webhook` should pass `originating_tenant_id` explicitly; `qb-record-payment` asserts equality. Also skip if `invoice.qb_payment_id` is already set.

- **C-qb-sync-invoice-update-gate** — **`qb-sync-invoice` in-place UPDATE path has no admin/manager gate; any Sales Rep can re-write a QB invoice + inject audit-trail text** (NEW)
  - `supabase/functions/qb-sync-invoice/index.ts:115`
  - Evidence:
    ```
    const caller = await authenticateCaller(sb, req, SUPABASE_SERVICE_ROLE_KEY);
    ...
    const { invoiceId, editReason } = await req.json();
    ```
  - Shipped this cycle with the in-place edit feature. Uses `authenticateCaller`, not `requireAdminOrManager`. Then at line 381-424 performs a `sparse:false` full-replacement PUT to QB against the existing `qb_invoice_id`, with caller-supplied `editReason` appended to `PrivateNote`. Any Sales Rep can (a) mutate a QB-recorded invoice's lines/amount/description/CustomerMemo, and (b) inject arbitrary text into the QB audit trail as `[EDITED] ... — {editReason}`. Same class as `C-qb-void-invoice` plus the in-place-edit surface.
  - Spec: OWASP-ASVS V4.2.1, CWE-863, `CLAUDE.md` Data Integrity Rule 6.
  - Fix: switch to `requireAdminOrManager` for the UPDATE branch (after fixing the case-mismatch above). Cap `editReason` length and strip control chars before writing to PrivateNote.

- **F-PayAppDetailModal-cascade** — **`handleSaveLines` updates `invoices.amount` but never propagates to `invoice_lines.amount` (Rule 6 cascade trap)**
  - `src/components/PayAppDetailModal.jsx:233`
  - Evidence (lines 238-254): iterates and updates `billing_schedule_pay_app_lines` rows (billed_pct/billed_amount), updates `billing_schedule_pay_apps` header (this_app_amount, retainage_withheld, current_payment_due), and updates `invoices` (amount, retention_amount) — but never UPDATEs `invoice_lines` rows for the same invoice.
  - Carry-over from 2026-06-28. `PublicInvoicePage.jsx` and `Invoices.jsx` InvoiceDetail render `line.amount` from `invoice_lines`, not `billing_schedule_pay_app_lines`. After a pay-app line edit, the per-line dollars the customer sees on the public page (and on the PDF) are stale while the header total is fresh. The invoice_lines rows are inserted by `NewPayAppModal.handleCreate` keyed by `billing_schedule_line_id + invoice_id`, so the join key exists — this save just omits the propagation. The canonical Data Integrity Rule 6 trap inverted (recompute upstream, fail to cascade to the display copy).
  - Spec: `CLAUDE.md` Data Integrity Rule 6.
  - Fix: after updating `billing_schedule_pay_app_lines`, iterate updated rows and `UPDATE invoice_lines SET amount = pl.billed_amount_this_app, billing_pct = pl.billed_pct_this_app WHERE invoice_id = payApp.invoice_id AND billing_schedule_line_id = pl.billing_schedule_line_id`. Guard the fallthrough for missing invoice_lines rows (preserve, don't zero).

- **F1** — **Proposal Summary Total fallback uses stale `proposals.total`**
  - `src/components/ProposalDetail.jsx:1299`
  - Evidence:
    ```
    const sov = sovContractSum != null && sovContractSum > 0 ? sovContractSum : null;
    const fallback = parseFloat(p.total) || 0;
    const value = sov != null ? sov : fallback;
    ```
  - Carry-over. CLAUDE.md Data Integrity Rule 2 forbids reading `proposals.total` for the on-screen summary — must be derived from live WTC data via `calcWtcPrice()`. `proposals.total` is only refreshed inside `toggleWtcLock` and `WTCCalculator.handleSave`; a WTC edit that never saves leaves this fallback displaying stale dollars while the WTC card just above shows the live number. Line moved 1195 → 1299 this cycle.
  - Spec: `CLAUDE.md` Data Integrity Rule 2.
  - Fix: `const fallback = wtcs.reduce((s, w) => s + calcWtcPrice(w, undefined, usesExactPricing(p)), 0);` — same expression already computed a few lines below (breakdowns.reduce at line 1313).

### Medium

- **B-db_hardening-stale-tenant-fn** — **`db_hardening_phase1.sql` defines `get_user_tenant_id()` as `SELECT id FROM tenant_config LIMIT 1` — cross-tenant landmine on re-run** (NEW)
  - `sql/db_hardening_phase1.sql:12`
  - Evidence:
    ```
    CREATE OR REPLACE FUNCTION public.get_user_tenant_id()
    RETURNS uuid AS $$
      SELECT id FROM public.tenant_config LIMIT 1;
    $$ LANGUAGE sql SECURITY DEFINER STABLE;
    ```
  - Pre-hardening body that migration `20260509120000` removed in prod. Overwritten by the auth-aware body in `rls_identity_tables.sql:129` / `rls_cleanup_and_remaining.sql:377` when seeds run in order — but if an operator re-runs `db_hardening_phase1.sql` in isolation (e.g. to reset an index), the function silently reverts to returning the first tenant for EVERY caller. Every RLS policy on every table then evaluates true for everyone; single-tenant assumptions become cross-tenant reads/writes on any second tenant provisioned. Also lacks `SET search_path = public` (trojan-search-path risk under SECURITY DEFINER).
  - Spec: `CLAUDE_RLS.md` tenant isolation, OWASP-ASVS V5.1.1 (search-path fixation).
  - Fix: replace with the auth-aware body used in `rls_identity_tables.sql:129` (`SELECT tenant_id FROM team_members WHERE auth_id = auth.uid() AND active LIMIT 1`, with `SET search_path = public`); or delete the definition here and depend on the canonical file.

- **C9** — **Verbose `error.message` returned to callers across 19 edge functions** (persistent)
  - Pattern across `supabase/functions/{qb-sync-invoice:456, qb-void-invoice:195, qb-record-payment:193, qb-create-job:317, qb-link-customer:149, qb-search-customers:123, qb-auth:196, send-invoice:551, send-pay-app:504, send-proposal:221, create-billing-session:211, deactivate-user:131, delete-user:121, invite-user:191, reset-password:145, deactivate-payment-link:99, extract-sov:194, proposal-signed:144, stripe-webhook:270, scc-stripe-webhook:188, check-orphan-users:33+61}`.
  - Every one still ships `return new Response(JSON.stringify({ error: error.message }), ...)`. Leaks Postgres constraint/column names, QB realm/customer/document IDs, Stripe payment-intent IDs. `send-pay-app` has a local `sanitizeProviderError` — never promoted to `_shared/errors.ts`.
  - Spec: OWASP-ASVS V7.4.1, OWASP A09.
  - Fix: promote `sanitizeProviderError` to `_shared/errors.ts`; replace every `error.message` in a Response body with `"Internal error"` + a server-logged correlation id.

- **C10** — **`send-invoice` returns raw Stripe / Resend response bodies verbatim**
  - `supabase/functions/send-invoice/index.ts:349` (Stripe), `:467` (main-Resend), `:491` (viewer-Resend).
  - Three verbatim upstream-response leaks: raw Stripe error.message to caller, entire Resend response body on main-recipient failure, Resend text in warnings array. `send-pay-app` has `sanitizeProviderError` — send-invoice was not updated after the invoice-recipients refactor. Carry-over.
  - Spec: OWASP-ASVS V7.4.1, OWASP A09.
  - Fix: import/promote `sanitizeProviderError`; apply to the Stripe + main-Resend + viewer-Resend branches.

- **C-invite-user-listusers** — **`invite-user` lists every `auth.users` entry across tenants (one page only)**
  - `supabase/functions/invite-user/index.ts:102`
  - Evidence: `const { data: { users } } = await supabase.auth.admin.listUsers(); const existing = users?.find((u: any) => u.email === email);`
  - Defaults to 50/page; iterates one. Cross-tenant enumeration primitive AND a correctness bug — on any instance with >50 auth users the duplicate check silently misses matches past page 1. Carry-over.
  - Spec: OWASP-ASVS V8.3.4, OWASP A01.
  - Fix: `supabase.auth.admin.getUserByEmail(email)`; verify tenant.

- **C-reset-password-listusers** — **`reset-password` uses the same one-page `listUsers` anti-pattern**
  - `supabase/functions/reset-password/index.ts:55`
  - Defeats the anti-enumeration property on instances with >50 users — password reset silently 200s for real users past page 1 (they don't get the email). Carry-over.
  - Spec: OWASP-ASVS V8.3.4.
  - Fix: `supabase.auth.admin.getUserByEmail(email)`.

- **C-deactivate-user-count** — **`deactivate-user` counts `call_log` rows across tenants**
  - `supabase/functions/deactivate-user/index.ts:119`
  - Evidence: `.from("call_log").select("id", { count: "exact", head: true }).eq("sales_name", member.name);`
  - No tenant filter — sales-rep name collision returns another tenant's row count. Small cross-tenant metadata leak + wrong `unassignedJobs` display. Carry-over.
  - Spec: OWASP A01.
  - Fix: `.eq("tenant_id", callerTenantId)`.

- **C-create-billing-session** — **`create-billing-session` lacks admin/manager gate**
  - `supabase/functions/create-billing-session/index.ts:57`
  - Any active tenant member can call action=checkout (mints Stripe subscription, writes `stripe_customer_id`) or action=portal. With B10, a Sales Rep can subscribe/unsubscribe the tenant. Carry-over.
  - Spec: OWASP-ASVS V4.2.1.
  - Fix: switch to `requireAdminOrManager` (after fixing the case-mismatch).

- **C-qb-search-customers** — **`qb-search-customers` no admin/manager gate**
  - `supabase/functions/qb-search-customers/index.ts:86`
  - Any active team_member can enumerate the tenant's full QB customer list. Carry-over.
  - Spec: OWASP-ASVS V4.2.1.
  - Fix: switch to `requireAdminOrManager`.

- **C-qb-void-invoice** — **`qb-void-invoice` no admin/manager gate**
  - `supabase/functions/qb-void-invoice/index.ts:63`
  - Voiding mutates QB audit trail permanently; any active team_member can trigger. Carry-over.
  - Spec: OWASP-ASVS V4.2.1, CWE-863.
  - Fix: `requireAdminOrManager` (mandatory for void).

- **C-extract-sov** — **`extract-sov` has no rate limit on paid Anthropic API calls**
  - `supabase/functions/extract-sov/index.ts:159`
  - `claude-opus-4-7` + `max_tokens: 16000`. Ownership check but no per-tenant rate limit. Compromised account drains Anthropic budget. Carry-over.
  - Spec: OWASP-ASVS V11.1.4, CWE-770.
  - Fix: per-tenant rate limit (~10/hr), daily cap, cache by SHA-256(pdfBytes).

- **C-proposal-signed-rate** — **`proposal-signed` has no rate limit + token-validity oracle**
  - `supabase/functions/proposal-signed/index.ts:79`
  - Public endpoint, no per-IP throttle; differentiated `INVALID_TOKEN` (403) vs `ALREADY_SIGNED` (409) returns enable an oracle for enumerating consumed vs invalid tokens. Carry-over.
  - Spec: OWASP-ASVS V11.1.4, CWE-307.
  - Fix: per-IP rate limit (~10/min); collapse both errors into a single generic 403.

- **C-proposal-signed-error** — **`proposal-signed` catch returns raw `error.message` to unauth callers**
  - `supabase/functions/proposal-signed/index.ts:144`
  - Leaks Postgres/RPC error text. Carry-over.
  - Spec: OWASP-ASVS V7.4.1.
  - Fix: return `{ error: "Sign failed" }`; keep detail in console.error only.

- **C-qb-record-payment-net** — **`netAmount = amount - discount` ignores retention; QB Payment posted at wrong amount on retention invoices**
  - `supabase/functions/qb-record-payment/index.ts:153`
  - Evidence: `const netAmount = (invoice.amount || 0) - (invoice.discount || 0);`
  - `send-invoice:161` computes the Stripe-charged amount as `amount - discount - retentionAmount`. QB Payment recorded for MORE than the customer paid on retention-bearing invoices — the delta becomes phantom cash in QB books. Money-correctness. Carry-over.
  - Spec: OWASP A04.
  - Fix: subtract `retention_amount` — or use `session.amount_total / 100` from the webhook payload.

- **C-send-invoice-sender** — **`send-invoice` trusts caller-supplied `senderEmail` for sender-notification `To`**
  - `supabase/functions/send-invoice/index.ts:278`
  - `From` is verified-domain-gated, but `senderEmail` is used at line 520 as the `to` for the sender-notification email without ownership check. Any caller can direct a colleague's copy of the send-notification (with recipient list, amounts) to any address on a verified domain. Confused-deputy. Carry-over.
  - Spec: OWASP-ASVS V5.1.3, CWE-345.
  - Fix: resolve senderEmail server-side from `team_members WHERE auth_id = caller.userId`; ignore body-supplied.

- **C-stripe-webhook-reclaim** — **`stripe-webhook` re-process branch can double-record QB Payment**
  - `supabase/functions/stripe-webhook/index.ts:101`
  - Evidence: `// 'claimed' (or unreadable): a prior delivery did not finish → RE-PROCESS.`
  - The re-process branch re-invokes `qb-record-payment`, which doesn't check `invoice.qb_payment_id` before POSTing. `processed_stripe_events` fixes invoice-status idempotency but not the downstream QB call. Stripe at-least-once redelivery duplicates the QB Payment. Low probability but exploitable. Carry-over.
  - Fix: in `qb-record-payment` skip if `invoice.qb_payment_id` is set.

- **B-viewing-token-echo** — **`PublicInvoicePage` echoes back its own `viewing_token` in the SELECT list** (NEW)
  - `src/pages/PublicInvoicePage.jsx:40`
  - Evidence: `.select(\`id, proposal_id, job_id, job_name, ..., voided_at, viewing_token, proposals(...)\`)`
  - The anon page fetches back its own bearer token. Caller already presented it in `x-viewing-token`, but the token is now embedded in a JSON response that browser history, disk cache, transport-layer captures, or DOM-inspecting extensions can reuse (per prior audits, viewing tokens don't rotate). Minor best-practice violation of not echoing bearer tokens back to their holder.
  - Spec: OWASP-ASVS V3.5.3.
  - Fix: drop `viewing_token` from the SELECT list — the page doesn't use it after the fetch (verified).

- **E3** — **`vite@8.0.10` has server.fs.deny bypass on Windows + launch-editor NTLMv2 disclosure (dev-only)**
  - `package-lock.json:2921`
  - Evidence: `"node_modules/vite": { "version": "8.0.10", "dev": true }`
  - GHSA-fx2h-pf6j-xcff (HIGH, Windows fs.deny bypass) + GHSA-v6wh-96g9-6wx3 (moderate NTLMv2 hash disclosure via launch-editor). devDependency only; Vercel serves the static `vite build` output — dev server never runs in prod. Downgraded from HIGH per SPA-only reachability. Carry-over.
  - Spec: OWASP A06.
  - Fix: `npm i -D vite@latest` (target ^8.0.16 or later).

- **E1** — **`ws@8.19.0` — memory-exhaustion DoS + uninitialized-memory disclosure (dev-only)**
  - `package-lock.json:3024`
  - Evidence: transitively via `@supabase/realtime-js@2.99.0` which declares `"ws": "^8.18.2"`.
  - GHSA-96hv-2xvq-fx4p (HIGH tiny-fragment DoS) + moderate uninit-memory disclosure. `ws` is Node-only; the browser bundle uses native `WebSocket` — realtime-js only loads `ws` in Node runtimes. Not reachable in shipped code. Carry-over.
  - Spec: OWASP A06.
  - Fix: wait for a `@supabase/supabase-js` bump pulling `ws>=8.21.0`, or `npm dedupe`/override once a fix cascades. Not blocking.

- **E-react-router** — **`react-router-dom@7.13.1` — 9 advisories; only the open-redirect one is client-reachable**
  - `package-lock.json:2632`
  - Evidence: `"node_modules/react-router-dom": { "version": "7.13.1" }`. `src/App.jsx` uses `BrowserRouter` (client routing only) — no RSC/prerender/single-fetch/data routers.
  - 8 of 9 advisories target SSR/RSC/manifest paths that BrowserRouter SPAs never execute. Only reachable one: GHSA-wrjc-x8rr-h8h6 (moderate) — open redirect via backslash in `<Link>`/`useNavigate`. `navigate(...)` is never called with attacker-controlled URL params (verified: only `navigate(location.pathname+location.search)` at `Invoices.jsx:2819`, not user-supplied). Downgraded to MEDIUM.
  - Spec: OWASP A06.
  - Fix: `npm i react-router-dom@^7.18.0` — closes open-redirect + all SSR CVEs. Non-urgent given usage.

- **E2** — **`pdf-lib@1.17.1` unmaintained (~5 yrs stale) in pay-app PDF path**
  - `package-lock.json:2479`
  - Evidence: `"node_modules/pdf-lib": { "version": "1.17.1" }`; only used in `src/lib/payAppPdf.js` (template merge, not cryptographic signing). Proposal signing PDFs use jsPDF (PublicSigningPage). Attack surface smaller than prior E2 assumed. No active CVE. Carry-over.
  - Spec: OWASP A06.
  - Fix: monitor; consider migrating pay-app template rendering to jsPDF (already a direct dep) to drop pdf-lib entirely.

### Medium

- **A1** — **Hardcoded Supabase URL fallback in public client**
  - `src/lib/supabasePublic.js:16`
  - Evidence: `import.meta.env.VITE_SUPABASE_URL || "https://pbgvgjjuhnpsumnowuym.supabase.co";`
  - A staging/preview/misbuilt deploy with `VITE_SUPABASE_URL` unset silently routes every public signing/viewing page to prod. Defeats env-driven separation and hides missing-env misconfigurations. Carry-over.
  - Spec: OWASP-ASVS V14.2.1.
  - Fix: drop the `||` fallback; throw at module load when missing (mirror `src/lib/supabase.js`).

- **A2** — **Hardcoded publishable-key fallback in public client**
  - `src/lib/supabasePublic.js:20`
  - Evidence: `import.meta.env.VITE_SUPABASE_ANON_KEY || "sb_publishable_LL6wYC7kJDPQfGYeKAaLaWKoK";`
  - Publishable key literal baked into the bundle. Rotation without editing this file leaves prod silently on the old key. The literal in this file (`LL6wYC7kJDPQfGYeKAaLaWKoK`) differs from the sibling literal in `Login.jsx:62` (`v7XktVvkAlX7y5f6xoFjng_AaLaWKoK`) — the drift hazard has already materialized. Carry-over.
  - Spec: OWASP-ASVS V14.2.1.
  - Fix: remove the literal fallback; require the env var and throw on missing.

- **A1b** — **Hardcoded URL fallback on Login forgot-password path**
  - `src/pages/Login.jsx:59`
  - Evidence: `` `${import.meta.env.VITE_SUPABASE_URL || "https://pbgvgjjuhnpsumnowuym.supabase.co"}/functions/v1/reset-password` ``
  - Same shape as A1. Carry-over (was 58, shifted +1 this cycle).
  - Spec: OWASP-ASVS V14.2.1.
  - Fix: drop the fallback; require the env var (or import the shared client).

- **A2b** — **Hardcoded publishable-key fallback on Login (DIFFERENT literal — drift)**
  - `src/pages/Login.jsx:62`
  - Evidence: `apikey: import.meta.env.VITE_SUPABASE_ANON_KEY || "sb_publishable_v7XktVvkAlX7y5f6xoFjng_AaLaWKoK"`
  - Literal here differs from `supabasePublic.js:20` — the exact rotation drift the audit has been calling out for four cycles. Any environment relying on the Login fallback authenticates with a stale key. Carry-over (was 61, shifted +1).
  - Spec: OWASP-ASVS V14.2.1.
  - Fix: remove both literals; require the env and route the reset-password fetch through a single central helper.

### Low

- **A5** — **`stripe-webhook` logs full recipient email on every payment-receipt send**
  - `supabase/functions/stripe-webhook/index.ts:49`
  - Evidence: `console.log("Email to", to, ":", res.status);`
  - Customer email address written to function logs on every payment-receipt send. PII in logs at scale (one per paid invoice). Carry-over.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09.
  - Fix: log only the domain (`to.split("@")[1]`) or drop the value.

- **A6** — **`send-pay-app` logs recipient email on every send** (NEW)
  - `supabase/functions/send-pay-app/index.ts:99`
  - Evidence: `console.log("send-pay-app invoked", { payAppId, invoiceId, recipientEmail });`
  - Function was changed this audit window. The invocation log includes `recipientEmail` verbatim on every pay-app send. Same class of PII-in-logs leak as A5; one entry per pay app sent.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09.
  - Fix: `console.log("send-pay-app invoked", { payAppId, invoiceId, recipientDomain: recipientEmail?.split("@")[1] });`

- **A7** — **`send-invoice` logs first 200 chars of raw Resend response body** (NEW)
  - `supabase/functions/send-invoice/index.ts:465`
  - Evidence: `console.log("Main email response:", mainRes.ok, mainRes.txt.slice(0, 200));`
  - Function changed this audit window. On every invoice send, the raw Resend response body (first 200 chars) is written to function logs. Resend's success payload contains recipient email + message ID; failure payload can include tenant/error identifiers. One entry per invoice sent.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09.
  - Fix: `console.log("Main email response:", { ok: mainRes.ok, status: mainRes.status });` — keep the body slice behind a debug flag or drop it.

- **C18** — **`buildCorsHeaders` omits `Access-Control-Allow-Methods`**
  - `supabase/functions/_shared/cors.ts:12`
  - Comment acknowledges the gap ("ADJ-4 filed and out of scope"). Strict browsers may fail preflight without Allow-Methods on non-simple requests. Carry-over.
  - Spec: OWASP-ASVS V14.5.
  - Fix: add `"Access-Control-Allow-Methods": "POST, OPTIONS"`.

- **C-check-orphan-error** — **`check-orphan-users` returns raw `error.message`**
  - `supabase/functions/check-orphan-users/index.ts:33` (also `:61`)
  - Cron-secret-gated, so exposure limited — but defense-in-depth: return generic text. Carry-over.
  - Fix: `{ error: "Internal error" }` at both sites.

- **C-deactivate-payment-link-cors** — **`deactivate-payment-link` returns CORS-less 5xx; browser hides body**
  - `supabase/functions/deactivate-payment-link/index.ts:99`
  - Only `Content-Type` in the catch response headers. Pattern-inconsistent. Carry-over.
  - Fix: spread `...corsHeaders` and replace `error.message` with generic text.

- **C-send-invoice-attachment-timeout** — **`send-invoice` attachment fetch has no timeout / AbortController** (NEW)
  - `supabase/functions/send-invoice/index.ts:107`
  - Evidence: `const res = await fetch(row.file_url); ... const buf = await res.arrayBuffer();`
  - Up to 3 attachment fetches per invoice, none with a timeout. Slow storage responses consume function execution budget. Impact bounded because URLs are pinned to same-host Supabase storage via `isAllowedStorageUrl`, but a storage outage cascades to invoice-send timeouts instead of failing fast.
  - Spec: OWASP-ASVS V11.1.3.
  - Fix: wrap each fetch in an AbortController with ~15s timeout; on timeout push a warning and continue.

- **C-send-invoice-attachment-mime** — **`send-invoice` attachment content-type not validated before mailing** (NEW)
  - `supabase/functions/send-invoice/index.ts:137`
  - Validates URL host/bucket/prefix + size but not Content-Type or file extension. If the upload pipeline is later relaxed, mail becomes a channel for arbitrary attached content (HTML, executables) to customers.
  - Spec: OWASP-ASVS V12.4.1.
  - Fix: whitelist Content-Type (application/pdf, image/png, image/jpeg) or extension on `row.file_name`; skip with warning otherwise.

- **E-dev-transitive** — **Dev-only transitive vulns (brace-expansion, js-yaml, postcss)**
  - `package-lock.json:1185`
  - Evidence: `brace-expansion@1.1.14 (dev), js-yaml@4.1.1 (dev), postcss@8.5.12 (dev)` — all `"dev": true` in lockfile.
  - npm audit flags each as HIGH but all are transitively via eslint/vite/build tooling and never enter the production bundle. brace-expansion DoS requires attacker-controlled glob input; js-yaml requires attacker-controlled YAML; postcss is a source-map/parser issue at build time. None reachable at runtime.
  - Spec: OWASP A06.
  - Fix: `npm audit fix` or `npm update` opportunistically. No production impact.

## Diff vs last audit
_pending_

## Coverage
_pending_
