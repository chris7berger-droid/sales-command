# Sales Command Security Audit — 2026-08-09

## Summary
_pending_

## Top 3 Urgent
_pending_

## Findings by Severity

### Critical

_none_

### High

- **B-PublicInvoicePage-wide-select** — **`PublicInvoicePage` leaks `proposal_wtc.*` (burden_rate, markup_pct, materials cost) to anon viewers** (carry — 4th cycle)
  - `src/pages/PublicInvoicePage.jsx:51`
  - Evidence: `.from("invoice_lines").select("*, proposal_wtc(*, work_types(name))").eq("invoice_id", inv.id);`
  - Anon token-gated select on `invoice_lines` joins `proposal_wtc(*)` — the response ships `regular_hours, ot_hours, burden_rate, ot_burden_rate, markup_pct, materials (jsonb cost basis with unit costs + freight), travel, discount, sales_sow, field_sow, sub_areas` — internal cost + margin — to every invoice recipient's browser. Sibling `PublicSigningPage` was hardened via SECURITY DEFINER RPC in H6/2026-05-02; `PublicInvoicePage` was never migrated. Line 225 calls `calcWtcPrice(wtc, …)` client-side, confirming the fields cross the wire.
  - Spec: `CLAUDE.md`, `CLAUDE_RLS.md`, OWASP-ASVS V5.1.3, OWASP A01.
  - Fix: mirror H6 — add `get_public_invoice_view(p_token uuid)` SECURITY DEFINER RPC returning invoice header + per-line `{wtc_name, wtc_index, line_amount, billing_pct, locked_line_total}`; delete the direct `invoice_lines` select and the client-side `calcWtcPrice`.

- **B-archive-grant-anon** — **`archive` schema `GRANT ALL … TO anon` with ALTER DEFAULT PRIVILEGES** (carry)
  - `sql/history_locker_phase1.sql:118-122`
  - Evidence: `GRANT USAGE ON SCHEMA archive TO anon, authenticated; GRANT ALL ON ALL TABLES IN SCHEMA archive TO anon, authenticated; ALTER DEFAULT PRIVILEGES IN SCHEMA archive GRANT ALL ON TABLES TO anon, authenticated;`
  - Anon has full DML on `archive.import_batches` + `archive.legacy_records`; ALTER DEFAULT PRIVILEGES auto-grants every future archive table to anon. Two defense-in-depth failures: RLS policies at :86-109 don't specify `TO authenticated` (default PUBLIC), and cover only SELECT/INSERT/DELETE while the grant includes UPDATE — any future permissive UPDATE policy opens a write path for anon.
  - Spec: `CLAUDE_RLS.md`, OWASP-ASVS V8.1.5, OWASP A05.
  - Fix: `REVOKE ALL ON ALL TABLES IN SCHEMA archive FROM anon; REVOKE USAGE ON SCHEMA archive FROM anon;` grant to `authenticated` only; scope every RLS policy with `TO authenticated`; `ALTER DEFAULT PRIVILEGES … TO authenticated` only.

- **B11 (legacy-seed)** — **`sql/rls_identity_tables.sql:64` still ships the pre-lockdown `team_members_update` policy** (carry)
  - `sql/rls_identity_tables.sql:64`
  - Evidence: `CREATE POLICY "team_members_update" ON public.team_members FOR UPDATE TO authenticated USING (tenant_id = public.get_user_tenant_id()) WITH CHECK (tenant_id = public.get_user_tenant_id());`
  - Prior CRITICAL. Prod claim of fix: `command-suite-db` migration `20260722120000_team_members_role_lockdown.sql` deployed 2026-07-22 (§8 smoke-verified). Cannot re-verify prod from this repo. The legacy seed here still contains the anti-pattern — a fresh deploy from these seeds re-introduces full privilege-escalation (any user rewrites another's `auth_id`/`role`/`active`). Not marked deprecated. Fourth cycle unfixed at the seed.
  - Spec: `CLAUDE_RLS.md` role-gating, OWASP-ASVS V8.2, OWASP A01.
  - Fix: update the seed to the lockdown pattern (subquery on `team_members.role = 'Admin'`), or delete the file with a comment pointing at `command-suite-db` as source of truth.

- **A4** — **`invite-user` logs full magic-link `action_link` via `linkData`** (carry — 4th cycle)
  - `supabase/functions/invite-user/index.ts:128`
  - Evidence: `console.log("generateLink result:", { linkData, linkErr: linkErr?.message });`
  - `linkData.properties.action_link` is a single-use magic-link URL that authenticates the invitee (redirectTo=`https://salescommand.app`). Written to Supabase function logs on every invite; anyone with log-read access (or a downstream log-sink) can hijack the account before the invitee redeems.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09.
  - Fix: `console.log("generateLink result:", { hasLink: !!linkData?.properties?.action_link, linkErr: linkErr?.message });` — never log `linkData`.

- **C-reset-password** — **`reset-password` references undeclared `origin` — branding always defaults to Sales Command; ReferenceError risk on runtime tightening** (carry — 4th cycle)
  - `supabase/functions/reset-password/index.ts:66`
  - Evidence: `const isSchedule = origin.includes("schmybiz") || origin.includes("schedulecommand");`
  - `origin` is never declared, imported, or read from `req.headers`. In Deno Edge `globalThis.origin` resolves to `""` unless `--location` is set — every reset link is branded Sales Command / `salescommand.app`. Schedule Command users always get the wrong branding. If Deno tightens resolution to throw ReferenceError, the outer catch (:145) leaks the raw error.
  - Spec: OWASP A05, A09, OWASP-ASVS V5.1.5.
  - Fix: `const origin = req.headers.get("origin") || "";` at handler top; collapse `error.message` returns to `"Internal error"` + correlation ID.

- **C-qb-auth** — **`qb-auth` has `verify_jwt = false` at the platform layer** (carry)
  - `supabase/config.toml:27`
  - Evidence: `[functions.qb-auth]` … `verify_jwt = false`
  - Platform JWT gate off; handler applies `authenticateCaller`/`requireAdminOrManager` at the app layer, but every other admin function relies on the platform gate as defense in depth. A future handler-auth refactor becomes a public service-role OAuth endpoint that writes `qb_connection` across tenants.
  - Spec: Supabase production checklist, OWASP-ASVS V4.1.5.
  - Fix: set `verify_jwt = true`; keep in-handler role gate as additional defense.

- **C16** — **`qb-record-payment` service-role branch skips tenant check** (carry)
  - `supabase/functions/qb-record-payment/index.ts:103`
  - Evidence: `if (!caller.isServiceRole && invoice.tenant_id !== caller.tenantId) { return unauthorizedResponse(403, corsHeaders); }`
  - For user callers the gate is correct; for the service-role path (stripe-webhook) there's no cross-validation that the Stripe event was about this invoice. Today's chain is safe because stripe-webhook verifies signature, but any future internal call or compromise of any service-role function lets an attacker post a phantom QB Payment against any tenant's invoice.
  - Spec: OWASP-ASVS V13.2.1, CWE-345.
  - Fix: stripe-webhook should pass `originating_tenant_id`; qb-record-payment must assert it matches `invoice.tenant_id`. Also skip when `invoice.qb_payment_id` is already set (pairs with C-stripe-webhook-reclaim).

- **F-PayAppDetailModal-cascade** — **`handleSaveLines` updates `invoices.amount` but never propagates to `invoice_lines.amount`** (carry — 4th cycle)
  - `src/components/PayAppDetailModal.jsx:249-253`
  - Evidence: `if (payApp?.invoice_id) { await supabase.from("invoices").update({ amount: Math.round(gross * 100) / 100, retention_amount: ret, }).eq("id", payApp.invoice_id); }`
  - Rewrites `billing_schedule_pay_app_lines` + `billing_schedule_pay_apps` header + `invoices.amount/retention_amount` but does NOT update `invoice_lines.amount` for the same invoice. `Invoices.jsx:1805` intentionally PRESERVES `invoice_lines.amount` for pay-app SOV lines — that preserve is safe only if the pay-app write path had already updated `invoice_lines`. After any pay-app line edit, `invoices.amount` reflects the new gross but `invoice_lines.amount` holds pre-edit numbers → the customer's public page + PDF show stale per-line dollars while the header total is fresh. Canonical Data Integrity Rule 6 trap. Line moved 233→249.
  - Spec: `CLAUDE.md` Data Integrity Rule 6.
  - Fix: after the `invoices` update, look up matching `invoice_lines` by `(billing_schedule_line_id, invoice_id)` and UPDATE each row's `amount` and `billing_pct` to match.

- **D-PayAppDetailModal-savelines-syncedLock** — **`handleSaveLines` writes QB-synced invoice.amount with no `syncedLock` guard** (NEW)
  - `src/components/PayAppDetailModal.jsx:249-253`
  - Evidence: `if (payApp?.invoice_id) { await supabase.from("invoices").update({ amount: Math.round(gross * 100) / 100, retention_amount: ret, }).eq("id", payApp.invoice_id); }`
  - `Invoices.jsx handleSaveEdit` has the `syncedLock` branch that refuses to mutate `invoices.amount` when `qb_invoice_id` is set (protecting QB parity). The pay-app path here has no such check — a rep who edits a pay app's per-line %'s silently shifts the QB-synced invoice's amount and retention_amount, breaking QB parity. Pairs with F-PayAppDetailModal-cascade for a compound Rule 6/7 violation.
  - Spec: `CLAUDE.md` Data Integrity Rule 6 + 7, OWASP A04.
  - Fix: at the top of `handleSaveLines`, if `invoice?.qb_invoice_id` is truthy, refuse the write and prompt the operator to void the invoice first (mirror `handleSaveEdit :1794`).

- **D-WTCCalculator** — **`WTCCalculator.handleSave` + `handleLock` are money-writes with NO role gate and NO `teamMember` prop** (carry)
  - `src/pages/WTCCalculator.jsx:2110` (handleSave), `:2169` (handleLock)
  - Evidence: both handlers recompute and write `proposal_wtc` (labor, materials, markup, discount) and `proposals.total`. The component takes no `teamMember` prop and has no role check anywhere. DMS-1 Phase 2 also routes materials-catalog writes through the same handler. Any authenticated user who can open a WTC can rewrite money on any proposal in the tenant.
  - Spec: `CLAUDE_RLS.md`, `CLAUDE.md` Data Integrity Rule 4, OWASP A01.
  - Fix: pass `teamMember` into WTCCalculator; gate `handleSave`/`handleLock` on `["Admin","Manager"].includes(role) OR sales_name match`; backstop with RLS on `proposal_wtc` UPDATE/INSERT and `proposals.total` UPDATE.

- **D-MobilizationsEditor** — **`MobilizationsEditor.persist` writes `proposals.mobilizations` with NO role gate; propagates cross-driver to Schedule Command** (carry)
  - `src/components/ProposalDetail.jsx:1600` (persist), plus `addMob`, `commit onBlur`, `deleteMob` in the same block
  - Component takes no `teamMember` prop and does no role check. `proposals.mobilizations` flows into Schedule Command via `commitSendToSchedule` — writing here creates a cross-driver contamination vector. Line moved 1614→1600.
  - Spec: `CLAUDE_RLS.md`, `docs/plans/command_suite_shared_data_contract.md`.
  - Fix: pass `teamMember`; gate `persist` on `canDelete`-style predicate. RLS on `proposals.mobilizations` UPDATE.

- **D-ProposalDetail-batch** — **`deleteWtc`, `toggleWtcLock`, `commitSendToSchedule`, `handleInternalApprove`, `handlePullBack` — destructive + cross-driver + money-writes, all client-gated only** (carry)
  - `src/components/ProposalDetail.jsx:302` (deleteWtc), `:310` (toggleWtcLock — writes `proposals.total`), `:629` (commitSendToSchedule — inserts `jobs` + `job_wtcs`), `:788` (handleInternalApprove — flips status Sold + call_log.stage Sold + fires QB job-create), `:556` (handlePullBack — hard-deletes `proposal_signatures`, resets status, mints new signing_token)
  - None of these handlers check `teamMember.role` before writing. `toggleWtcLock` violates Rule 4; `commitSendToSchedule` is a cross-driver write into Schedule Command; `handlePullBack` destroys legal evidence (pairs with B13). `handleInternalApprove` moved 806→788.
  - Spec: `CLAUDE_RLS.md`, `CLAUDE.md` Data Integrity Rule 4, `docs/plans/command_suite_shared_data_contract.md`, OWASP A01, A08.
  - Fix: enforce role/ownership at each handler entry; backstop with RLS on `proposal_wtc` (INSERT/UPDATE/DELETE), `proposals` (UPDATE), `jobs` (INSERT), `job_wtcs` (INSERT), `proposal_signatures` (DELETE).

- **D-ArchiveProposalPanel** — **`ArchiveProposalPanel.handleSave` rewrites `proposals.total` and `historical_billed_amount` with NO role gate** (carry)
  - `src/components/ProposalDetail.jsx:1712`
  - Evidence: `async function handleSave() { … const { error } = await supabase.from("proposals").update({ total: n, historical_billed_amount: h }).eq("id", p.id); … }`
  - Archive-proposal money path — user types a Sold Amount and writes it directly to `proposals.total`. Linked-invoice `handleSaveEdit` in Invoices.jsx depends on `proposals.total` for its recompute path — durable upstream money-motion primitive. Line moved 1726→1712.
  - Spec: `CLAUDE.md` Data Integrity Rule 6, `CLAUDE_RLS.md`.
  - Fix: gate the Edit button + `handleSave` on canManage; backstop with RLS on `proposals.total` UPDATE.

- **D-ArchiveProposalModal-handleCreate** — **`ArchiveProposalModal.handleCreate` mints Sold proposals with `approved_at` set and NO role gate** (NEW)
  - `src/components/ArchiveProposalModal.jsx:79-109`
  - Evidence (:97-109): `.from("proposals").insert([{ … status: "Sold", total: amount, proposal_number: proposalNumber, signing_token: crypto.randomUUID(), is_archive_proposal: true, historical_billed_amount: histBilled, intro: description || null, approved_at: new Date().toISOString(), }])`
  - Any authenticated user who reaches the ArchiveProposalModal (via the History Locker Import flow or its equivalent) can create a proposal in status Sold with `approved_at` set to now and any sold amount. Combined with `D-ArchiveProposalPanel.handleSave` (which lets any user edit that Sold Amount afterward) and the archive-invoice recompute path, this is a fully client-authored path from "job exists" to "invoiceable Sold proposal with historical billed amount" with zero role gate. No `teamMember` prop on the component.
  - Spec: `CLAUDE.md` Data Integrity Rule 6, `CLAUDE_RLS.md`, OWASP A01.
  - Fix: pass `teamMember` and require `role IN ('Admin','Manager')` at the top of `handleCreate`; enforce INSERT policy on `proposals` that blocks direct-Sold creation for non-privileged roles.

- **D-ImportToLiveWizard-replace-delete** — **Archive-import "replace" mode hard-deletes `job_work_types` + `call_log` with NO role gate** (NEW)
  - `src/components/HistoryLocker/ImportToLiveWizard.jsx:393-396`
  - Evidence: `const { error: wtErr } = await supabase.from("job_work_types").delete().eq("call_log_id", legacyTakenBy.id); … const { error: clErr } = await supabase.from("call_log").delete().eq("id", legacyTakenBy.id);`
  - The replace flow's preflight children-check at :386-391 is preflight-only (races with concurrent creates), and neither the wizard nor the surrounding page verifies role. Any authenticated tenant user who opens the History Locker import wizard can wipe a live call_log row for any job whose display_job_number they can guess (as long as it currently has no children). Combined with D5 (CallLogDetail.handleDelete client-gated only) and B11 legacy seed, `call_log` DELETE remains ungated at every entry point.
  - Spec: `CLAUDE_RLS.md`, OWASP-ASVS V4.1.5, OWASP A01.
  - Fix: gate replace path on `["Admin","Manager"].includes(role)`; make the preflight check inside a serializable transaction or replace with an RPC that atomically checks + deletes; RLS DELETE on `call_log`/`job_work_types` restricted to Admin/Manager.

- **D3** — **`Settings.handleSave` writes `tenant_config` with no role gate** (carry)
  - `src/pages/Settings.jsx:645` (handler), Save button at `:678`
  - `canManage` only wraps the WorkTypesSection (:758) + MaterialsCatalogSection (:764); the top-level form + Save button ships to every role. A Sales Rep can overwrite tenant defaults — rates, goals, license number, apps[], logo URL — flowing through every WTC calc and every customer-facing page. Pairs with B10. Line moved 654→645.
  - Spec: `CLAUDE_RLS.md`, OWASP-ASVS V4.1.3, OWASP A01.
  - Fix: wrap the whole form (and Save) in `{canManage && …}` AND enforce `is_admin_or_manager()` at the RLS level.

- **D4** — **`ProposalDetail.handleDelete` is client-gated only** (carry)
  - `src/components/ProposalDetail.jsx:533`
  - `canDelete` exposed as a button-hiding hint; the handler itself doesn't re-verify. No server backstop for the soft-delete.
  - Spec: `CLAUDE_RLS.md`, OWASP A01.
  - Fix: enforce in RLS on `proposals` UPDATE: `(is_admin_or_manager() OR sales_name = current_user_name)`.

- **D5** — **`CallLogDetail.handleDelete` is client-gated only** (carry, line moved 235→280)
  - `src/components/CallLogDetail.jsx:280`
  - Evidence: `const canDelete = teamMember && (teamMember.role === "Admin" || teamMember.name === job.sales_name); async function handleDelete() { … }`
  - `call_log` + `job_work_types` have authenticated DELETE policies (CLAUDE.md); the handler is only guarded by the button being hidden. Any tenant member can delete any job via direct supabase call, and now via ImportToLiveWizard replace mode (see D-ImportToLiveWizard-replace-delete).
  - Spec: `CLAUDE_RLS.md`, OWASP A01.
  - Fix: restrict `call_log` DELETE to `(is_admin_or_manager() OR sales_name = current_user_name)`.

- **D6** — **`Home.jsx` sales filter is client-only** (carry)
  - `src/pages/Home.jsx:105`
  - Evidence: `const isRep = !["Admin","Manager"].includes(displayRole); … filters: isRep ? [["eq", "sales_name", displayName]] : []`
  - `displayRole` is a React prop; a Sales Rep who edits it in DevTools flips `isRep=false` and pulls every rep's pipeline. Same shape on `:119` for proposals.
  - Spec: `CLAUDE_RLS.md`, OWASP-ASVS V4.1.3, OWASP A01.
  - Fix: RLS SELECT on `call_log`/`proposals` limiting non-admin/manager reps to `sales_name = current user's name`.

- **D7** — **`SalesDash` trusts client-supplied `selectedRep`** (carry)
  - `src/pages/SalesDash.jsx:107`
  - Picker UI is hidden for non-admins (:217) but the load query trusts `state.selectedRep`. Non-admin sets it in DevTools and pulls another rep's data. Also: `team_members.select` at `:92` returns every rep's name to every role.
  - Spec: `CLAUDE_RLS.md`, OWASP-ASVS V4.1.3.
  - Fix: RLS SELECT as D6; gate the `team_members` fetch so non-admins see only their own row.

- **D8** — **`CallLog` cache writes customer PII into sessionStorage** (carry)
  - `src/pages/CallLog.jsx:114`
  - Evidence: `sessionStorage.setItem(CACHE_KEY, JSON.stringify({ rows: allLog, team: tm || [], customers: allCx, workTypes: wt || [] }));`
  - `allCx = fetchAll("customers", "*")` — full customer directory (contact_email, contact_phone, billing_email, billing_phone, business_address) plus every call_log row written to sessionStorage. Any XSS sink exfiltrates the entire customer book in one JSON read.
  - Spec: OWASP-ASVS V8.3.1, OWASP A02, A04, GDPR Art. 32.
  - Fix: cache only `{id, display_job_number, stage, customer_name, sales_name, bid_due, follow_up}`; refetch full detail on click.

- **D10** — **`Invoices` destructive/money handlers rendered without a role gate on any of them** (carry)
  - `src/pages/Invoices.jsx:1727` (handleDelete), `:1767` (handleSaveEdit), `:1909` (handleBillRetention), `:1983` (handlePullBack), `:2016` (handleToggleDeposit), `:2048` (handleVoidConfirm)
  - Buttons render with no `canManage`/`canDelete` guard on any of them. Every destructive/money-write handler on InvoiceDetail is reachable by every Sales Rep. Lines drifted from prior audit due to header + retention-release edits.
  - Spec: `CLAUDE.md` Data Integrity Rule 6, OWASP-ASVS V4.1.3.
  - Fix: define `const canManage = teamMember && ["Admin","Manager"].includes(teamMember.role);`; gate every InvoiceDetail button + handler entry; add matching RLS UPDATE/DELETE policies on `invoices`.

- **D-PayAppDetailModal-delete** — **`handleDeletePayApp` no role gate** (carry)
  - `src/components/PayAppDetailModal.jsx:264`
  - Soft-deletes linked invoice, hard-deletes pay-app row. Only guard is `invoice?.qb_invoice_id` short-circuit. No `teamMember` prop, no role check.
  - Spec: `CLAUDE.md` Data Integrity Rule 6, OWASP-ASVS V4.1.3.
  - Fix: add role gate before writes; RLS on `billing_schedule_pay_apps` DELETE + `invoices` UPDATE(deleted_at) restricted to admin/manager.

### Medium

- **B10 (legacy-seed)** — **`sql/rls_identity_tables.sql:94` `tenant_config_update` still lacks admin gate** (carry)
  - Any tenant member can UPDATE tenant_config — company_name, logo_url, license_number, billing goals, stripe_customer_id/stripe_subscription_id, apps[], default_burden_rate. Prod state unverifiable; legacy seed permissive. Pairs with D3.
  - Spec: `CLAUDE_RLS.md`, OWASP A01, A05.
  - Fix: same lockdown pattern as B11.

- **B13 (legacy-seed)** — **`sql/rls_child_tables.sql:261` `proposal_signatures_delete` allows any tenant user to erase signature evidence** (carry)
  - Signatures are legal evidence (signer_name, signed_at, ip_address, pdf_url). Any tenant member can DELETE, wiping the forensic record while leaving the proposal Sold.
  - Spec: OWASP-ASVS V7.1, OWASP A08.
  - Fix: drop DELETE (signatures append-only) or gate on `role='Admin'`; if pull-back is needed, add `voided_at` instead.

- **B-archive-tenant-helper** — **`archive.get_user_tenant_id()` doesn't filter `active = true` — deactivated employee retains archive access** (carry)
  - `sql/history_locker_phase1.sql:76-82` (helper body); same shape at `sql/rls_cleanup_and_remaining.sql:389` (redefine).
  - Divergent from `public.get_user_tenant_id()` at `sql/rls_identity_tables.sql:129-137` which restricts to `active = true`. Deactivated employee with a stale refresh token retains read/insert/delete on `archive.import_batches` + `archive.legacy_records`. Offboarding hole.
  - Spec: OWASP-ASVS V7.1, OWASP A07.
  - Fix: add `AND active = true` in both places, matching the public helper.

- **B-proposals-invoices-select-deleted-at** — **`proposals_select` + `invoices_select` don't filter `deleted_at IS NULL`** (NEW)
  - `sql/rls_core_tables.sql:138` (proposals), `:185` (invoices)
  - Evidence (`:138-140`): `CREATE POLICY "proposals_select" … USING (tenant_id = public.get_user_tenant_id());` — no `deleted_at` predicate.
  - CLAUDE.md rules assume `deleted_at NULL` = active; the DB does not enforce that. Any authenticated tenant user querying PostgREST directly (or a UI regression) resurrects soft-deleted proposals + invoices. Defense-in-depth gap.
  - Spec: OWASP-ASVS V8.3.4, OWASP A01.
  - Fix: add `AND deleted_at IS NULL` to both SELECT policies; add separate admin-only `_deleted_select` policies for restore workflows.

- **F1** — **`ProposalDetail` Summary total uses stale `parseFloat(p.total)` fallback** (carry, 4th cycle; line moved 1271→1256)
  - `src/components/ProposalDetail.jsx:1256-1258`
  - Evidence: `const sov = sovContractSum != null && sovContractSum > 0 ? sovContractSum : null; const fallback = parseFloat(p.total) || 0; const value = sov != null ? sov : fallback;`
  - Data Integrity Rule 2 forbids reading `proposals.total` — must compute via `calcWtcPrice()` on live WTC data. `proposals.total` is only written on `handleLock`/`handleSave`. An unlocked proposal edited after last lock shows a stale figure directly beside the live WTC breakdown a few lines below (:1268-1272).
  - Spec: `CLAUDE.md` Data Integrity Rule 2.
  - Fix: compute the fallback with `calcProposalTotal(wtcs, undefined, usesExactPricing(p))` (already imported); use `p.total` only when `wtcs` is empty.

- **A1** — **Hardcoded Supabase project URL fallback in `supabasePublic.js`** (carry)
  - `src/lib/supabasePublic.js:16`
  - Evidence: `const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL || "https://pbgvgjjuhnpsumnowuym.supabase.co";`
  - A misbuilt non-prod deploy silently routes to prod; project ref baked into every browser bundle.
  - Spec: OWASP-ASVS V14.3.2, 12-factor III.
  - Fix: drop the `||` fallback; throw at module load like `src/lib/supabase.js:8`.

- **A2** — **Hardcoded Supabase publishable-key fallback in `supabasePublic.js`** (carry)
  - `src/lib/supabasePublic.js:20`
  - Evidence: `const SUPABASE_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY || "sb_publishable_LL6wYC7kJDPQfGYeKAaLaWKoK";`
  - After anon-key rotation, stale bundles keep using the OLD key. Not a bearer secret, but defeats env-var rotation.
  - Spec: OWASP-ASVS V14.3.2, OWASP A05.
  - Fix: remove the literal; require the env var (throw on missing).

- **A1b** — **Hardcoded URL fallback on Login forgot-password path** (carry)
  - `src/pages/Login.jsx:59`
  - Same rotation hazard as A1.
  - Spec: OWASP-ASVS V14.3.2.
  - Fix: import from `src/lib/supabase.js`.

- **A2b** — **Hardcoded publishable-key fallback on Login (DIFFERENT literal than A2 — key drift)** (carry)
  - `src/pages/Login.jsx:62`
  - Evidence: `apikey: import.meta.env.VITE_SUPABASE_ANON_KEY || "sb_publishable_v7XktVvkAlX7y5f6xoFjng_AaLaWKoK"`
  - Literal differs from `supabasePublic.js:20`. Exactly the rotation drift called out for months.
  - Spec: OWASP-ASVS V14.3.2, OWASP A05.
  - Fix: remove both literals; require the env var everywhere.

- **C-invite-user-sender** — **`invite-user` accepts caller-supplied `senderEmail` without verified-domain gate** (carry)
  - `supabase/functions/invite-user/index.ts:153`
  - `send-invoice`/`send-pay-app`/`send-proposal` gate the `From:` to VERIFIED_DOMAINS; `invite-user` doesn't. Resend blocks unverified domains, so the exploitable case is picking any Resend-verified tenant domain to spoof a colleague's invite.
  - Spec: OWASP A04, CWE-345.
  - Fix: apply the VERIFIED_DOMAINS gate.

- **C-invite-user-listusers** — **`invite-user` uses default-paginated `listUsers()` and cross-tenant enumerates** (carry)
  - `supabase/functions/invite-user/index.ts:102`
  - Default page 50; beyond page 1 existing users appear absent → invite links to `authId = undefined`. Also `listUsers` returns global `auth.users` → an "already registered" match from another tenant silently attaches that user's auth_id to this tenant's team_members row at :114-119, enabling cross-tenant login.
  - Spec: OWASP-ASVS V8.3.4, CWE-1284.
  - Fix: `supabase.auth.admin.getUserByEmail(email)` or direct service-role SELECT; block linking if the found user belongs to another tenant.

- **C-reset-password-listusers** — **`reset-password` uses the same unpaginated `listUsers()` pattern** (carry)
  - `supabase/functions/reset-password/index.ts:55`
  - Silent hard-block on password reset at scale.
  - Spec: CWE-1284.
  - Fix: same as invite-user.

- **C-deactivate-user-count** — **`deactivate-user` job-count query not tenant-scoped** (carry)
  - `supabase/functions/deactivate-user/index.ts:119`
  - Evidence: `.from("call_log").select("id", { count: "exact", head: true }).eq("sales_name", member.name);`
  - Service-role client with no tenant filter. Same-named reps in different tenants leak each other's row counts.
  - Spec: OWASP A01.
  - Fix: `.eq("tenant_id", callerTenantId)`.

- **C-qb-record-payment-net** — **`qb-record-payment` `netAmount` ignores `retention_amount`** (carry)
  - `supabase/functions/qb-record-payment/index.ts:153`
  - `send-invoice:161` charges Stripe `amount - discount - retentionAmount`; qb-record-payment writes gross-minus-discount. QB Payment mis-applied on any invoice with retention.
  - Spec: OWASP A04, CWE-682.
  - Fix: `netAmount = amount - discount - (invoice.retention_amount || 0);`.

- **C-create-billing-session** — **`create-billing-session` lacks admin/manager gate** (carry)
  - `supabase/functions/create-billing-session/index.ts:57`
  - Any tenant member can open the Stripe billing portal or initiate a checkout that writes `tenant_config.stripe_customer_id`.
  - Spec: OWASP-ASVS V4.2.1.
  - Fix: `requireAdminOrManager`.

- **C-qb-search-customers** — **`qb-search-customers` has no role gate** (carry)
  - `supabase/functions/qb-search-customers/index.ts:86`
  - Spec: OWASP-ASVS V4.2.1.
  - Fix: `requireAdminOrManager`.

- **C-qb-void-invoice** — **`qb-void-invoice` does not require admin/manager** (carry)
  - `supabase/functions/qb-void-invoice/index.ts:63`
  - Spec: OWASP-ASVS V4.2.1, CWE-863.
  - Fix: `requireAdminOrManager`; explicitly reject `caller.isServiceRole`.

- **C-extract-sov** — **`extract-sov` has no rate limit / cost gate on paid Anthropic API** (carry)
  - `supabase/functions/extract-sov/index.ts:159`
  - `claude-opus-4-7` + `max_tokens: 16000`. Any tenant user can loop unbounded.
  - Spec: OWASP-ASVS V11.1.4, CWE-770.
  - Fix: per-user + per-tenant token-bucket (~10/hr), daily cap, cache by SHA-256 of PDF bytes.

- **C-extract-sov-ssrf** — **`extract-sov` fetches `pdf_url` with no host allowlist — own-tenant SSRF** (NEW)
  - `supabase/functions/extract-sov/index.ts:141`
  - Evidence: `const pdfRes = await fetch(pdf_url);`
  - Ownership check at :128-138 confirms `pdf_url` matches a `billing_schedule.contract_pdf_url`/`contract_pdf_urls` value in the caller's schedules — but that value can be any string a tenant wrote. If a tenant user writes `http://169.254.169.254/latest/meta-data/` or a private-network URL to `contract_pdf_url`, `extract-sov` fetches it via the service-role edge runtime and streams the response into Anthropic's context. Response contents can be inferred from the SOV output — internal network + cloud-metadata read via own-tenant URL injection.
  - Spec: OWASP A10 (SSRF), OWASP-ASVS V13.4.1.
  - Fix: reuse `_shared/attachments.ts` `isAllowedStorageUrl()` — require `pdf_url` host === Supabase project host AND path under `job-attachments/`. Reject others with 400.

- **C-proposal-signed-rate** — **`proposal-signed` has no rate limit + response-code oracle** (carry)
  - `supabase/functions/proposal-signed/index.ts:31`
  - Public endpoint; INVALID_TOKEN (403) vs ALREADY_SIGNED (409) vs INVALID_SIGNER_NAME (400) form a token-state oracle.
  - Spec: OWASP-ASVS V11.1.4, CWE-307.
  - Fix: token-bucket by client IP (5/15min); collapse token errors into a single generic 403 externally.

- **C9** — **Raw `error.message` returned across 23 catch handlers (now includes new `proposal-approved`)** (carry, extended)
  - Pattern across `check-orphan-users:33,61`, `create-billing-session:211`, `deactivate-payment-link:99`, `deactivate-user:131`, `delete-user:120`, `invite-user:191`, `qb-auth:196`, `qb-create-job:317`, `qb-link-customer:149`, `qb-record-payment:193`, `qb-search-customers:123`, `qb-sync-invoice:456`, `qb-void-invoice:195`, `reset-password:145`, `scc-stripe-webhook:188`, `send-invoice:551`, `send-pay-app:508`, `send-proposal:225`, `stripe-webhook:143,270`, `follow-up-reminders:90,197`, `proposal-signed:108,144`, `extract-sov:194`, **NEW: `proposal-approved:87`**.
  - Leaks Postgres error text, Supabase auth internals, QB Fault details, JSON stack fragments.
  - Spec: OWASP-ASVS V7.4.1, OWASP A09, CWE-209.
  - Fix: shared `safeError(e)` helper.

- **C10** — **`send-invoice` returns raw Stripe + Resend response bodies verbatim** (carry)
  - `supabase/functions/send-invoice/index.ts:349` (Stripe), `:467` (Resend main), `:491` (Resend viewer warnings)
  - `send-pay-app:42` uses `sanitizeProviderError()`; `send-invoice` was not updated.
  - Spec: OWASP-ASVS V7.4.1, OWASP A09.
  - Fix: promote `sanitizeProviderError` to `_shared/`; redact tokens; cap at 500 chars.

- **C-send-invoice-sender** — **`send-invoice` `senderEmail` only checked for domain, not identity** (carry)
  - `supabase/functions/send-invoice/index.ts:278`
  - VERIFIED_DOMAINS blocks external spoofing, but any authenticated tenant member can send `From:` any co-worker's address. `send-pay-app:218-223` resolves sender server-side; `send-invoice` was not migrated.
  - Spec: OWASP A04.
  - Fix: resolve `senderEmail` from `team_members.email WHERE auth_id = caller.userId`.

- **C-repNotify-hardcoded-brand** — **`_shared/repNotify.ts` hard-codes HDSP `From:` address + branding for all tenants** (NEW)
  - `supabase/functions/_shared/repNotify.ts:19` (FROM), `:79-80` (h2/tagline)
  - Evidence: `const FROM_ADDRESS = "Sales Command <estimates@hdspnv.com>";` and `<h2 …>High Desert Surface Prep</h2><p …>Industrial & Commercial Concrete Coatings</p>`
  - Both new `proposal-approved` and `follow-up-reminders` paths route through this shared helper. Every rep-notification email — for ANY tenant onboarded — is sent From `estimates@hdspnv.com` with HDSP branding. Multi-tenant Command Suite ships to a second tenant → their reps receive emails claiming to be from HDSP. Data-integrity + brand-integrity issue; not currently an unauth exploit but breaks tenant boundary the moment a second tenant onboards.
  - Spec: `CLAUDE.md` Command Suite Shared-Data Contract; OWASP A04.
  - Fix: resolve FROM address + company name + tagline from `tenant_config` (as `send-proposal:143-147` already does); fall back to `noreply@salescommand.app` when the tenant's From-domain is not Resend-verified.

- **D-handleToggleDeposit** — **`handleToggleDeposit` can silently zero retention with no role gate** (carry)
  - `src/pages/Invoices.jsx:2016`
  - Evidence: `const zeroRetention = turningOn && !linkedPayApp && !syncedLock && …; const updates = zeroRetention ? { is_deposit: turningOn, retention_pct: 0, retention_amount: 0 } : { is_deposit: turningOn };`
  - Toggle the deposit checkbox → retention silently zeros. No role check. Combined with the missing gates on `handleSaveEdit`/`handleBillRetention`, most-exposed money surface on the deposit-per-invoice flow. Line moved 2003→2016.
  - Spec: `CLAUDE.md` Data Integrity Rule 6, Rule 7.
  - Fix: role gate at handler head; disable the Checkbox for non-admins; enforce in RLS.

- **D-NewPayAppModal-create** — **`NewPayAppModal.handleCreate` mints invoices with no role gate** (carry)
  - `src/components/NewPayAppModal.jsx:125`
  - Inserts `billing_schedule_pay_apps`, `billing_schedule_pay_app_lines`, and `invoices` (with client-scanned `nextId`). No `teamMember`, no role symbol. Line moved 123→125.
  - Spec: `CLAUDE.md` Data Integrity Rule 6, OWASP-ASVS V4.1.3.
  - Fix: gate handler + Create button on canManage; RLS INSERT on `billing_schedule_pay_apps` + `invoices` restricted to admin/manager.

- **D-PayAppDetailModal-savelines** — **`handleSaveLines` rewrites pay-app + invoice money with no role gate** (carry)
  - `src/components/PayAppDetailModal.jsx:233`
  - Rewrites `billed_pct_this_app`, `billed_amount_this_app`, `invoices.amount`, `invoices.retention_amount`. No role gate. (Missing syncedLock guard tracked separately as D-PayAppDetailModal-savelines-syncedLock, HIGH.)
  - Spec: `CLAUDE.md` Data Integrity Rule 6, OWASP-ASVS V4.1.3.
  - Fix: role gate at handler head; RLS restricting UPDATE on these columns.

- **D-MultiGCWizard-server-verify** — **MultiGC clone RPC has no client-side role gate — server contract needs verification in `command-suite-db`** (carry)
  - `src/components/MultiGCWizard.jsx:184`
  - Evidence: `const { data, error: rpcErr } = await supabase.rpc("clone_proposal_to_gcs", { p_source_proposal_id, p_targets });`
  - No client-side role gate; targets and per-target `markup_override_pct` are attacker-controlled. Security depends on the RPC body in `command-suite-db`. Requires: (1) role IN ('Admin','Manager') OR source-proposal `sales_name` match, (2) target `customer_ids` belong to caller's tenant, (3) `markup_override_pct` floor, (4) intro_override sanitization.
  - Spec: `CLAUDE_RLS.md`, OWASP A01.
  - Fix: verify `clone_proposal_to_gcs` in command-suite-db; add client-side `canManage` gate on '+ Send to Additional GCs' to match the server contract.

- **E-react-router** — **`react-router-dom@7.13.1` — 11 advisories; open-redirect via backslash is client-reachable but latent** (carry, re-classified this cycle from previously-mislabeled "unused")
  - `package.json:19`
  - SSR/RSC advisories don't apply (no `react-router-dom/server`/`createStaticHandler`/`renderToString`/`RouterProvider` imports; only BrowserRouter + hooks across 17 files). Client-side backslash open-redirect in `<Link>`/`useNavigate` (GHSA-wrjc-x8rr-h8h6, <7.18.0) IS reachable in principle but every `navigate(...)` site passes hard-coded or id-interpolated internal paths — latent, not currently exploitable.
  - Spec: OWASP A06.
  - Fix: `npm install react-router-dom@^7.18.2` (drop-in minor).

- **E-vite** — **`vite@8.0.10` fs.deny bypass + launch-editor NTLM (dev-only, Windows)** (carry)
  - `package.json:31`
  - GHSA-fx2h-pf6j-xcff + GHSA-v6wh-96g9-6wx3. Reachable only while `vite dev` runs on Windows; prod is static Vercel bundle.
  - Spec: OWASP A06.
  - Fix: `npm install -D vite@^8.0.16`.

- **E-ws** — **`ws` DoS + uninit-memory (dev-only transitive via vite HMR)** (carry)
  - `package-lock.json` (transitive of vite HMR)
  - GHSA-96hv-2xvq-fx4p + GHSA-58qx-3vcg-4xpx. Fixed in 8.21.0+. Only in dev server.
  - Spec: OWASP A06.
  - Fix: `npm audit fix` (bumps transitively via vite).

- **E-postcss** — **`postcss <=8.5.22` sourceMappingURL path traversal (build-only)** (carry)
  - GHSA-r28c-9q8g-f849 / GHSA-fxqj-rqcc-2cmp. All CSS is inline JS strings via `src/lib/tokens.js`; no untrusted CSS enters the build.
  - Spec: OWASP A06.
  - Fix: `npm audit fix`.

- **E-nanoid** — **`nanoid <3.3.17` infinite loop on zero/negative size (build-only)** (NEW)
  - `package-lock.json` (transitive of postcss)
  - GHSA-28wg-ghj8-5hjv / GHSA-2v37-7h3g-55p8. Requires attacker control of `size` arg. Only build-time postcss dep, invoked with hard-coded sizes.
  - Spec: OWASP A06.
  - Fix: bumped transitively when postcss/vite update.

- **E-eslint-transitives** — **`brace-expansion` + `js-yaml` DoS advisories (dev-only via ESLint)** (carry)
  - GHSA-3jxr-9vmj-r5cp / GHSA-mh99-v99m-4gvg / GHSA-rgw5-rvv9-x895 (brace-expansion); GHSA-52cp-r559-cp3m / GHSA-5p4m-2wfm-xmqj (js-yaml). Dev-time only.
  - Spec: OWASP A06.
  - Fix: `npm audit fix`.

### Low

- **A5** — **`stripe-webhook` logs full recipient email for every outbound receipt** (carry)
  - `supabase/functions/stripe-webhook/index.ts:49`
  - Evidence: `console.log("Email to", to, ":", res.status);`
  - Spec: OWASP-ASVS V7.1.1, OWASP A09, GDPR Art. 5(1)(c).
  - Fix: `console.log("Email send status:", res.status);` — drop `to`.

- **A6** — **`invite-user` logs invitee + sender emails and names on every call** (carry)
  - `supabase/functions/invite-user/index.ts:88`
  - Evidence: `console.log("invite-user invoked", { email, name, teamMemberId, senderEmail, senderName });`
  - Spec: OWASP-ASVS V7.1.1, OWASP A09, GDPR Art. 5(1)(c).
  - Fix: log only `{ hasEmail: !!email, teamMemberId }`.

- **A7** — **`send-proposal` logs recipient + rep emails on every send** (carry)
  - `supabase/functions/send-proposal/index.ts:139`
  - Evidence: `console.log("send-proposal invoked", { proposalId, recipientEmail, repEmail, proposalNumber });`
  - Spec: OWASP-ASVS V7.1.1, OWASP A09.
  - Fix: log only `{ proposalId, proposalNumber, hasRecipient: !!recipientEmail }`.

- **A8** — **`send-proposal` logs full Resend response body (echoes recipient on error)** (NEW)
  - `supabase/functions/send-proposal/index.ts:184`
  - Evidence: `console.log("Recipient email response:", customerRes.status, customerResBody);`
  - `customerResBody` is Resend's raw JSON — validation-failure paths echo the offending `to` plus internal error text. Body is also interpolated verbatim into the client 500 at :187, surfacing provider text to the browser.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09.
  - Fix: log status only; on success parse and log `{ id }`; return a generic 500.

- **A9** — **`send-pay-app` logs recipient email on every invocation** (NEW)
  - `supabase/functions/send-pay-app/index.ts:99`
  - Evidence: `console.log("send-pay-app invoked", { payAppId, invoiceId, recipientEmail });`
  - Same shape as A6/A7 in a code path that changed this cycle.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09, GDPR Art. 5(1)(c).
  - Fix: log `{ payAppId, invoiceId }` only.

- **A10** — **`_shared/repNotify.sendEmail` logs recipient email + Resend response body on success** (NEW)
  - `supabase/functions/_shared/repNotify.ts:118`
  - Evidence: `` console.log(`${tag}: emailed ${to} — ${body}`); ``
  - Multiplies the PII log surface across `proposal-approved` and `follow-up-reminders`. Loud-on-failure at :115 is intentional; success path doesn't need `to` in the log.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09.
  - Fix: log a Resend id only on success (parse `body` for `id`); drop `to`.

- **A11** — **`follow-up-reminders` logs sales rep names + job numbers for all unassigned rows** (NEW)
  - `supabase/functions/follow-up-reminders/index.ts:128-131`
  - New nightly cron dumps job numbers paired with rep names on every run when any rep is inactive/missing. Cross-tenant data mixing in one log line.
  - Spec: OWASP-ASVS V7.1.1.
  - Fix: log a count only per tenant; persist detail to a `follow_up_reminder_failures` table if operator needs it.

- **A12** — **`reset-password` logs full Resend response body** (NEW)
  - `supabase/functions/reset-password/index.ts:129`
  - Evidence: `console.log("Reset email response:", emailRes.status, emailResBody);`
  - Body is interpolated verbatim into the returned error at :132.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09.
  - Fix: log status only; parse for `id` on success; return generic 500 externally.

- **B-legacy-search-path** — **`sql/db_hardening_phase1.sql:12` `get_user_tenant_id()` seed body has no `SET search_path` AND uses tenant_config fallback** (carry)
  - Earliest SECURITY DEFINER body returns first tenant regardless of caller. Overwritten by the hardened body in `rls_identity_tables.sql:129-137`, but a fresh deploy that skips the second file is exposed. Same shape on `public.set_updated_at()` at :109.
  - Spec: Supabase function search_path hardening.
  - Fix: delete this section from the seed, or replace with the auth-aware body + `SET search_path = public`.

- **C-stripe-webhook-reclaim** — **`stripe-webhook` re-process branch can double-record QB Payment** (carry)
  - `supabase/functions/stripe-webhook/index.ts:101`
  - If a prior delivery ran `qb-record-payment` but died before `markDone()`, Stripe retries land here, re-process → `qb-record-payment` creates a duplicate QB Payment (no `qb_payment_id` idempotency guard) + duplicate emails.
  - Spec: CWE-362.
  - Fix: in `qb-record-payment` early-return when `invoice.qb_payment_id` is set. Second: mark stripe-webhook rows `in_progress` vs `claimed`.

- **C-check-orphan-error** — **`check-orphan-users` returns raw `error.message`** (carry)
  - `supabase/functions/check-orphan-users/index.ts:33,61`
  - Cron target — low external exposure. Included for parity with C9.
  - Spec: OWASP A09.
  - Fix: return generic error; log server-side.

- **C-deactivate-payment-link-cors** — **`deactivate-payment-link` catch omits CORS headers** (carry)
  - `supabase/functions/deactivate-payment-link/index.ts:99`
  - Spec: CWE-1021.
  - Fix: spread `...corsHeaders` in the catch response.

- **C18** — **`buildCorsHeaders` omits `Access-Control-Allow-Methods`** (carry)
  - `supabase/functions/_shared/cors.ts:49`
  - Preflight responses omit Allow-Methods; strict CORS clients refuse.
  - Spec: CWE-1021.
  - Fix: add `"Access-Control-Allow-Methods": "POST, OPTIONS"`.

- **C-invoice-existence-oracles** — **404-vs-403 oracle across four QB fns lets a caller enumerate cross-tenant invoiceIds** (carry)
  - `supabase/functions/deactivate-payment-link/index.ts:41`; `qb-void-invoice/index.ts:79`; `qb-sync-invoice/index.ts:127`; `qb-record-payment/index.ts:95`
  - Existence check before tenant check — 404 for nonexistent vs 403 for other-tenant.
  - Spec: OWASP A01, CWE-204.
  - Fix: return 404 uniformly across all four fns.

- **C-qb-sync-invoice-svc** — **`qb-sync-invoice` service-role branch skips tenant check** (carry)
  - `supabase/functions/qb-sync-invoice/index.ts:134`
  - Same shape as C16; no current internal caller uses service role.
  - Spec: OWASP A01.
  - Fix: explicitly reject `caller.isServiceRole` unless documented.

- **C-proposal-signed-error** — **`proposal-signed` returns raw `error.message` on top-level catch** (carry)
  - `supabase/functions/proposal-signed/index.ts:144`
  - Public endpoint; any malformed JSON triggers 500 + raw error text.
  - Spec: OWASP A09.
  - Fix: return `{ error: "Sign failed" }`; keep detail in `console.error`.

- **D9** — **Email persisted to localStorage on Login regardless of Remember checkbox** (carry)
  - `src/pages/Login.jsx:38`
  - Evidence: `localStorage.setItem("sc_remember", remember ? "true" : "false"); localStorage.setItem("sc_last_email", email.trim());`
  - `sc_last_email` written unconditionally.
  - Spec: OWASP-ASVS V8.3.1, OWASP A02.
  - Fix: `if (remember) localStorage.setItem(…); else localStorage.removeItem("sc_last_email");`

- **D-NewPayAppModal-id** — **Invoice ID generated from client-side max-id scan (race + soft IDOR)** (carry)
  - `src/components/NewPayAppModal.jsx:170` (also `src/pages/Invoices.jsx:2100` in `handleVoidConfirm`, `:243` `mintNextInvoiceId`)
  - Evidence: `.from("invoices").select("id").order("created_at", { ascending: false }).limit(50); … const nextId = String(lastNum + 1).padStart(5, "0");`
  - Concurrent creates collide on the PK constraint; the SELECT also enumerates 50 recent invoice IDs across the tenant.
  - Spec: OWASP A04.
  - Fix: server-side RPC or Postgres sequence keyed on tenant.

- **D-materials-catalog-wtc** — **`WTCCalculator` inline material picker writes `materials_catalog` with no role gate** (carry)
  - `src/lib/materialsCatalog.js:48` (saveCatalogRow); called from `WTCCalculator.jsx:472,507` inline picker.
  - Settings UI wraps MaterialsCatalogSection in `{canManage && …}` (Settings.jsx:764), but the shared writer is also called from WTCCalculator's inline picker with no gate. Any tenant member can insert/fork/update `materials_catalog`.
  - Spec: `CLAUDE_RLS.md`.
  - Fix: enforce write-role on `materials_catalog` + `work_types` via RLS; UI gating alone insufficient.

- **D-dbErrors-column-leak** — **`friendlyDbError` echoes DB column names into user-facing text on 23502** (NEW)
  - `src/lib/dbErrors.js:68-72`
  - Evidence: `const col = /null value in column "([^"]+)"/.exec(msg)?.[1]; return col ? \`Can't save — "${col}" is required and came through empty.\` : "…";`
  - For not-null violations the raw DB column name (e.g. `tenant_id`, `sales_name`, `auth_id`) is surfaced to the UI. Not an XSS vector (React text render), but leaks schema detail. Low risk; hygiene.
  - Spec: OWASP A05.
  - Fix: map the small set of user-visible columns to friendly labels; fall back to a generic message for unknowns.

- **E-pdf-lib** — **`pdf-lib@1.17.1` unmaintained (~4 years) on the signed-PDF path** (carry)
  - `package.json:16`
  - `npm view pdf-lib time.modified` → 2022-05-12. Used in `invoicePdf.js`, `payAppPdf.js`, `sovPdf.js` — money-document / signed-PDF path. No open CVEs today.
  - Spec: OWASP A06.
  - Fix: track alternative (`@cantoo/pdf-lib` fork, `pdfme`) for migration.

- **E-dompurify** — **`dompurify <=3.4.12` XSS bypasses (optional transitive of jspdf, `.html()` path unused)** (carry)
  - `package-lock.json` (optional transitive of jspdf)
  - No `html2canvas` or `.html(` call in `src/` — code path unreachable.
  - Spec: OWASP A03, A06.
  - Fix: no action unless jsPDF `.html()` is adopted; then add `overrides` forcing dompurify ≥3.5.

- **F-stray-fix-script** — **Stray Python here-doc script tracked under `src/pages/`** (carry)
  - `src/pages/fix_proposals_pdf.py:1`
  - Evidence: `cat > fix_proposals_pdf.py << 'ENDOFSCRIPT'` — here-doc wrapper around a Python patch targeting the pre-v52 `src/pages/Proposals.jsx` path.
  - Inert (never imported); no runtime bug. Seeds a copy-paste that violates Rule 3 and targets a stale path.
  - Spec: `CLAUDE.md` Data Integrity Rule 3, Workflow Rules.
  - Fix: `git rm src/pages/fix_proposals_pdf.py`.

## Diff vs last audit

Prior audit: `audit-reports/2026-08-03.md` on branch `claude/audit-2026-08-03` (merged to main).

Posture then (2026-08-03): 0 critical · 20 high · 31 medium · 16 low.
Posture now (2026-08-09): 0 critical · 23 high · 34 medium · 23 low.

### Resolved since 2026-08-03

- None.

### Persistent (same root cause; new lines reflected where they drifted)

- **A1, A2, A1b, A2b** — hardcoded URL + publishable-key fallbacks in `supabasePublic.js:16,20` and `Login.jsx:59,62`. Literal drift between the two publishable-key fallbacks unchanged.
- **A4** — `invite-user` logs `linkData.action_link` at `:128`. Fourth audit cycle.
- **A5** — `stripe-webhook` logs recipient email at `:49`.
- **A6** — `invite-user` logs invitee + inviter PII at `:88`.
- **A7** — `send-proposal` logs recipient + rep emails at `:139`.
- **B10, B13** — legacy-seed permissive policies on `tenant_config_update` (`sql/rls_identity_tables.sql:94`) and `proposal_signatures_delete` (`sql/rls_child_tables.sql:261`).
- **B11** — legacy-seed `team_members_update` at `sql/rls_identity_tables.sql:64`.
- **B-archive-grant-anon** — `sql/history_locker_phase1.sql:118-122` — GRANT ALL to anon on the archive schema.
- **B-archive-tenant-helper** — `sql/history_locker_phase1.sql:76` missing `AND active = true` (also `sql/rls_cleanup_and_remaining.sql:389`).
- **B-legacy-search-path** — `sql/db_hardening_phase1.sql:12` seed body of `get_user_tenant_id()`.
- **B-PublicInvoicePage-wide-select** — `PublicInvoicePage.jsx:51` still ships `proposal_wtc(*)` to anon via viewing_token. Fourth audit cycle.
- **C-qb-auth** — `qb-auth verify_jwt = false` at `supabase/config.toml:27` (line shifted from `:14` due to a new `[functions.follow-up-reminders]` block for the cron; the flag itself is unchanged).
- **C-reset-password** — undeclared `origin` at `reset-password/index.ts:66`. Fourth cycle.
- **C16** — `qb-record-payment` service-role skips tenant check (`:103`).
- **C-invite-user-listusers**, **C-reset-password-listusers**, **C-invite-user-sender**, **C-deactivate-user-count**, **C-qb-record-payment-net**, **C-create-billing-session**, **C-qb-search-customers**, **C-qb-void-invoice**, **C-extract-sov**, **C-proposal-signed-rate**, **C-proposal-signed-error**, **C-stripe-webhook-reclaim**, **C-send-invoice-sender**, **C-check-orphan-error**, **C-deactivate-payment-link-cors**, **C-invoice-existence-oracles**, **C-qb-sync-invoice-svc**, **C9**, **C10**, **C18** — all edge-function carry-overs at their current lines.
- **D3, D4, D6, D7, D8, D10** — client-only role gates on Settings (`:645`, moved 654→645), ProposalDetail delete (`:533`), Home (`:105`), SalesDash (`:107`), CallLog sessionStorage PII (`:114`), Invoices destructive handlers (`:1727/:1767/:1909/:1983/:2016/:2048`, shifted from prior `:1714/:1754/:1896/:1970/:2003/:2035` due to invoice-header commits). Every one still exposed.
- **D5** — `CallLogDetail.handleDelete` now at `:280` (moved from `:235` after the plain-English DB-error + stale-page guard commits). Client-gated only.
- **D9** — email persisted to localStorage regardless of Remember checkbox at `Login.jsx:38`.
- **D-WTCCalculator** at `:2110/:2169` (moved from `:2114/:2173` — retention-release cascade fix touched sibling files).
- **D-ProposalDetail-batch** — `deleteWtc:302`, `toggleWtcLock:310`, `handlePullBack:556`, `commitSendToSchedule:629`, `handleInternalApprove:788` (moved from `:806`).
- **D-MobilizationsEditor** — `persist:1600` (moved from `:1614`).
- **D-ArchiveProposalPanel** — `handleSave:1712` (moved from `:1726`).
- **D-PayAppDetailModal-delete**, **D-PayAppDetailModal-savelines**, **D-NewPayAppModal-create** (`:125`, moved from `:123`), **D-NewPayAppModal-id** (`:170`), **D-MultiGCWizard-server-verify**, **D-handleToggleDeposit** (`:2016`, moved from `:2003`), **D-materials-catalog-wtc** — pay-app / catalog carry-overs.
- **F1** — `ProposalDetail:1256` (moved from `:1271`) still reads `parseFloat(p.total)` for Summary fallback — Rule 2 violation, 4th cycle.
- **F-PayAppDetailModal-cascade** — `handleSaveLines:249` (moved from `:233`) still misses `invoice_lines.amount` cascade. 4th cycle.
- **F-stray-fix-script** — `src/pages/fix_proposals_pdf.py` still in tree.
- **E1 (ws), E3 (vite), E-react-router, E4 (postcss), E5 (brace-expansion/js-yaml), E-pdf-lib, E-dompurify** — dep advisories unchanged from prior audit; no `package.json`/`package-lock.json` commit in the 7-day window.

### Severity changes this cycle

- **E-react-router** — re-classified. Prior audit called it "unused SSR/RSC advisories"; grep of `src/` shows 17 files import react-router-dom (BrowserRouter + hooks). SSR/RSC subset remains unreachable; but the **client-side backslash open-redirect (GHSA-wrjc-x8rr-h8h6, <7.18.0) IS reachable in principle** — kept at MEDIUM this cycle because every `navigate(...)` call site currently passes hard-coded or id-interpolated internal paths. Track any future feature that would pass a user-supplied string to `navigate()`.

### Regressed

- None.

### New this cycle

- **D-PayAppDetailModal-savelines-syncedLock (HIGH)** — `PayAppDetailModal.jsx:249-253` writes `invoices.amount` + `retention_amount` for a QB-synced invoice with no `syncedLock` branch. Compound with F-PayAppDetailModal-cascade: a pay-app line edit silently mutates a QB-synced invoice AND desyncs the invoice_lines. Rule 6 + 7 double violation.
- **D-ArchiveProposalModal-handleCreate (HIGH)** — `ArchiveProposalModal.jsx:79-109` mints a proposal in status Sold with `approved_at` set and any sold amount, with no `teamMember` role check. History Locker's live-side counterpart. Combined with the still-open `D-ArchiveProposalPanel.handleSave` and the archive-invoice recompute path, a fully client-authored path from "job exists" to "invoiceable Sold proposal with historical billed amount" with zero role gate.
- **D-ImportToLiveWizard-replace-delete (HIGH)** — `ImportToLiveWizard.jsx:393-395` hard-deletes `job_work_types` + `call_log` with no role gate. Preflight children-check at `:386-391` is racy. Adds a second ungated entry point for `call_log` DELETE alongside D5.
- **B-proposals-invoices-select-deleted-at (MEDIUM)** — `sql/rls_core_tables.sql:138` (proposals), `:185` (invoices) — neither SELECT policy filters `deleted_at IS NULL`. Any PostgREST-direct query (or UI regression) resurrects soft-deleted rows. Defense-in-depth gap.
- **C-extract-sov-ssrf (MEDIUM)** — `extract-sov/index.ts:141` fetches `pdf_url` with no host allowlist. Ownership check only confirms the URL matches a tenant-stored `billing_schedule.contract_pdf_url`, which can be any string. Own-tenant SSRF into internal networks / cloud metadata; response inferred from Anthropic's SOV output.
- **C-repNotify-hardcoded-brand (MEDIUM)** — `_shared/repNotify.ts:19` (`FROM_ADDRESS = "Sales Command <estimates@hdspnv.com>"`) + `:79-80` (h2 `High Desert Surface Prep`). All rep-notification emails for every tenant onboarded are sent from HDSP's address with HDSP branding. Multi-tenant boundary broken the moment a second tenant onboards.
- **E-nanoid (MEDIUM)** — `nanoid <3.3.17` transitive of postcss. Infinite-loop DoS on zero/negative size. Build-only; nanoid is invoked with hard-coded sizes.
- **A8 (LOW)** — `send-proposal/index.ts:184` logs full Resend response body (echoes recipient on error) AND interpolates it into the client 500 at `:187`.
- **A9 (LOW)** — `send-pay-app/index.ts:99` logs recipient email on every invocation.
- **A10 (LOW)** — `_shared/repNotify.ts:118` logs recipient email + Resend body on success — multiplies PII log surface across proposal-approved + follow-up-reminders.
- **A11 (LOW)** — `follow-up-reminders/index.ts:128-131` dumps cross-tenant `{job_number:sales_name}` pairs into a single log line every run when any rep is inactive/missing.
- **A12 (LOW)** — `reset-password/index.ts:129` logs full Resend response body; also interpolated into the returned error at `:132`.
- **D-dbErrors-column-leak (LOW)** — `src/lib/dbErrors.js:68-72` echoes raw DB column name into user-facing text on 23502 not-null violations. Not XSS; schema-detail leak.

### Not re-flagged this cycle

- **D-sc_calllog_cache-shape** — carry-over from 2026-08-03. Re-verified this cycle: the JSON.parse at `CallLog.jsx:44` is inside a try/catch and the result is assigned to state without shape validation. Same risk profile as prior LOW, no change; dropped from this cycle's list because no new evidence and the same-origin XSS-primary threat model is unchanged.

### Not verifiable from this repo (moved to command-suite-db)

These were on the 2026-06-28 carry-over triage list; the parent tracker in `command-suite-db` should re-audit.
- B2 / B2b / B2c anon signing-token SELECT `deleted_at`.
- B3 `invoices_public_view_token` missing `deleted_at IS NULL`.
- B4 `get_public_tenant_config` no tenant scope.
- B5 `get_rep_contact` cross-tenant enumeration.
- B7 `qb_connection.tenant_id` nullable.
- B8 customers viewing/signing tokens missing `deleted_at`.
- B12 `mark_proposal_signed` accepts caller-supplied IP.
- B15 `mark_proposal_signed` initial SELECT missing `deleted_at`.
- B16 `clone_proposal_to_gcs` SECURITY DEFINER no admin gate (paired with D-MultiGCWizard-server-verify).
- B17 `job-attachments` storage DELETE policy lacks tenant scope.
- B-invoice_recipients-update role flip.
- B-invoice_lines-token missing parent `deleted_at`.

## Coverage

### Pass A — Secrets & config
- Grep for `service_role`, `eyJ`, `sk_live`, `sk_test`, `SUPABASE_SERVICE`, `BEARER`, `sb_publishable_`, `sb_secret_`, `whsec_`, `sk-ant-`, `re_[a-zA-Z0-9]{20,}`, `rk_` across `.ts/.tsx/.js/.jsx/.mjs/.json/.toml/.html`.
- `service_role` usage in `src/` (none — confined to `supabase/functions/`).
- `VITE_*` env enumeration — only `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` referenced.
- `.gitignore` + `git ls-files`: `.env*` + `supabase/.temp/` absent from index.
- `console.log/warn/error/info` scan across all 23 edge functions + `src/`.
- NEW code paths reviewed: `proposal-approved/index.ts`, `follow-up-reminders/index.ts`, `_shared/repNotify.ts`, `send-pay-app` timezone fix.

### Pass B — RLS & data access (in-repo evidence only)
- All 9 `sql/*.sql` legacy seed files read end-to-end.
- Verified no new `sql/` files added since 2026-08-03.
- Grepped `sql/` for `TO anon`, `USING (true)`, `USING (active = true)`, `WITH CHECK (true)`, `GRANT`, and the 2026-04-26 anti-pattern shape (`TO anon USING (… IS NOT NULL)` without a `request_signing_token()`/`request_viewing_token()` match) — no active hits; only historical comment refs.
- Read `src/pages/PublicSigningPage.jsx` end-to-end — uses `createPublicClient`, RPC-only reads, no sensitive-table selects.
- Read `src/pages/PublicInvoicePage.jsx` end-to-end — explicit column list on `invoices`, wide select on `invoice_lines + proposal_wtc(*)` still leaks cost basis.
- Cross-checked `archive.get_user_tenant_id` vs `public.get_user_tenant_id` for consistency — divergent on `active = true`.
- Reviewed `src/lib/TenantConfigContext.jsx` + `src/lib/config.js` (commit 9cc47cd cache-poisoning fix): anon read returns fresh DEFAULTS without caching; module-scoped cache invalidated on `onAuthStateChange`; only real rows cached. Cross-tenant poisoning surface closed.
- Soft-delete `deleted_at` coverage check for proposals + invoices SELECT policies.

### Pass C — Edge functions
- All 23 functions read end-to-end plus `_shared/{cors.ts, tenantAuth.ts, recipientAllowlist.ts, attachments.ts, repNotify.ts}` and `supabase/config.toml`.
- **NEW** `proposal-approved` — cron-secret authenticated via `timingSafeEqual` (correct), tenant-scoped via row read, error path leaks `error.message`.
- **NEW** `_shared/repNotify.ts` — rep resolution tenant-scoped and active-filtered; log leaks flagged; hardcoded HDSP FROM+brand flagged.
- **NEW/changed** `follow-up-reminders` — cross-tenant log-mixing on unassigned dump flagged.
- **CHANGED** `send-pay-app` — 5pm Pacific date fix reviewed (todayLocal via `en-CA` + `America/Los_Angeles`, matches `invoices.sent_at` date-column semantics — clean).
- CORS: exact-host allowlist (BASE_ALLOWED_ORIGINS + PREVIEW_ORIGINS + per-fn extraOrigins) — no wildcard/regex; C18 (missing Allow-Methods) still open.
- Auth: `authenticateCaller` / `requireAdminOrManager` applied to money/admin fns; cron endpoints protected by cron-secret + platform JWT.
- Webhook signature: stripe-webhook + scc-stripe-webhook HMAC-SHA256 verified; `processed_stripe_events` claim/done idempotency.
- Tenant scope: money-writing fns assert `invoice.tenant_id === caller.tenantId` (with service-role bypass noted).
- Recipient/attachment allowlists in send-invoice/-pay-app/-proposal.
- SSRF: `attachments.ts` + `isAllowedStorageUrl` pin host + bucket. `extract-sov` pdf_url fetch flagged as own-tenant SSRF (NEW C-extract-sov-ssrf).
- SQL-injection into QB Query language: escaping present in qb-search-customers, qb-link-customer, qb-create-job, qb-sync-invoice.

### Pass D — Client-side React
- `dangerouslySetInnerHTML`, `eval`, `new Function`, `setTimeout(string)` — 0 matches.
- `javascript:` URIs — 0 matches (defensive comment only).
- Untrusted href/src review — Invoices `invoice_attachments` uses `isSafeAttachmentHref` allowlist; PublicSigningPage/CallLogDetail/ProposalDetail attachments use `getPublicUrl`; PayAppDetailModal uses stored pdf_url/sov_pdf_url/waiver_url (safe).
- `JSON.parse` of untrusted input — only `CallLog.jsx:44` sessionStorage (try/catch wrapped).
- localStorage/sessionStorage inventory: Login (sc_remember/sc_last_email), App (sc_session_only/sc_recovery_mode), CallLog (sc_calllog_cache with PII).
- Client-side role gate audit across ProposalDetail, CallLogDetail, Invoices, PayAppDetailModal, NewPayAppModal, WTCCalculator, MobilizationsEditor, ArchiveProposalPanel, ArchiveProposalModal, ImportToLiveWizard, MultiGCWizard, Settings.
- File-upload sanitization — every `storage.upload` call sanitizes; system-generated names in PublicSigningPage/invoicePdf/sovPdf/NewPayAppModal safe.
- NEW `src/lib/dbErrors.js` reviewed — no XSS sink; column-name echo flagged as LOW.
- NEW `TenantConfigContext.jsx` cache fix — cross-tenant mixing check passed (cache dropped on auth transition).
- NEW `src/lib/workTypes.js` custom-only refactor + tenant filter application checked across Settings, WTCCalculator, ArchiveProposalModal, ImportToLiveWizard, NewInquiryWizard, CallLogDetail (filter-bar sites intentionally keep the full catalog).
- proposal-approved is called by a DB trigger, not from `src/` (grep confirms no `supabase.functions.invoke("proposal-approved")` anywhere in src/) — client-side surface for this new fn is nil.

### Pass E — Dependencies
- `npm audit --json` — 8 findings triaged (0 critical, 4 high advisories with reachability analysis).
- `git diff HEAD~30 -- package.json package-lock.json` — no dep changes in the 7-day window since 2026-08-03.
- `package.json` scripts — dev/build/lint/preview only, no pre/postinstall.
- `package-lock.json` registries — only `xlsx@0.20.3` from `cdn.sheetjs.com` (documented, integrity present).
- SPA-only reachability applied to vite/ws/postcss/react-router SSR advisories.
- react-router usage grep across 17 import sites — reversed prior audit's "unused" mislabel; kept at MEDIUM because latent.
- pdf-lib usage confirmed in `payAppPdf.js`, `invoicePdf.js`, `sovPdf.js` (templates internal).
- jspdf `.html()` usage — 0 matches, dompurify path unreachable.

### Pass F — Project invariants
- Data Integrity Rules 1-7 — `fmt$` `maximumFractionDigits=0`; `calcWtcPrice`/`calcMaterialRow`/`calcTravel` usage in new/changed money paths (Invoices.handleSaveEdit, ProposalDetail.handleLock, WTCCalculator.handleLock, PayAppDetailModal.handleSaveLines).
- Retention-release cascade fix (commit 45e204b, `src/lib/calc.js`): reviewed `sumContractBilled` aggregator + call sites in CallLogDetail/ProposalDetail/Invoices — read-side aggregator, no Rule 6 write regression.
- Column-name correctness — `full_name`, `total_price`, `accepted_at`, `job_type`, `call_log.date` — 0 matches.
- Storage filename sanitization — every user-supplied upload path sanitizes; system-generated names safe.
- RLS anti-pattern grep on `sql/*.sql` — only in negation comments; no bare `xxx IS NOT NULL` policies.
- DMS-1 Phase 5 (commit 6290801, ProposalDetail.jsx): "stop writing legacy materials table at Send-to-Schedule" — canonical `job_wtcs` write + `call_log.stage` update + `field_sow` mirror intact.
- TenantConfigContext + config.js: single-tenant deploy today; onAuthStateChange clears cache; no cross-tenant poisoning surface.
- Work-types custom-only refactor: `selectableWorkTypes` tenant filter applied in Settings, WTCCalculator, ArchiveProposalModal, ImportToLiveWizard, NewInquiryWizard, CallLogDetail; filter-bar sites intentionally keep the full catalog.
- UI-hidden-vs-save-guarded (Rule 7) for Invoices.jsx handleSaveEdit: syncedLock / isArchiveInvoice / linkedPayApp / isDepositInvoice / retention-release fallthrough all explicitly branched. NOT branched: PayAppDetailModal.handleSaveLines syncedLock (flagged as NEW HIGH).

### Skipped / out of scope
- `supabase/migrations/*.sql` — moved to `command-suite-db` on 2026-06-29 (see "Not verifiable from this repo" list above). Parent tracker in `command-suite-db` must re-audit.
- Live pg_policies inspection — requires DB access; audit is source-file level.
- SECURITY DEFINER RPC bodies (`get_public_proposal_view`, `mark_proposal_signed`, `mark_recipient_viewed`, `get_rep_contact`, `get_public_tenant_config`, `clone_proposal_to_gcs`, `is_admin_or_manager`) — live in command-suite-db.
- Cross-repo RLS impact (`sch-command`, `field-command`, `AR-Command-Center`) — CLAUDE_RLS.md notes as policy-author responsibility; no sales-command policy shipped since 2026-08-03.
- Dynamic testing / live-endpoint probing — static review only.
- Runtime log-sink configuration (Supabase log retention, Vercel log drains) — out of repo scope.
- Visual style rules — not mechanically verifiable via grep without false positives.

