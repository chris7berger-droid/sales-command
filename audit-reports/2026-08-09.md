# Sales Command Security Audit — 2026-08-09

## Summary
_pending_

## Top 3 Urgent
_pending_

## Findings by Severity
### Critical
### High
### Medium
### Low

<!-- Pass A findings begin -->

- **A4** — **`invite-user` logs full magic-link `action_link` via `linkData`**
  - `supabase/functions/invite-user/index.ts:128`
  - Evidence: `console.log("generateLink result:", { linkData, linkErr: linkErr?.message });`
  - `linkData.properties.action_link` is a single-use magic-link that signs the invitee in and lets them set a password. Written to Supabase edge-function logs on every invite — anyone with log-read access (or a future log breach / third-party sink) can hijack the newly-invited account before the legitimate invitee redeems it. Fourth audit cycle in a row.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09.
  - Fix: `console.log("generateLink result:", { hasLink: !!linkData?.properties?.action_link, linkErr: linkErr?.message });` — never log linkData.

- Bumped to **HIGH** for the log-secret class per severity rubric (single-use auth link in logs = exploitable by any actor with log access).

<!-- Pass A hardcoded-fallback set (all MEDIUM) -->

- **A1** — **Hardcoded Supabase project URL fallback in `supabasePublic.js`**
  - `src/lib/supabasePublic.js:16`
  - Evidence: `const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL || "https://pbgvgjjuhnpsumnowuym.supabase.co";`
  - A misbuilt non-prod deploy silently routes to prod; project ref baked into every browser bundle. Carry-over.
  - Spec: OWASP-ASVS V14.3.2, 12-factor III.
  - Fix: drop the `||` fallback and throw at module load like `src/lib/supabase.js:8` already does.

- **A2** — **Hardcoded Supabase publishable-key fallback in `supabasePublic.js`**
  - `src/lib/supabasePublic.js:20`
  - Evidence: `const SUPABASE_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY || "sb_publishable_LL6wYC7kJDPQfGYeKAaLaWKoK";`
  - After anon-key rotation, stale bundles keep authenticating with the OLD key. Not a bearer secret, but defeats env-var rotation. Carry-over.
  - Spec: OWASP-ASVS V14.3.2, OWASP A05.
  - Fix: remove the literal; require the env var (throw on missing).

- **A1b** — **Hardcoded URL fallback on Login forgot-password path**
  - `src/pages/Login.jsx:59`
  - Evidence: `` `${import.meta.env.VITE_SUPABASE_URL || "https://pbgvgjjuhnpsumnowuym.supabase.co"}/functions/v1/reset-password` ``
  - Same rotation hazard as A1. Carry-over.
  - Spec: OWASP-ASVS V14.3.2.
  - Fix: import from `src/lib/supabase.js`.

- **A2b** — **Hardcoded publishable-key fallback on Login (DIFFERENT literal than A2 — key drift)**
  - `src/pages/Login.jsx:62`
  - Evidence: `apikey: import.meta.env.VITE_SUPABASE_ANON_KEY || "sb_publishable_v7XktVvkAlX7y5f6xoFjng_AaLaWKoK"`
  - The literal here (`v7XktVvkAlX7y5f6xoFjng_…`) still differs from `supabasePublic.js:20` (`LL6wYC7kJDPQfGYeKAaLaWKoK`). Exactly the rotation-drift the audit has flagged for months. Carry-over.
  - Spec: OWASP-ASVS V14.3.2, OWASP A05.
  - Fix: remove both literals; require the env var everywhere.

- **A5** — **`stripe-webhook` logs full recipient email for every outbound receipt**
  - `supabase/functions/stripe-webhook/index.ts:49`
  - Evidence: `console.log("Email to", to, ":", res.status);`
  - Customer email address written to function logs on every payment-receipt send. PII in logs; searchable index of who is being billed. Carry-over.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09, GDPR Art. 5(1)(c).
  - Fix: `console.log("Email send status:", res.status);` — drop `to`.

- **A6** — **`invite-user` logs invitee + sender emails and names on every call**
  - `supabase/functions/invite-user/index.ts:88`
  - Evidence: `console.log("invite-user invoked", { email, name, teamMemberId, senderEmail, senderName });`
  - Every invite writes invitee email + name + inviter email + name to function logs. Same PII-in-logs class as A5; enumerable by anyone with Supabase log access. Carry-over.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09, GDPR Art. 5(1)(c).
  - Fix: log only `{ hasEmail: !!email, teamMemberId }`.

- **A7** — **`send-proposal` logs recipient + rep emails on every send**
  - `supabase/functions/send-proposal/index.ts:139`
  - Evidence: `console.log("send-proposal invoked", { proposalId, recipientEmail, repEmail, proposalNumber });`
  - Same PII-in-logs pattern. Every proposal-send writes customer + rep emails. Carry-over.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09.
  - Fix: log only `{ proposalId, proposalNumber, hasRecipient: !!recipientEmail }`.

- **A8** — **`send-proposal` logs full Resend response body (echoes recipient on error)** (NEW)
  - `supabase/functions/send-proposal/index.ts:184`
  - Evidence: `console.log("Recipient email response:", customerRes.status, customerResBody);`
  - `customerResBody` is Resend's raw JSON — on validation-failure paths Resend echoes the offending `to` address plus internal error text. Second PII surface beyond line 139, and the raw body is later interpolated into the client error response at :187 (`\`Failed to send email: ${customerResBody}\``) so a diagnostics message from Resend surfaces to the browser too.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09.
  - Fix: log status only; parse and log only `{ id }` on success; return a generic 500 to the client and keep the body server-side.

- **A9** — **`send-pay-app` logs recipient email on every invocation** (NEW)
  - `supabase/functions/send-pay-app/index.ts:99`
  - Evidence: `console.log("send-pay-app invoked", { payAppId, invoiceId, recipientEmail });`
  - Every pay-app send writes the customer's billing email to edge logs. Same shape as A6/A7, in a code path that changed this cycle.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09, GDPR Art. 5(1)(c).
  - Fix: log `{ payAppId, invoiceId }` only.

- **A10** — **`_shared/repNotify.sendEmail` logs recipient email + Resend response body on success** (NEW)
  - `supabase/functions/_shared/repNotify.ts:118`
  - Evidence: `console.log(\`${tag}: emailed ${to} — ${body}\`);`
  - Shared helper logs the rep's email address on every proposal-approved / follow-up-reminders send AND dumps Resend's full response body. Multiplies the PII log surface across the two new callers (proposal-approved, follow-up-reminders). The intent (:113 comment) is loud-on-failure to avoid silent send drops — the success path doesn't need `to` in the log line.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09.
  - Fix: log a Resend id only on success (parse `body` for `id`), drop `to`; keep the error-path detail as-is if the failure-visibility goal requires it.

- **A11** — **`follow-up-reminders` logs sales rep names + job numbers for all unassigned rows** (NEW)
  - `supabase/functions/follow-up-reminders/index.ts:128`
  - Evidence (:128-131): `` console.error(`follow-up-reminders: ${unassigned.length} due follow-up(s) have no active rep with an email — ` + unassigned.map((r) => `${r.display_job_number || r.id}:"${r.sales_name ?? ""}"`).join(", ")); ``
  - New nightly cron dumps job numbers paired with rep names on every run when any rep is inactive/missing. Internal-only PII, but noisier per-run than the per-invocation logs.
  - Spec: OWASP-ASVS V7.1.1.
  - Fix: log a count only; persist detail to a `follow_up_reminder_failures` table if the operator needs it.

- **A12** — **`reset-password` logs full Resend response body** (NEW)
  - `supabase/functions/reset-password/index.ts:129`
  - Evidence: `console.log("Reset email response:", emailRes.status, emailResBody);`
  - Resend body on the reset path can echo the target email on validation failures. Also interpolated verbatim into the returned error at :132 (`\`Failed to send reset email: ${emailResBody}\``), surfacing internal error text to the caller (pairs with the existing `error.message` leak at :145).
  - Spec: OWASP-ASVS V7.1.1, OWASP A09.
  - Fix: log status only; parse and log the returned email id on success; return a generic 500 externally.

<!-- Pass A findings end -->

<!-- Pass E findings begin -->

- **E-react-router** — **`react-router-dom@7.13.1` — 11 advisories; open-redirect via backslash is client-reachable but latent** (severity re-scored this cycle)
  - `package.json:19`
  - Evidence: `"react-router-dom": "^7.13.1"`
  - Prior audits marked this dep "unused SSR/RSC" — INCORRECT. Grep of `src/` shows 17 files import `react-router-dom` (BrowserRouter, Routes/Route, useNavigate, useParams, useSearchParams, useLocation, Navigate). SSR/RSC advisories (turbo-stream RCE, RSC XSS, __manifest DoS, single-fetch DoS, deserializeErrors constructor injection) do NOT apply — grep confirms no `react-router-dom/server`, `createStaticHandler`, `renderToString`, `RouterProvider`, `createBrowserRouter` imports. The client-side open-redirect via backslash in `<Link>`/`useNavigate` (GHSA-wrjc-x8rr-h8h6, affects <7.18.0) IS reachable in principle, but every `navigate(...)` call site passes hard-coded or id-interpolated internal paths (Home, ProposalDetail, Customers, Invoices, CallLog, etc.) — no user-controlled target reaches the router. Latent, not currently exploitable.
  - Spec: OWASP A06.
  - Fix: `npm install react-router-dom@^7.18.2` (closes all 11 advisories; minor bump, drop-in for v7). Track any future feature that would pass a user-supplied string to `navigate()`.

- **E-pdf-lib** — **`pdf-lib@1.17.1` unmaintained (~4 years, on the signed-PDF path)**
  - `package.json:16`
  - Evidence: `"pdf-lib": "^1.17.1"`
  - `npm view pdf-lib time.modified` → 2022-05-12. No open CVEs today, but used in `src/lib/invoicePdf.js`, `src/lib/payAppPdf.js`, `src/lib/sovPdf.js` — the money-document / signed-PDF path. A future disclosure would sit in a security-critical code path with no upstream to fix. Carry-over.
  - Spec: OWASP A06.
  - Fix: track alternative (`@cantoo/pdf-lib` fork, `pdf-lib-plus-encrypt`, `pdfme`) for eventual migration.

- **E-vite** — **`vite@8.0.10` fs.deny bypass + launch-editor NTLM (dev-only, Windows)**
  - `package.json:31`
  - Evidence: `"vite": "^8.0.10"`
  - GHSA-fx2h-pf6j-xcff + GHSA-v6wh-96g9-6wx3. Reachable only while `vite dev` is running on Windows; prod is static Vercel bundle. Carry-over.
  - Spec: OWASP A06.
  - Fix: `npm install -D vite@^8.0.16`.

- **E-ws** — **`ws` DoS + uninit-memory (dev-only transitive via vite HMR)**
  - `package-lock.json` (transitive of vite HMR)
  - GHSA-96hv-2xvq-fx4p + GHSA-58qx-3vcg-4xpx. Fixed in 8.21.0+. Only in dev server; prod build never includes ws. Carry-over.
  - Spec: OWASP A06.
  - Fix: `npm audit fix` — bumps transitively when vite is updated.

- **E-postcss** — **`postcss <=8.5.22` sourceMappingURL path traversal (build-only)**
  - `package-lock.json` (transitive of vite)
  - GHSA-r28c-9q8g-f849 / GHSA-fxqj-rqcc-2cmp. All CSS in this repo is inline JS strings via `src/lib/tokens.js`; no untrusted CSS files enter the build. Carry-over.
  - Spec: OWASP A06.
  - Fix: `npm audit fix`.

- **E-nanoid** — **`nanoid <3.3.17` infinite loop on zero/negative size (build-only)** (NEW)
  - `package-lock.json` (transitive of postcss)
  - GHSA-28wg-ghj8-5hjv / GHSA-2v37-7h3g-55p8. Requires attacker control of the `size` arg. nanoid is only present as a build-time postcss dep, invoked with hard-coded sizes. Not shipped to prod bundle.
  - Spec: OWASP A06.
  - Fix: bumped transitively when postcss/vite update.

- **E-eslint-transitives** — **`brace-expansion` + `js-yaml` DoS advisories (dev-only via ESLint)**
  - `package-lock.json` (transitives of ESLint)
  - GHSA-3jxr-9vmj-r5cp / GHSA-mh99-v99m-4gvg / GHSA-rgw5-rvv9-x895 (brace-expansion); GHSA-52cp-r559-cp3m / GHSA-5p4m-2wfm-xmqj (js-yaml). Reachable when ESLint parses attacker-controlled globs/YAML — dev-time only. Carry-over.
  - Spec: OWASP A06.
  - Fix: `npm audit fix`.

- **E-dompurify** — **`dompurify <=3.4.12` XSS bypasses (jsPDF `.html()` path unused)**
  - `package-lock.json` (optional transitive of jspdf)
  - Multiple XSS advisories. jspdf pulls dompurify only via its `.html()` / html2canvas path. Grep confirms no `html2canvas` import and no `.html(` call anywhere in `src/`; only `.text()`, `.rect()`, primitives used in `PublicSigningPage.jsx:115`, `invoicePdf.js`, `payAppPdf.js`, `sovPdf.js`. Dead code path. Carry-over.
  - Spec: OWASP A03, A06.
  - Fix: no action required unless jsPDF `.html()` is adopted; then add `overrides` to force dompurify ≥3.5.

<!-- Pass E findings end -->

<!-- Pass B findings begin -->

- **B-PublicInvoicePage-wide-select** — **`PublicInvoicePage` leaks `proposal_wtc.*` (burden_rate, markup_pct, materials cost) to anon viewers**
  - `src/pages/PublicInvoicePage.jsx:51`
  - Evidence: `.from("invoice_lines").select("*, proposal_wtc(*, work_types(name))").eq("invoice_id", inv.id);`
  - Anon token-gated select on `invoice_lines` joins `proposal_wtc(*)` — the response ships `regular_hours, ot_hours, burden_rate, ot_burden_rate, markup_pct, materials (jsonb cost basis with unit costs + freight), travel, discount, sales_sow, field_sow, sub_areas` — internal cost + margin — to every invoice recipient's browser. The sibling `PublicSigningPage` was hardened against this in the H6/2026-05-02 cycle via a SECURITY DEFINER RPC; `PublicInvoicePage` was never migrated. Line 225 calls `calcWtcPrice(wtc, …)` client-side, confirming these fields cross the wire. Anyone with a valid viewing token — or anyone the token leaks to (a forwarded email, an intermediate MTA log) — can read labor rates and material cost basis. Fourth audit cycle unfixed.
  - Spec: `CLAUDE.md`, `CLAUDE_RLS.md`, OWASP-ASVS V5.1.3, OWASP A01.
  - Fix: mirror the H6 pattern — add `get_public_invoice_view(p_token uuid)` SECURITY DEFINER RPC returning only invoice header + per-line `{wtc_name, wtc_index, line_amount, billing_pct, locked_line_total}`; delete the direct `invoice_lines` select and the client-side `calcWtcPrice` call.

- **B-archive-grant-anon** — **`archive` schema `GRANT ALL … TO anon` with ALTER DEFAULT PRIVILEGES**
  - `sql/history_locker_phase1.sql:118-122`
  - Evidence: `GRANT USAGE ON SCHEMA archive TO anon, authenticated; GRANT ALL ON ALL TABLES IN SCHEMA archive TO anon, authenticated; ALTER DEFAULT PRIVILEGES IN SCHEMA archive GRANT ALL ON TABLES TO anon, authenticated;`
  - Anon is granted full DML on `archive.import_batches` and `archive.legacy_records`, and `ALTER DEFAULT PRIVILEGES` auto-grants every future archive table to anon. Data protection today rests entirely on the RLS policies at :86-109 which use `archive.get_user_tenant_id()` returning NULL for anon → predicate NULL → rows denied. Two defense-in-depth failures: (a) the RLS policies do NOT specify `TO authenticated`, so they default to PUBLIC — one future policy with `USING (true)` inherits anon access; (b) the RLS policies cover only SELECT/INSERT/DELETE, no UPDATE — but the GRANT includes UPDATE, so any future permissive UPDATE policy opens a write path for anon. Legacy seed file; prod state may differ, but a fresh deploy from this seed re-establishes the grants. Carry-over.
  - Spec: `CLAUDE_RLS.md` (least privilege), OWASP-ASVS V8.1.5, OWASP A05.
  - Fix: `REVOKE ALL ON ALL TABLES IN SCHEMA archive FROM anon; REVOKE USAGE ON SCHEMA archive FROM anon;` grant to `authenticated` only; scope every RLS policy with `TO authenticated`; `ALTER DEFAULT PRIVILEGES … TO authenticated` only.

- **B11 (legacy-seed)** — **`sql/rls_identity_tables.sql:64` still ships the pre-lockdown `team_members_update` policy**
  - `sql/rls_identity_tables.sql:64`
  - Evidence: `CREATE POLICY "team_members_update" ON public.team_members FOR UPDATE TO authenticated USING (tenant_id = public.get_user_tenant_id()) WITH CHECK (tenant_id = public.get_user_tenant_id());`
  - Prior CRITICAL. **Prod claim of fix**: commit 37fa00f references migration `20260722120000_team_members_role_lockdown.sql` in `command-suite-db` deployed 2026-07-22 with §8 smoke-verified. Cannot re-verify prod from this repo. The legacy seed here still contains the anti-pattern; a fresh deploy from these seed files re-introduces the vulnerability, and the file is not marked deprecated. Escalation path: any tenant user can rewrite another user's `auth_id`, `role`, or `active` — including pointing an admin's row at their own auth.uid() to inherit admin queries on next request. Fourth cycle unfixed at the seed.
  - Spec: `CLAUDE_RLS.md` role-gating, OWASP-ASVS V8.2, OWASP A01.
  - Fix: update this seed to the lockdown pattern (subquery on `team_members.role = 'Admin'`) so a fresh deploy is safe, or delete the file with a comment pointing at the `command-suite-db` migration as source of truth.

- **B10 (legacy-seed)** — **`sql/rls_identity_tables.sql:94` `tenant_config_update` still lacks admin gate**
  - `sql/rls_identity_tables.sql:94`
  - Evidence: `CREATE POLICY "tenant_config_update" ON public.tenant_config FOR UPDATE TO authenticated USING (id = public.get_user_tenant_id()) WITH CHECK (id = public.get_user_tenant_id());`
  - Any tenant member can UPDATE tenant_config — company_name, logo_url, license_number, monthly_billing_goal, yearly_billing_goal, stripe_customer_id/stripe_subscription_id, apps[], default_burden_rate. Prod state unverifiable (migrations moved to `command-suite-db`); legacy seed is still permissive. Pairs with D3 (Settings.handleSave has no client gate either).
  - Spec: `CLAUDE_RLS.md` role-gating, OWASP A01, A05.
  - Fix: same pattern as B11 — subselect on `team_members.role = 'Admin'`.

- **B13 (legacy-seed)** — **`sql/rls_child_tables.sql:261` `proposal_signatures_delete` allows any tenant user to erase signature evidence**
  - `sql/rls_child_tables.sql:261`
  - Evidence: `CREATE POLICY "proposal_signatures_delete" ON public.proposal_signatures FOR DELETE TO authenticated USING (tenant_id = public.get_user_tenant_id());`
  - Signatures are legal evidence (signer_name, signed_at, ip_address, pdf_url). Allowing any tenant member to DELETE wipes forensic record while leaving the proposal in Sold. Prod state unverifiable.
  - Spec: OWASP-ASVS V7.1, OWASP A08.
  - Fix: drop DELETE policy entirely (signatures append-only) or gate on `role='Admin'`; if pull-back is needed, add a `voided_at` column instead.

- **B-archive-tenant-helper** — **`archive.get_user_tenant_id()` doesn't filter `active = true` — deactivated employee retains archive access**
  - `sql/history_locker_phase1.sql:76-82`
  - Evidence: `SELECT tenant_id FROM public.team_members WHERE auth_id = auth.uid() LIMIT 1;` — no `AND active = true`.
  - Divergent from `public.get_user_tenant_id()` at `sql/rls_identity_tables.sql:129-137` which restricts to `active = true`. A deactivated employee with a stale refresh token retains read/insert/delete access to their old tenant's `archive.import_batches` and `archive.legacy_records`. Offboarding hole. Same shape at `sql/rls_cleanup_and_remaining.sql:389` where the helper is redefined without the `active` filter.
  - Spec: OWASP-ASVS V7.1, OWASP A07.
  - Fix: add `AND active = true` to the WHERE clause in both places, matching the public helper.

- **B-proposals-invoices-select-deleted-at** — **`proposals_select` + `invoices_select` don't filter `deleted_at IS NULL`** (NEW)
  - `sql/rls_core_tables.sql:138` (proposals), `:185` (invoices)
  - Evidence (`:138-140`): `CREATE POLICY "proposals_select" ON public.proposals FOR SELECT TO authenticated USING (tenant_id = public.get_user_tenant_id());`
  - Both `proposals` and `invoices` carry `deleted_at timestamptz` soft-delete (per `db_hardening_phase1.sql:161-165` + CLAUDE.md schema). Neither SELECT policy filters `deleted_at IS NULL`. CLAUDE.md rules assume `deleted_at NULL` = active; the DB does not enforce that. Any authenticated tenant user querying PostgREST directly (or a UI regression that stops filtering `deleted_at` client-side) resurrects soft-deleted proposals + invoices — including any their tenant explicitly deleted. Defense-in-depth gap.
  - Spec: OWASP-ASVS V8.3.4, OWASP A01.
  - Fix: add `AND deleted_at IS NULL` to `proposals_select` and `invoices_select`; add separate admin-only `_deleted_select` policies for restore workflows.

<!-- Pass B findings end -->

<!-- Pass C findings begin -->

- **C-reset-password** — **`reset-password` references undeclared `origin` — branding always defaults to Sales Command; risk of ReferenceError if runtime tightens**
  - `supabase/functions/reset-password/index.ts:66`
  - Evidence: `const isSchedule = origin.includes("schmybiz") || origin.includes("schedulecommand");`
  - `origin` is never declared, imported, or read from `req.headers`. In Deno Edge `globalThis.origin` resolves to `""` unless `--location` is set, so every reset link is branded Sales Command and points to `salescommand.app`. Schedule Command users always get the wrong branding. If Deno tightens the resolution to throw ReferenceError, the outer catch (line 145) leaks the raw error via `error.message`. Fourth audit cycle unfixed.
  - Spec: OWASP A05, A09, OWASP-ASVS V5.1.5.
  - Fix: `const origin = req.headers.get("origin") || "";` at the top of the handler; also collapse `error.message` returns to `"Internal error"` with a correlation ID.

- **C16** — **`qb-record-payment` service-role branch skips tenant check**
  - `supabase/functions/qb-record-payment/index.ts:103`
  - Evidence: `if (!caller.isServiceRole && invoice.tenant_id !== caller.tenantId) { return unauthorizedResponse(403, corsHeaders); }`
  - For user callers the gate is correct; for the service-role path (stripe-webhook) there is no cross-validation that the Stripe event was about this invoice. Today's chain is safe because stripe-webhook verifies signature, but any future internal call or compromise of any service-role function lets an attacker post a phantom QB Payment against any tenant's invoice. Carry-over.
  - Spec: OWASP-ASVS V13.2.1, CWE-345.
  - Fix: stripe-webhook should pass `originating_tenant_id`; qb-record-payment must assert it matches `invoice.tenant_id`. Also skip when `invoice.qb_payment_id` is already set (idempotency).

### Medium

- **C-invite-user-sender** — **`invite-user` accepts caller-supplied `senderEmail` without verified-domain gate**
  - `supabase/functions/invite-user/index.ts:153`
  - Evidence: `from: senderEmail || "noreply@salescommand.app",`
  - `send-invoice`/`send-pay-app`/`send-proposal` gate the `From:` to VERIFIED_DOMAINS; `invite-user` doesn't. Resend blocks fully unverified domains, so the exploitable case is picking any Resend-verified tenant domain to spoof a colleague's invite. Carry-over.
  - Spec: OWASP A04, CWE-345.
  - Fix: apply the VERIFIED_DOMAINS gate; fall back to `noreply@salescommand.app` for unverified domains.

- **C-invite-user-listusers** — **`invite-user` lists auth.users with default pagination (50/page); cross-tenant enumeration primitive**
  - `supabase/functions/invite-user/index.ts:102`
  - Evidence: `const { data: { users } } = await supabase.auth.admin.listUsers(); const existing = users?.find((u: any) => u.email === email);`
  - Default page size 50. Beyond the first page, existing users appear absent → the invite links to `authId = undefined`. Also `listUsers` returns global auth.users → an "already registered" branch on a match from another tenant silently attaches that user's auth_id to this tenant's team_members row at :114-119, enabling cross-tenant login. Carry-over.
  - Spec: OWASP-ASVS V8.3.4, CWE-1284.
  - Fix: use `supabase.auth.admin.getUserByEmail(email)` or a direct service-role `SELECT` against `auth.users`; block linking if the found user belongs to another tenant.

- **C-reset-password-listusers** — **`reset-password` uses the same unpaginated `listUsers()` pattern**
  - `supabase/functions/reset-password/index.ts:55`
  - Same failure mode: users past the first page appear to not exist; function silently returns success without sending a reset email. Persistent hard-block on password reset at scale. Carry-over.
  - Spec: CWE-1284.
  - Fix: same as invite-user — direct-lookup call.

- **C-deactivate-user-count** — **`deactivate-user` job-count query not tenant-scoped**
  - `supabase/functions/deactivate-user/index.ts:119`
  - Evidence: `.from("call_log").select("id", { count: "exact", head: true }).eq("sales_name", member.name);`
  - Service-role client with no tenant filter. Same-named reps in different tenants leak each other's row counts. Carry-over.
  - Spec: OWASP A01.
  - Fix: `.eq("tenant_id", callerTenantId)`.

- **C-qb-record-payment-net** — **`qb-record-payment` `netAmount` ignores `retention_amount`**
  - `supabase/functions/qb-record-payment/index.ts:153`
  - Evidence: `const netAmount = (invoice.amount || 0) - (invoice.discount || 0);`
  - `send-invoice:161` computes Stripe-charged amount as `amount - discount - retentionAmount`; qb-record-payment excludes retention. QB Payment recorded at the wrong amount on any invoice with retention → credit balance in QB or LinkedTxn validation failure. Money-reconciliation issue. Carry-over.
  - Spec: OWASP A04 (insecure design), CWE-682.
  - Fix: `netAmount = amount - discount - (invoice.retention_amount || 0);`.

- **C-create-billing-session** — **`create-billing-session` lacks admin/manager gate**
  - `supabase/functions/create-billing-session/index.ts:57`
  - Any tenant member can open the Stripe billing portal or initiate a checkout that writes `tenant_config.stripe_customer_id`. Portal enables self-service cancel / plan-change / payment-method rotation. Carry-over.
  - Spec: OWASP-ASVS V4.2.1.
  - Fix: switch to `requireAdminOrManager`.

- **C-qb-search-customers** — **`qb-search-customers` has no role gate**
  - `supabase/functions/qb-search-customers/index.ts:86`
  - Any active team_member can enumerate the tenant's full QB customer list. Carry-over.
  - Spec: OWASP-ASVS V4.2.1.
  - Fix: `requireAdminOrManager`.

- **C-qb-void-invoice** — **`qb-void-invoice` does not require admin/manager**
  - `supabase/functions/qb-void-invoice/index.ts:63`
  - Voiding mutates the QB audit trail permanently. Any active team_member can void any tenant invoice. Carry-over.
  - Spec: OWASP-ASVS V4.2.1, CWE-863.
  - Fix: `requireAdminOrManager`; also explicitly reject `caller.isServiceRole` unless documented.

- **C-extract-sov** — **`extract-sov` has no rate limit / cost gate on paid Anthropic API**
  - `supabase/functions/extract-sov/index.ts:159`
  - `claude-opus-4-7` + `max_tokens: 16000`. Any tenant user can loop this over their own attached PDFs unbounded — a compromised session drains Anthropic budget in minutes. Carry-over.
  - Spec: OWASP-ASVS V11.1.4, CWE-770.
  - Fix: per-user + per-tenant token-bucket (~10/hr), daily cap, cache by SHA-256 of PDF bytes.

- **C-extract-sov-ssrf** — **`extract-sov` fetches `pdf_url` with no host allowlist — own-tenant SSRF** (NEW)
  - `supabase/functions/extract-sov/index.ts:141`
  - Evidence: `const pdfRes = await fetch(pdf_url);`
  - The ownership check at :128-138 confirms `pdf_url` matches a `billing_schedule.contract_pdf_url` / `contract_pdf_urls` value in the caller's accessible schedules — but that value can be any string a tenant wrote. If a tenant admin (or any user with UPDATE on `billing_schedule`) writes `http://169.254.169.254/latest/meta-data/` or a private-network URL to `contract_pdf_url`, `extract-sov` fetches it via the service-role edge runtime and streams the response into Anthropic's context. Response can be inferred from the SOV Claude returns, an internal-network + cloud-metadata read via the SOV output.
  - Spec: OWASP A10 (SSRF), OWASP-ASVS V13.4.1.
  - Fix: reuse the `_shared/attachments.ts` `isAllowedStorageUrl()` — require `pdf_url` host === Supabase project host AND path under `job-attachments/`. Reject everything else with 400.

- **C-proposal-signed-rate** — **`proposal-signed` has no rate limit + response-code oracle**
  - `supabase/functions/proposal-signed/index.ts:31`
  - Public endpoint. No IP throttle; INVALID_TOKEN (403) vs ALREADY_SIGNED (409) vs INVALID_SIGNER_NAME (400) responses form a token-state oracle. Carry-over.
  - Spec: OWASP-ASVS V11.1.4, CWE-307.
  - Fix: token-bucket by client IP (5/15min); collapse token errors into a single generic 403 externally.

- **C9** — **Raw `error.message` returned across 23 catch handlers (now includes new `proposal-approved`)**
  - Pattern across all functions: `check-orphan-users:33,61`, `create-billing-session:211`, `deactivate-payment-link:99`, `deactivate-user:131`, `delete-user:120`, `invite-user:191`, `qb-auth:196`, `qb-create-job:317`, `qb-link-customer:149`, `qb-record-payment:193`, `qb-search-customers:123`, `qb-sync-invoice:456`, `qb-void-invoice:195`, `reset-password:145`, `scc-stripe-webhook:188`, `send-invoice:551`, `send-pay-app:508`, `send-proposal:225`, `stripe-webhook:143,270`, `follow-up-reminders:90,197`, `proposal-signed:108,144`, `extract-sov:194`, **NEW: `proposal-approved:87`** (`return json(500, { error: (error as Error).message });`).
  - Leaks Postgres error text (columns, constraint names, cross-tenant FK IDs), Supabase auth internals, QB Fault details, JSON stack fragments. Carry-over — extended by the new proposal-approved handler this cycle.
  - Spec: OWASP-ASVS V7.4.1, OWASP A09, CWE-209.
  - Fix: shared `safeError(e)` helper — log full message server-side, return generic message + request-id externally.

- **C10** — **`send-invoice` returns raw Stripe + Resend response bodies verbatim**
  - `supabase/functions/send-invoice/index.ts:349` (Stripe), `:467` (Resend main), `:491` (Resend viewer warnings)
  - `send-pay-app:42` implements `sanitizeProviderError()`; `send-invoice` was not updated. Bodies can include request-ids, delivery diagnostics, and header/config values. Carry-over.
  - Spec: OWASP-ASVS V7.4.1, OWASP A09.
  - Fix: promote `sanitizeProviderError` to `_shared/`; redact Bearer/`sk_`/`pk_`/`re_` tokens; cap at 500 chars.

- **C-send-invoice-sender** — **`send-invoice` `senderEmail` only checked for domain, not identity**
  - `supabase/functions/send-invoice/index.ts:278`
  - Evidence: `const fromAddress = senderEmail && VERIFIED_DOMAINS.includes(senderDomain) ? senderEmail : "noreply@salescommand.app";`
  - VERIFIED_DOMAINS blocks external spoofing, but any authenticated tenant member can send `From:` any co-worker's address by supplying that string. No cross-check that senderEmail matches caller's team_members row. `send-pay-app:218-223` was updated to resolve sender server-side; `send-invoice` was not migrated. Carry-over.
  - Spec: OWASP A04.
  - Fix: resolve senderEmail from `team_members.email WHERE auth_id = caller.userId` (mirror send-pay-app).

- **C-repNotify-hardcoded-brand** — **`_shared/repNotify.ts` hard-codes HDSP `From:` address + branding for all tenants** (NEW)
  - `supabase/functions/_shared/repNotify.ts:19` (FROM), `:79-80` (h2/tagline)
  - Evidence: `const FROM_ADDRESS = "Sales Command <estimates@hdspnv.com>";` and (`:79`) `<h2 …>High Desert Surface Prep</h2><p …>Industrial &amp; Commercial Concrete Coatings</p>`
  - Both new proposal-approved and follow-up-reminders paths route through this shared helper. Every rep-notification email — for ANY tenant onboarded to the platform — is sent From `estimates@hdspnv.com` with HDSP branding in the body. Multi-tenant Command Suite ships to a second tenant → their reps receive emails claiming to be from HDSP. Data-integrity + brand-integrity issue; not currently an unauth exploit but breaks tenant boundary the moment a second tenant onboards.
  - Spec: `CLAUDE.md` Command Suite Shared-Data Contract; OWASP A04 (insecure design).
  - Fix: resolve FROM address + company name + tagline from `tenant_config` (as `send-proposal` already does at :143-147); fall back to `noreply@salescommand.app` when the tenant's From-domain is not Resend-verified.

### Low

- **C-qb-auth** — **`qb-auth` has `verify_jwt = false` at the Supabase platform layer**
  - `supabase/config.toml:27`
  - Evidence: `[functions.qb-auth]` block with `verify_jwt = false`
  - Platform JWT gate off; handler calls `authenticateCaller`/`requireAdminOrManager` so unauth callers are rejected at the app layer, but every other admin function relies on the platform gate as defense in depth. A future handler-auth refactor becomes a public service-role OAuth endpoint that writes `qb_connection` across tenants. Carry-over.
  - Spec: Supabase production checklist, OWASP-ASVS V4.1.5.
  - Fix: set `verify_jwt = true`; keep in-handler role gate as additional defense.

- **B-legacy-search-path** — **`sql/db_hardening_phase1.sql:12` `get_user_tenant_id()` seed body has no SET search_path AND uses tenant_config fallback**
  - `sql/db_hardening_phase1.sql:12-15`
  - Evidence: `CREATE OR REPLACE FUNCTION public.get_user_tenant_id() RETURNS uuid AS $$ SELECT id FROM public.tenant_config LIMIT 1; $$ LANGUAGE sql SECURITY DEFINER STABLE;`
  - Earliest SECURITY DEFINER body has no `SET search_path` and returns the first tenant regardless of caller identity. Overwritten by the hardened version in `rls_identity_tables.sql:129-137`, but between the two runs (fresh deploy, or the second file skipped) the function is exposed to search_path injection and cross-tenant leakage. Prior audit tracked this via `docs/plans/s1_remove_get_user_tenant_id_fallback.md`.
  - Spec: Supabase function search_path hardening.
  - Fix: delete this section from the seed (canonical body lives in `rls_identity_tables.sql`), or replace with the auth-aware body + `SET search_path = public`; do the same for `public.set_updated_at()` at :109.

- **C-stripe-webhook-reclaim** — **`stripe-webhook` re-process branch can double-record QB Payment**
  - `supabase/functions/stripe-webhook/index.ts:101`
  - Evidence: `// 'claimed' (or unreadable): a prior delivery did not finish → RE-PROCESS.`
  - If a prior delivery updated `invoices.status='Paid'` and `qb-record-payment` succeeded but died before `markDone()`, Stripe retries land here, see "claimed", re-process → `qb-record-payment` runs again. That function does NOT check `invoice.qb_payment_id` before creating a QB Payment (creates unconditionally at :170, overwrites `qb_payment_id` at :175). Net: duplicate QB Payment + duplicate receipt/rep emails on every retry. Carry-over.
  - Spec: CWE-362.
  - Fix: in `qb-record-payment` early-return `{ success: true, alreadyRecorded: true }` when `invoice.qb_payment_id` is set. Second-line fix: mark stripe-webhook rows as `in_progress` vs `claimed`.

- **C-check-orphan-error** — **`check-orphan-users` returns raw `error.message`**
  - `supabase/functions/check-orphan-users/index.ts:33,61`
  - Cron target — caller is Supabase scheduler with cron secret, so external exposure is low. Included for parity with C9. Carry-over.
  - Spec: OWASP A09.
  - Fix: return generic error; log server-side.

- **C-deactivate-payment-link-cors** — **`deactivate-payment-link` catch omits CORS headers**
  - `supabase/functions/deactivate-payment-link/index.ts:99`
  - Catch returns `Content-Type` only — browser hides the body and the UI sees an opaque network error. Carry-over.
  - Spec: CWE-1021.
  - Fix: spread `...corsHeaders` in the catch response.

- **C18** — **`buildCorsHeaders` omits `Access-Control-Allow-Methods`**
  - `supabase/functions/_shared/cors.ts:49`
  - Preflight responses omit Allow-Methods. Chrome/Firefox default permissively so most POSTs still succeed; strict CORS clients refuse. Comment at :12 acknowledges out-of-scope. Carry-over.
  - Spec: CWE-1021.
  - Fix: add `"Access-Control-Allow-Methods": "POST, OPTIONS"`.

- **C-invoice-existence-oracles** — **404-vs-403 oracle across four QB fns lets a caller enumerate cross-tenant invoiceIds**
  - `supabase/functions/deactivate-payment-link/index.ts:41`; `supabase/functions/qb-void-invoice/index.ts:79`; `supabase/functions/qb-sync-invoice/index.ts:127`; `supabase/functions/qb-record-payment/index.ts:95`
  - Evidence (pattern): `if (!invoice) return 404 "Invoice not found"; if (!caller.isServiceRole && invoice.tenant_id !== caller.tenantId) return 403 unauthorized;`
  - Existence check happens BEFORE tenant check — 404 for nonexistent vs 403 for other-tenant. Lets an authenticated attacker enumerate valid invoiceIds across tenants. Carry-over.
  - Spec: OWASP A01, CWE-204.
  - Fix: return 404 uniformly for both branches across all four fns.

- **C-qb-sync-invoice-svc** — **`qb-sync-invoice` service-role branch skips tenant check**
  - `supabase/functions/qb-sync-invoice/index.ts:134`
  - Same shape as C16. No caller invokes qb-sync-invoice with service role today, so impact is defense-in-depth only, but any future internal call with a service key gains cross-tenant privilege. Carry-over.
  - Spec: OWASP A01.
  - Fix: explicitly reject `caller.isServiceRole` unless a documented internal call needs it.

- **C-proposal-signed-error** — **`proposal-signed` returns raw `error.message` on the top-level catch**
  - `supabase/functions/proposal-signed/index.ts:144`
  - Public endpoint — any unauth caller can trigger 500 (e.g. malformed JSON) and read raw Postgres/Deno error text. Every RPC-error branch is mapped to safe codes; this last catch is the only leak vector left. Carry-over.
  - Spec: OWASP A09.
  - Fix: return `{ error: "Sign failed" }`; keep detail in `console.error`.

<!-- Pass C findings end -->

<!-- Pass F findings begin -->

- **F1** — **`ProposalDetail` Summary total falls back to stored `proposals.total` instead of `calcWtcPrice`**
  - `src/components/ProposalDetail.jsx:1256-1258`
  - Evidence: `const sov = sovContractSum != null && sovContractSum > 0 ? sovContractSum : null; const fallback = parseFloat(p.total) || 0; const value = sov != null ? sov : fallback;`
  - Data Integrity Rule 2 forbids reading `proposals.total` — must compute via `calcWtcPrice()` on live WTC data. `proposals.total` is only written on `handleLock`/`handleSave`; an unlocked proposal edited after last lock shows a stale figure directly beside the live WTC breakdown a few lines below (:1268-1272). Fourth cycle unfixed; line moved 1271→1256.
  - Spec: `CLAUDE.md` Data Integrity Rule 2.
  - Fix: compute the fallback with `calcProposalTotal(wtcs, undefined, usesExactPricing(p))` (already imported); use `p.total` only if `wtcs` is empty.

- **F-PayAppDetailModal-cascade** — **`handleSaveLines` updates `invoices.amount` but never propagates to `invoice_lines.amount`**
  - `src/components/PayAppDetailModal.jsx:249-253`
  - Evidence: `if (payApp?.invoice_id) { await supabase.from("invoices").update({ amount: Math.round(gross * 100) / 100, retention_amount: ret, }).eq("id", payApp.invoice_id); }`
  - Rewrites `billing_schedule_pay_app_lines` + `billing_schedule_pay_apps` header + `invoices.amount/retention_amount` — but does NOT update `invoice_lines.amount` for the same invoice. `Invoices.jsx:1805` intentionally PRESERVES `invoice_lines.amount` for pay-app SOV lines (`if (l.billing_schedule_line_id) return {..., amount: parseFloat(l.amount) || 0}`); that preserve is only safe if the pay-app write path had already updated `invoice_lines`. After any pay-app line edit, `invoices.amount` is fresh but `invoice_lines.amount` holds pre-edit numbers — customer's public page + PDF show stale per-line dollars while the header total is fresh. Canonical Data Integrity Rule 6 trap. Fourth cycle unfixed; line moved 233→249.
  - Spec: `CLAUDE.md` Data Integrity Rule 6.
  - Fix: after the `invoices` update, look up matching `invoice_lines` by `(billing_schedule_line_id, invoice_id)` and UPDATE each row's `amount` (and `billing_pct`) to match.

- **F-stray-fix-script** — **Stray Python here-doc script tracked under `src/pages/`**
  - `src/pages/fix_proposals_pdf.py:1`
  - Evidence: `cat > fix_proposals_pdf.py << 'ENDOFSCRIPT'` — a here-doc wrapper around a Python patch that targets `src/pages/Proposals.jsx` (a path from before the v52 extraction) and redefines `calcMaterialRowLocal`/`calcLaborLocal` inline.
  - Inert (never imported), no runtime bug — but seeds a copy-paste that violates Rule 3 (calcs must use `calcMaterialRow()`, not local re-definitions), targets a stale path so anyone re-running it silently writes nothing. Hygiene.
  - Spec: `CLAUDE.md` Data Integrity Rule 3, Workflow Rules.
  - Fix: `git rm src/pages/fix_proposals_pdf.py`.

<!-- Pass F findings end -->


## Diff vs last audit
_pending_

## Coverage
_pending_
