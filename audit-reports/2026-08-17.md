# Sales Command Security Audit — 2026-08-17

## Summary
_pending_

## Top 3 Urgent
_pending_

## Findings by Severity

### Critical

_pending_

### High

- **B-PublicInvoicePage-postgrest** — **`proposal_wtc` cost basis still reachable via direct PostgREST call by any viewing-token holder (DOM hardened; DB grant is the real boundary)** (carry-over — refined framing; backlog S13)
  - `src/pages/PublicInvoicePage.jsx:60-67` (in-code comment naming the residual vector); underlying policy in `command-suite-db`
  - Evidence: `// BUT THIS ALLOW-LIST IS NOT THE SECURITY BOUNDARY — corrected 2026-08-10 by security review. proposal_wtc carries a TABLE-level anon SELECT grant, so a link holder can query burden_rate / markup_pct / pw_rate / materials / discount straight from PostgREST regardless of what this page selects.`
  - The 2026-08-10 change (line 73-79) closed the DOM allow-list on `PublicInvoicePage` — burden_rate/markup/materials no longer cross to the browser via THIS page's fetch. But the underlying anon SELECT grant on `proposal_wtc` remains: any holder of a valid viewing_token or signing_token can call `/rest/v1/proposal_wtc?proposal_id=eq.<id>&select=burden_rate,ot_burden_rate,markup_pct,pw_rate,materials,discount` with the `x-viewing-token` / `x-signing-token` header and receive our cost basis and margin. The comment itself acknowledges this and points at backlog S13. Third audit cycle for the underlying leak.
  - Spec: `CLAUDE_RLS.md`, `CLAUDE.md`, OWASP-ASVS V5.1.3, OWASP A01.
  - Fix: mirror the H6 pattern on invoices — expose only display columns via a SECURITY DEFINER RPC or column-restricted view; `REVOKE SELECT (burden_rate, ot_burden_rate, pw_rate, pw_ot_rate, markup_pct, materials, discount) FROM anon` on the `proposal_wtc` base table (or `REVOKE SELECT … ON proposal_wtc FROM anon` entirely and drop the token-gated SELECT policy).

- **B-archive-grant-anon** — **`archive` schema `GRANT ALL … TO anon` with ALTER DEFAULT PRIVILEGES** (carry-over)
  - `sql/history_locker_phase1.sql:118-122`
  - Evidence: `GRANT USAGE ON SCHEMA archive TO anon, authenticated; GRANT ALL ON ALL TABLES IN SCHEMA archive TO anon, authenticated; GRANT ALL ON ALL SEQUENCES IN SCHEMA archive TO anon, authenticated; ALTER DEFAULT PRIVILEGES IN SCHEMA archive GRANT ALL ON TABLES TO anon, authenticated;`
  - Anon granted full DML on `archive.import_batches` and `archive.legacy_records`, and `ALTER DEFAULT PRIVILEGES` auto-grants every future archive table to anon. Data protection today rests entirely on the RLS policies at :86-109 which use `archive.get_user_tenant_id()` returning NULL for anon. Two defense-in-depth failures: (a) the RLS policies do NOT specify `TO authenticated`, so they default to PUBLIC — one future policy with `USING (true)` inherits anon access; (b) the RLS policies cover only SELECT/INSERT/DELETE, no UPDATE — but the GRANT includes UPDATE, so any future permissive UPDATE policy opens a write path for anon. Legacy seed file; prod state may differ, but a fresh deploy from this seed re-establishes the grants.
  - Spec: `CLAUDE_RLS.md` (least privilege), OWASP-ASVS V8.1.5, OWASP A05.
  - Fix: `GRANT ... TO authenticated` only (drop anon); scope every RLS policy with `TO authenticated`; `ALTER DEFAULT PRIVILEGES ... TO authenticated` only.

- **B11 (legacy-seed)** — **`sql/rls_identity_tables.sql:64` still ships the pre-lockdown `team_members_update` policy** (carry-over)
  - `sql/rls_identity_tables.sql:64`
  - Evidence: `CREATE POLICY "team_members_update" ON public.team_members FOR UPDATE TO authenticated USING (tenant_id = public.get_user_tenant_id()) WITH CHECK (tenant_id = public.get_user_tenant_id());`
  - Prior CRITICAL. **Prod claim of fix**: commit 37fa00f references migration `20260722120000_team_members_role_lockdown.sql` in `command-suite-db` deployed 2026-07-22 with §8 smoke-verified — we CANNOT verify prod from this repo. However, the legacy seed here still contains the anti-pattern; a fresh deploy from these seed files re-introduces the vulnerability, and the file is not marked deprecated. Downgraded from CRITICAL to HIGH because the prod primitive is claimed fixed, but the seed drift is a landing hazard.
  - Spec: `CLAUDE_RLS.md` role-gating, OWASP-ASVS V8.2, OWASP A01.
  - Fix: either (a) update this seed to the lockdown pattern (subquery on `team_members.role = 'Admin'`) so a fresh deploy is safe, or (b) delete the file with a comment pointing at the command-suite-db migration as source of truth.

- **C-send-proposal-allowlist-bypass** — **`send-proposal` recipient-allowlist skipped entirely when the proposal has no `customer_id`** (NEW)
  - `supabase/functions/send-proposal/index.ts:94-115`
  - Evidence: `const customerId = (proposal as any).customer_id || proposal.call_log?.customer_id; if (customerId) { … if (!allowedEmails.has(recipientEmail.toLowerCase())) { return 400 } }` — the allowlist is inside the `if (customerId)` gate.
  - Any authenticated tenant user calling `send-proposal` with a proposalId whose row has NO `customer_id` AND whose `call_log` has NO `customer_id` skips the allowlist entirely, then continues to send Sales Command's branded Resend email — carrying a valid `signing_token` link — to any attacker-supplied `recipientEmail`. Proposals routinely exist without customer_id during normal use: early-stage inquiries, imports, orphan sisters. Reachable now.
  - Spec: OWASP-ASVS V5.1.4, OWASP A01, CWE-345.
  - Fix: invert the gate — `if (!customerId) return 400 "proposal has no linked customer — assign one before sending";` then run the allowlist unconditionally.

- **C-qb-record-payment-idempotency** — **`qb-record-payment` never checks `qb_payment_id` before POSTing to QB `/payment` — reprocessed Stripe events book duplicate QB Payments** (NEW / elevated from prior LOW C-stripe-webhook-reclaim)
  - `supabase/functions/qb-record-payment/index.ts:141-175`
  - Evidence: handler gates on `!invoice.qb_invoice_id` at :141 but has NO check on `invoice.qb_payment_id`. QB POST at :170; `qb_payment_id` only persisted AFTER the POST at :175.
  - `stripe-webhook` reprocess branch (:101, "claimed"→re-process) invokes `qb-record-payment` on every retry. On any invoice whose first delivery succeeded through the QB POST but died before `markDone()`, the next retry creates an additional duplicate `Payment` in QuickBooks against the same invoice, over-crediting the customer and leaving a credit balance / A/R drift. Money integrity impact. Prior audit tracked this as LOW C-stripe-webhook-reclaim at the stripe-webhook layer; the sharper framing is that `qb-record-payment` itself lacks idempotency.
  - Spec: OWASP-ASVS V13.2.5, CWE-362, money-idempotency.
  - Fix: add early-return at top of handler after invoice fetch — `if (invoice.qb_payment_id) return { success: true, alreadyRecorded: true, qbPaymentId: invoice.qb_payment_id };` — Stripe-webhook's `stripe_payment_id` idempotency is separate; this fixes the QB side.

- **C-qb-record-payment-net** — **`qb-record-payment` `netAmount` still ignores `retention_amount` — books gross-minus-discount instead of net collected** (carry-over — elevated to HIGH; money integrity)
  - `supabase/functions/qb-record-payment/index.ts:153`
  - Evidence: `const netAmount = (invoice.amount || 0) - (invoice.discount || 0);`
  - `send-invoice/index.ts:161` computes the Stripe-charged amount as `amount - discount - retentionAmount`. When Stripe fires the webhook and `qb-record-payment` books the payment, it reads directly from `invoices` and excludes retention — QB Payment posts for `amount - discount` which INCLUDES retention that was never collected. On any retention invoice this over-books QB by the retention amount, leaves A/R short by that amount, and the eventual release invoice double-counts. Third audit cycle. Elevated to HIGH: money-reconciliation defect on a shipped path.
  - Spec: OWASP-ASVS V13.2.4, CWE-682, money integrity.
  - Fix: `const netAmount = (invoice.amount || 0) - (invoice.discount || 0) - (Number(invoice.retention_amount) || 0);` (matches send-invoice) — or use `session.amount_total / 100` passed via the webhook.

- **C-reset-password** — **`reset-password` references undeclared `origin` — branding always defaults to Sales Command; ReferenceError if runtime tightens** (carry-over — fourth cycle)
  - `supabase/functions/reset-password/index.ts:66`
  - Evidence: `const isSchedule = origin.includes("schmybiz") || origin.includes("schedulecommand");`
  - `origin` is never declared, imported, or read from `req.headers`. In Deno Edge, `globalThis.origin` resolves to `""` unless `--location` is set, so every reset link is branded Sales Command and points to `salescommand.app`. Schedule Command users always get the wrong branding. If Deno tightens the resolution to throw ReferenceError, the outer catch (:145) leaks the raw error via `error.message`.
  - Spec: OWASP A05, A09, OWASP-ASVS V5.1.5.
  - Fix: `const origin = req.headers.get("origin") || "";` at the top of the handler; also collapse `error.message` returns to `"Internal error"` with a correlation ID.

- **B10 (legacy-seed)** — **`sql/rls_identity_tables.sql:94` `tenant_config_update` still lacks admin gate** (carry-over — elevated back to HIGH)
  - `sql/rls_identity_tables.sql:94`
  - Evidence: `CREATE POLICY "tenant_config_update" ON public.tenant_config FOR UPDATE TO authenticated USING (id = public.get_user_tenant_id()) WITH CHECK (id = public.get_user_tenant_id());`
  - Any tenant member can UPDATE tenant_config — company_name, logo_url, license_number, monthly_billing_goal, yearly_billing_goal, `stripe_customer_id`/`stripe_subscription_id`, `apps[]`, `default_burden_rate`. Prod state unverifiable (migrations moved to command-suite-db); the legacy seed is still permissive. Pairs with D3 (Settings.handleSave has no client gate either). Elevated to HIGH given `stripe_*` columns and rate-affecting `default_burden_rate` are exposed to every tenant member.
  - Spec: `CLAUDE_RLS.md` role-gating, OWASP A01, A05.
  - Fix: same pattern as B11 — subselect on `team_members.role = 'Admin'`.

- **F-Invoices-auto-schedule-rate-card** — **`Invoices.jsx` auto-create billing_schedule fallback sums ALL WTCs into contract_sum without excluding rate cards (F44 regression on a sibling path)** (NEW — both Pass D and Pass F flagged independently)
  - `src/pages/Invoices.jsx:148-165`
  - Evidence: `const { data: wtcRows } = await supabase.from("proposal_wtc").select("*, work_types(name)").eq("proposal_id", p.id); if (wtcRows?.length) { const total = wtcRows.reduce((s, w) => s + calcWtcPrice(w, undefined, exact), 0); const { data: newSch } = await supabase.from("billing_schedule").insert({ proposal_id: p.id, contract_sum: total, retainage_pct: 5, status: "active", }).select().single(); if (newSch) { const lines = wtcRows.map((w, i) => ({ billing_schedule_id: newSch.id, description: w.work_types?.name || \`Work Type ${i + 1}\`, scheduled_value: calcWtcPrice(w, undefined, exact), ordinal: i, })); await supabase.from("billing_schedule_lines").insert(lines);`
  - When a pay-app-flagged customer's Sold proposal reaches Create-Invoice with no existing billing_schedule, this fallback path auto-creates one summing ALL WTCs (line 151) into `contract_sum` — including rate-card WTCs — then inserts one `billing_schedule_lines` row per WTC (line 156) — including a phantom scheduled line for the rate card. `ProposalDetail.jsx:341-362` got the F44 fix (`calcProposalTotal(wtcs) + .filter(w => !w.is_rate_card)`), the sibling `WTCCalculator.jsx:2248/2275` fix, `ProposalPDFModal.jsx:189`, `calc.js:198`, `CallLogDetail.jsx:941` — every OTHER sum-site excludes rate cards. This one does not. A T&M-bearing proposal that lands here re-books the P7 $380-overstatement class of bug and seeds a phantom SOV line the customer never scheduled.
  - Spec: F44 rate-card contract-sum exclusion; `CLAUDE.md` Data Integrity Rule 3/6 (all sum sites must be consistent).
  - Fix: replace the raw `reduce` with `calcProposalTotal(wtcRows, undefined, exact)`; add `.filter(w => !w.is_rate_card)` before the `.map` that builds `billing_schedule_lines`, mirroring `ProposalDetail.jsx:341/356`.

- **D-WTCCalculator** — **`WTCCalculator.handleSave` + `handleLock` write `proposals.total` + `proposal_wtc` money with NO role gate and NO `teamMember` prop** (carry-over — lines moved 2114→2249, 2173→2257)
  - `src/pages/WTCCalculator.jsx:2249` (handleSave); `:2257` (handleLock)
  - Evidence: `const proposalTotal = calcProposalTotal(allWtcs, undefined, exact); await supabase.from("proposals").update({ total: proposalTotal }).eq("id", proposalId);`
  - Both handlers recompute and write `proposals.total` (Data Integrity Rule 4). Component takes no `teamMember` prop and has no role check anywhere. `handleLock` also writes `proposal_wtc.locked` + `locked_line_total` (the H6 snapshot the public signing RPC returns). Any authenticated user who can open a WTC can rewrite money on any proposal in the tenant. Materials-catalog writes flow through the same page via `materialsCatalog.saveCatalogRow`.
  - Spec: `CLAUDE_RLS.md`, `CLAUDE.md` Data Integrity Rule 4, OWASP A01.
  - Fix: pass `teamMember` into WTCCalculator; gate `handleSave`/`handleLock` on `["Admin","Manager"].includes(role) OR sales_name match`; backstop with RLS on `proposal_wtc` UPDATE/INSERT and `proposals.total` UPDATE.

- **D-ProposalDetail-batch** — **`deleteWtc`, `toggleWtcLock`, `handleDelete`, `handlePullBack`, `commitSendToSchedule`, `handleInternalApprove`, `MobilizationsEditor.persist`, `ArchiveProposalPanel.handleSave` — destructive + cross-driver + money-writes on `ProposalDetail`, all client-gated only** (carry-over — expanded; lines shifted)
  - `src/components/ProposalDetail.jsx:302` (deleteWtc), `:310` (toggleWtcLock — writes `proposals.total`), `:535` (handleDelete), `:558` (handlePullBack — hard-deletes `proposal_signatures`, resets status, unlocks WTCs), `:631` (commitSendToSchedule — inserts `jobs` + `job_wtcs`), `:790` (handleInternalApprove — flips status Sold + fires QB job-create), `:1611` (MobilizationsEditor.persist), `:1726` (ArchiveProposalPanel.handleSave — Sold Amount → `proposals.total` + `historical_billed_amount`)
  - None of these handlers check `teamMember.role` before writing. `toggleWtcLock` writes `proposals.total` (Rule 4). `commitSendToSchedule` writes cross-driver into Schedule Command. `handlePullBack` destroys legal signature evidence (pairs with B13). `handleInternalApprove` flips a proposal to Sold and fires an edge-function QB job-create. `MobilizationsEditor.persist` writes `proposals.mobilizations` which feeds cross-driver material-flow. `ArchiveProposalPanel.handleSave` writes the archive Sold Amount that feeds `handleSaveEdit` (the 14000c5 trap upstream). `handleDelete` has a `canDelete` UI gate at :533 but the handler at :535 still fires the write.
  - Spec: `CLAUDE_RLS.md`, `CLAUDE.md` Data Integrity Rule 4/6, OWASP A01, A08.
  - Fix: enforce role/ownership at every handler entry; backstop with RLS on `proposal_wtc` (INSERT/UPDATE/DELETE), `proposals` (UPDATE), `jobs` (INSERT), `job_wtcs` (INSERT), `proposal_signatures` (DELETE), `proposals.mobilizations` (UPDATE).

- **D3** — **`Settings.handleSave` writes `tenant_config` with no role gate** (carry-over — line moved 654→667)
  - `src/pages/Settings.jsx:667` (handler), Save button at `:700`
  - Evidence: `const handleSave = async () => { … await updateTenantConfig({ ...fields, default_burden_rate: ..., monthly_billing_goal: ..., ... }); }`
  - `canManage` gates only WorkTypes + Materials Catalog sub-sections; the top-level form + Save button ships to every role. A Sales Rep can overwrite tenant defaults — rates, goals, license number, `apps[]`, logo URL — that flow through every WTC calc and every customer-facing page. Pairs with B10.
  - Spec: `CLAUDE_RLS.md`, OWASP-ASVS V4.1.3, OWASP A01.
  - Fix: wrap the whole form (and Save) in `{canManage && …}` AND enforce `is_admin_or_manager()` at the RLS level (see B10).

- **D5** — **`CallLogDetail.handleDelete` is client-gated only** (carry-over — line moved 235→285)
  - `src/components/CallLogDetail.jsx:285`
  - Evidence: `const canDelete = teamMember && (teamMember.role === "Admin" || teamMember.name === job.sales_name); async function handleDelete() { … await supabase.from("call_log").delete().eq("id", job.id) … }`
  - `call_log` + `job_work_types` have authenticated DELETE policies (CLAUDE.md); any tenant member can delete any job via direct supabase call.
  - Spec: `CLAUDE_RLS.md`, OWASP A01.
  - Fix: restrict `call_log` DELETE to `(is_admin_or_manager() OR sales_name = current_user_name)`.

- **D6** — **`Home.jsx` sales filter is client-only — not backstopped by RLS** (carry-over)
  - `src/pages/Home.jsx:105` (call_log filter), `:119` (proposals filter)
  - Evidence: `const isRep = !["Admin","Manager"].includes(displayRole); const log = await fetchAll("call_log", "*", { …, filters: isRep ? [["eq", "sales_name", displayName]] : [] });`
  - `displayRole` is a React prop; a Sales Rep who edits it in DevTools flips `isRep=false` and the WHERE clause drops, pulling every rep's pipeline.
  - Spec: `CLAUDE_RLS.md`, OWASP-ASVS V4.1.3, OWASP A01.
  - Fix: RLS SELECT policy on `call_log`/`proposals` limiting non-admin/manager reps to `sales_name = current user's name`.

- **D7** — **`SalesDash` trusts client-supplied `selectedRep`** (carry-over)
  - `src/pages/SalesDash.jsx:107`
  - Evidence: `const filterByRep = selectedRep !== "__all__" ? selectedRep : null; … filters: filterByRep ? [["eq", "sales_name", filterByRep]] : []`
  - Picker UI is hidden for non-admins (`:217`) but the load query trusts `state.selectedRep`. Non-admin sets it in DevTools and pulls another rep's data.
  - Spec: `CLAUDE_RLS.md`, OWASP-ASVS V4.1.3.
  - Fix: RLS SELECT as D6; gate the `team_members` fetch so non-admins see only their own row.

- **D10** — **`Invoices` destructive/money handlers with no role gate — expanded to eleven handlers as T&M and deposit ship** (carry-over — expanded; lines shifted)
  - `src/pages/Invoices.jsx:316` (NewInvoiceModal handleCreate — NEW-listed), `:1890` (updateStatus — NEW-listed), `:1909` (handleQBSync — NEW-listed), `:2008` (handleDelete), `:2048` (handleSaveEdit — includes the T&M rate-card preserve branch at :2099-2101, correct per Rule 6, but the handler itself has no role gate), `:2203` (handleBillRetention), `:2277` (handlePullBack), `:2295` (T&M pull-back branch — NEW-listed, soft-deletes invoice + clears stripe_payment_link_id), `:2332` (handleToggleDeposit — zeros retention), `:2364` (handleVoidConfirm), `:2428` (void-and-replace carry-forward — NEW-listed, mints replacement invoice)
  - Every destructive/money-write handler on InvoiceDetail is reachable by every Sales Rep. Pays-app, deposit, retention-release, T&M, and void-and-replace paths — a rep can void, delete, edit, mint retention-release invoices, flip the deposit toggle to strip retention, soft-delete a T&M invoice, or mint a replacement invoice on void. Expanded coverage compared to prior D10 (six handlers → eleven).
  - Spec: `CLAUDE.md` Data Integrity Rule 6/7, `CLAUDE_RLS.md`, OWASP-ASVS V4.1.3.
  - Fix: define `const canManage = teamMember && ["Admin","Manager"].includes(teamMember.role);`; gate every InvoiceDetail button + handler entry (including the NewInvoiceModal); add matching RLS UPDATE/DELETE policies on `invoices`, RLS INSERT on `invoices` for the void-and-replace mint.

- **D-PayAppDetailModal-delete** — **`handleDeletePayApp` no role gate** (carry-over)
  - `src/components/PayAppDetailModal.jsx:264`
  - Soft-deletes linked invoice + hard-deletes pay-app row. Only guard is `invoice?.qb_invoice_id` short-circuit. No `teamMember` prop.
  - Spec: `CLAUDE.md` Data Integrity Rule 6, OWASP-ASVS V4.1.3.
  - Fix: add role gate before writes; RLS on `billing_schedule_pay_apps` DELETE + `invoices` UPDATE(deleted_at) restricted to admin/manager.

- **D-PayAppDetailModal-savelines** — **`handleSaveLines` rewrites pay-app + invoice money with no role gate AND no syncedLock check** (carry-over — expanded)
  - `src/components/PayAppDetailModal.jsx:233`
  - Rewrites `billed_pct_this_app`, `billed_amount_this_app`, `invoices.amount`, `invoices.retention_amount`. No role gate. NOT protected by `syncedLock` the way `Invoices.handleSaveEdit` is (which preserves stored $ fields when `qb_invoice_id` is set) — a QB-synced pay-app invoice's amount can still move here silently.
  - Spec: `CLAUDE.md` Data Integrity Rule 6, OWASP-ASVS V4.1.3.
  - Fix: role gate + syncedLock (`if invoice.qb_invoice_id: preserve`) at handler head; restrict UPDATE on these columns in RLS to admin/manager.

- **D-NewPayAppModal-create** — **`NewPayAppModal.handleCreate` mints invoices with no role gate** (carry-over)
  - `src/components/NewPayAppModal.jsx:125`
  - Inserts `billing_schedule_pay_apps`, `billing_schedule_pay_app_lines`, and `invoices` (with client-scanned `nextId`). No `teamMember` prop, no role symbol.
  - Spec: `CLAUDE.md` Data Integrity Rule 6, OWASP-ASVS V4.1.3.
  - Fix: gate handler + Create button on canManage; RLS INSERT on `billing_schedule_pay_apps` + `invoices` restricted to admin/manager.

- **D-materials-catalog-wtc** — **`materialsCatalog.saveCatalogRow` write-through has no role gate; called from every WTC via `WTCCalculator.jsx:530`** (carry-over — elevated from LOW to HIGH; write path is more open than prior audit reflected)
  - `src/lib/materialsCatalog.js:48-87`
  - Settings.jsx wraps WorkTypesSection + MaterialsCatalogSection in `{canManage && …}`, but the shared writer is also called from WTCCalculator's inline material picker with no gate. Any tenant member can insert/fork/update `materials_catalog` rows via the WTCCalculator materials tab, bypassing the Settings UI gate. `WTCCalculator` runs for every rep on every WTC edit.
  - Spec: `CLAUDE_RLS.md`.
  - Fix: enforce write-role on `materials_catalog` + `work_types` via RLS; UI gating alone is insufficient because WTCCalculator is a second call site.

- **A4** — **`invite-user` logs full magic-link `action_link` via the `linkData` object** (carry-over — fourth cycle)
  - `supabase/functions/invite-user/index.ts:128`
  - Evidence: `console.log("generateLink result:", { linkData, linkErr: linkErr?.message });`
  - `linkData.properties.action_link` is a one-click magic-link that authenticates the invitee (redirectTo=`https://salescommand.app`). Written to Supabase function logs on every invite; anyone with log-read access can claim the account before the legitimate invitee.
  - Spec: OWASP-ASVS V7.1.1, OWASP-ASVS V3.5.3, OWASP A09.
  - Fix: `console.log("generateLink result:", { hasLink: !!linkData?.properties?.action_link, linkErr: linkErr?.message });`

### Medium

- **D-MultiGCWizard-server-verify** — **MultiGC clone RPC has no client-side role gate — client-server contract needs server-side check verified in `command-suite-db`** (carry-over)
  - `src/components/MultiGCWizard.jsx:188`
  - Evidence: `const { data, error: rpcErr } = await supabase.rpc("clone_proposal_to_gcs", { p_source_proposal_id, p_targets });`
  - No client-side role gate; targets, contact_ids, `markup_override_pct`, and `billing_terms` are attacker-controlled. Security depends on the RPC body in command-suite-db (not verifiable from this repo). Requires: (1) role IN ('Admin','Manager') OR source proposal's `sales_name` match; (2) target `customer_ids`/`contact_ids` belong to caller's tenant; (3) `markup_override_pct` floor.
  - Spec: `CLAUDE_RLS.md`, OWASP A01.
  - Fix: verify `clone_proposal_to_gcs` in command-suite-db; add client-side `canManage` gate on '+ Send to Additional GCs' to match the server contract.

- **D-senderEmail-client-supplied** — **Client-supplied `senderEmail` sent to `send-invoice`, `send-pay-app`, `send-proposal`, and `invite-user` — server contract required** (NEW — cross-cut with C-invite-user-sender / C-send-invoice-sender / C-send-proposal-sender)
  - `src/pages/Invoices.jsx:1003` (`senderEmail: repContact.email || …`); `src/pages/Team.jsx:84` (`invite-user` senderEmail/senderName); `src/components/PayAppDetailModal.jsx:353` (`senderEmail`); `src/components/ProposalPDFModal.jsx:118` (via `send-proposal`)
  - Client is trusted to declare the sender identity. If any edge function uses this body value as From/Reply-To without validating against the caller's authenticated identity, a rep can spoof another rep or the CEO in customer-facing email. Prior audit tracked this on the server side (C-invite-user-sender, C-send-invoice-sender); this D-flag captures the client attack surface — the client should not send `senderEmail` at all.
  - Spec: OWASP A04, CWE-345.
  - Fix: drop `senderEmail` from the client body; edge functions must derive sender from `auth.uid → team_members.email`. Mirror `send-pay-app:218` server-side pattern.

- **F1** — **`ProposalDetail` Summary total uses stale `parseFloat(p.total)` fallback instead of live `calcWtcPrice`** (carry-over — line moved 1271→1260)
  - `src/components/ProposalDetail.jsx:1260`
  - Evidence: `const sov = sovContractSum != null && sovContractSum > 0 ? sovContractSum : null; const fallback = parseFloat(p.total) || 0; const value = sov != null ? sov : fallback;`
  - Data Integrity Rule 2 forbids reading `proposals.total` — must compute via `calcWtcPrice()` on live WTC data. `handleLock`/`handleSave` now write `proposals.total` via `calcProposalTotal` (F44 fixes), so the drift is smaller — but the invariant is still violated: a proposal not re-saved since a WTC edit (or an archive-import pre-F44) shows a stale figure alongside the live per-WTC Price/Cost/Margin table rendered right below at :1271-1296.
  - Spec: `CLAUDE.md` Data Integrity Rule 2.
  - Fix: when `!p.is_archive_proposal && wtcs.length > 0`, use `calcProposalTotal(wtcs, undefined, usesExactPricing(p))` as the fallback; keep the `parseFloat(p.total)` branch only for archive proposals (`wtcs.length === 0`).

- **F-PayAppDetailModal-cascade** — **`handleSaveLines` updates `invoices.amount` but never propagates to `invoice_lines.amount`** (carry-over — fourth cycle unfixed)
  - `src/components/PayAppDetailModal.jsx:233`
  - Evidence (lines 233-253): updates `billing_schedule_pay_app_lines` rows, updates `billing_schedule_pay_apps` header, updates `invoices` (amount, retention_amount) — but does NOT update `invoice_lines.amount` for the same invoice.
  - `NewPayAppModal.jsx:198-211` inserts one `invoice_lines` row per SOV line at pay-app creation keyed by `billing_schedule_line_id`; those rows are read by `Invoices.jsx:1529,1792` for line-item rendering and by `PublicInvoicePage.jsx:73` for customer display, and by `qb-sync-invoice` to build QB Line rows. After any pay-app line edit, `invoices.amount` is fresh but `invoice_lines.amount` still holds pre-edit numbers — public page and PDF show stale per-line dollars while header total is fresh; QB re-sync pushes stale per-line amounts with fresh header total (header ≠ Σ lines in QB). Canonical Data Integrity Rule 6 trap. Backlog B42 (T2 Open).
  - Spec: `CLAUDE.md` Data Integrity Rule 6.
  - Fix: after updating `billing_schedule_pay_app_lines`, look up matching `invoice_lines` by `(billing_schedule_line_id, invoice_id)` and UPDATE each row's `amount` and `billing_pct` to match.

- **C-repNotify-signerName-injection** — **`_shared/repNotify.ts` interpolates unescaped customer-controlled `signerName` (and other fields) into the sales rep's HTML notification email** (NEW — introduced this cycle)
  - `supabase/functions/_shared/repNotify.ts:150-165`
  - Evidence: `Signed by: <strong>${how.signerName || "—"}</strong>` (:154); `<p>Hi ${rep.name}, …` (:160); `${lede}` where lede contains `<strong>${customer}</strong>` (:153); `Reason: ${how.reason || "—"}` (:157).
  - `how.signerName` is populated by `proposal-signed` from `proposal_signatures.signer_name` — a value the **anonymous public signing page** collects from the customer. It, along with `customer`, `jobLabel`, `approvedBy`, `reason`, `proposal.status`, `rep.name`, and `proposal.proposal_number`, is interpolated raw into the internal rep's HTML email. A malicious signer can inject HTML/anchors (e.g. `<a href="https://phish/">Open in Sales Command</a>`) into the rep's inbox, spoofing an internal link. `notifyProposalApproved` fires on every signed proposal.
  - Spec: OWASP-ASVS V5.2.5, OWASP A03, HTML-injection.
  - Fix: escape every interpolated field — reuse `send-proposal`'s `escapeHtml` (or the local `esc` in `follow-up-reminders`) and wrap `customer`, `jobLabel`, `signerName`, `approvedBy`, `reason`, `proposal.status`, `rep.name`, `proposal.proposal_number`.

- **B13 (legacy-seed)** — **`sql/rls_child_tables.sql:261` `proposal_signatures_delete` allows any tenant user to erase signature evidence** (carry-over)
  - `sql/rls_child_tables.sql:261`
  - Evidence: `CREATE POLICY "proposal_signatures_delete" ON public.proposal_signatures FOR DELETE TO authenticated USING (tenant_id = public.get_user_tenant_id());`
  - Signatures are legal evidence (signer_name, signed_at, ip_address, pdf_url). Allowing any tenant member to DELETE wipes forensic record while leaving the proposal in Sold. Prod state unverifiable. `handlePullBack` in `ProposalDetail.jsx` already exercises this — pull-back should be admin-gated and produce an audit row.
  - Spec: OWASP-ASVS V7.1, OWASP A08, OWASP A09.
  - Fix: drop DELETE policy entirely (signatures append-only) or gate on `role='Admin'`; if pull-back is needed, add a `voided_at` column instead.

- **B-archive-tenant-helper** — **`archive.get_user_tenant_id()` doesn't filter `active = true` — deactivated employee retains archive access** (carry-over)
  - `sql/history_locker_phase1.sql:76-83`
  - Evidence: `SELECT tenant_id FROM public.team_members WHERE auth_id = auth.uid() LIMIT 1;` — no `AND active = true`. Note: `SET search_path = public, archive;` was added at line 83 since prior audit (partial hardening).
  - Divergent from `public.get_user_tenant_id()` at `sql/rls_identity_tables.sql:129-137` which restricts to `active = true`. A deactivated employee with a stale refresh token retains read/insert/delete access to their old tenant's `archive.import_batches` and `archive.legacy_records`. Offboarding hole.
  - Spec: OWASP-ASVS V7.1, OWASP A07.
  - Fix: add `AND active = true` to the WHERE clause, matching the public schema helper.

- **B-legacy-search-path** — **`sql/db_hardening_phase1.sql:12` `get_user_tenant_id()` seed body returns first tenant regardless of caller AND has no SET search_path** (carry-over — was LOW, upgraded to MEDIUM because of the CREATE OR REPLACE hazard)
  - `sql/db_hardening_phase1.sql:12`
  - Evidence: `CREATE OR REPLACE FUNCTION public.get_user_tenant_id() RETURNS uuid AS $$ SELECT id FROM public.tenant_config LIMIT 1; $$ LANGUAGE sql SECURITY DEFINER STABLE;`
  - Earliest SECURITY DEFINER body has no `SET search_path` and returns the first tenant regardless of caller identity. Overwritten by the hardened version in `rls_identity_tables.sql:129-137`, BUT `CREATE OR REPLACE` means execution order matters — a fresh deploy that runs these seeds in the wrong order (or a downstream seed that inherits this weakened body) yields cross-tenant reads for every RLS policy that calls the helper. Also missing SET search_path so any search_path override on the connection re-routes public.tenant_config to an attacker-controlled schema.
  - Spec: `CLAUDE_RLS.md`, Supabase function search_path hardening, OWASP A05.
  - Fix: delete this section from the seed (canonical body lives in `rls_identity_tables.sql`); if kept, replace with the auth-aware version + `SET search_path = public` + a comment forbidding future `CREATE OR REPLACE` from downstream seeds.

- **C-qb-auth** — **`qb-auth` has `verify_jwt = false` at the Supabase platform layer** (carry-over)
  - `supabase/config.toml:14`
  - Evidence: `[functions.qb-auth] enabled = true verify_jwt = false`
  - Platform JWT gate off. The handler calls `authenticateCaller`/`requireAdminOrManager` so unauth callers are rejected at the app layer, but every other admin function relies on the platform gate as defense in depth. A future handler-auth refactor becomes a public service-role OAuth endpoint that writes `qb_connection` across tenants.
  - Spec: Supabase production checklist, OWASP-ASVS V4.1.5.
  - Fix: set `verify_jwt = true`; keep in-handler role gate as additional defense.

- **C-invite-user-sender** — **`invite-user` accepts caller-supplied `senderEmail`/`senderName` without verified-domain gate or identity check** (carry-over)
  - `supabase/functions/invite-user/index.ts:153`
  - Evidence: `from: senderEmail || "noreply@salescommand.app",`
  - `send-invoice`/`send-pay-app`/`send-proposal` all gate the `From:` to VERIFIED_DOMAINS; `invite-user` doesn't. Also interpolates `senderName` raw into Subject (`${senderName || "Your team"} invited you…`). Any tenant admin/manager can spoof any address as From.
  - Spec: OWASP A04, CWE-345.
  - Fix: apply the VERIFIED_DOMAINS gate; resolve caller email via `auth.uid → team_members`.

- **C-invite-user-listusers** — **`invite-user` lists auth.users with default pagination (50/page)** (carry-over)
  - `supabase/functions/invite-user/index.ts:102`
  - Evidence: `const { data: { users } } = await supabase.auth.admin.listUsers(); const existing = users?.find((u: any) => u.email === email);`
  - Beyond the first page, existing users appear absent → the invite links to `authId = undefined`, leaving the row broken.
  - Spec: OWASP-ASVS V8.3.4, CWE-1284.
  - Fix: use `supabase.auth.admin.getUserByEmail(email)` or paginate.

- **C-reset-password-listusers** — **`reset-password` uses the same unpaginated `listUsers()` pattern** (carry-over)
  - `supabase/functions/reset-password/index.ts:55`
  - Same failure mode; silent success without sending a reset email past page 1.
  - Spec: CWE-1284.
  - Fix: direct-lookup call.

- **C-deactivate-user-count** — **`deactivate-user` job-count query not tenant-scoped** (carry-over)
  - `supabase/functions/deactivate-user/index.ts:119`
  - Evidence: `.from("call_log").select("id", { count: "exact", head: true }).eq("sales_name", member.name);`
  - Service-role client with no tenant filter. Same-named reps in different tenants leak each other's row counts.
  - Spec: OWASP A01.
  - Fix: `.eq("tenant_id", callerTenantId)`.

- **C-create-billing-session** — **`create-billing-session` lacks admin/manager gate** (carry-over)
  - `supabase/functions/create-billing-session/index.ts:57`
  - Any tenant member can open the Stripe billing portal or initiate a checkout that writes `tenant_config.stripe_customer_id`. Portal enables self-service cancel / plan-change / payment-method rotation.
  - Spec: OWASP-ASVS V4.2.1.
  - Fix: switch to `requireAdminOrManager`.

- **C-qb-search-customers** — **`qb-search-customers` has no role gate** (carry-over)
  - `supabase/functions/qb-search-customers/index.ts:86`
  - Any active team_member can enumerate the tenant's full QB customer list (customer names + subs = job pipeline).
  - Spec: OWASP-ASVS V4.2.1.
  - Fix: `requireAdminOrManager`.

- **C-qb-void-invoice** — **`qb-void-invoice` does not require admin/manager** (carry-over)
  - `supabase/functions/qb-void-invoice/index.ts:63`
  - Voiding mutates the QB audit trail permanently.
  - Spec: OWASP-ASVS V4.2.1, CWE-863.
  - Fix: `requireAdminOrManager`; explicitly reject `caller.isServiceRole`.

- **C-extract-sov** — **`extract-sov` has no rate limit / cost gate on paid Anthropic API** (carry-over)
  - `supabase/functions/extract-sov/index.ts:159`
  - `claude-opus-4-7` + `max_tokens: 16000`. Any tenant user can loop unbounded — a compromised session drains Anthropic budget in minutes.
  - Spec: OWASP-ASVS V11.1.4, CWE-770.
  - Fix: per-user + per-tenant token-bucket (~10/hr), daily cap, cache by SHA-256 of PDF bytes.

- **C-proposal-signed-rate** — **`proposal-signed` has no rate limit + response-code oracle** (carry-over)
  - `supabase/functions/proposal-signed/index.ts:30`
  - Public endpoint. No IP throttle; INVALID_TOKEN (403) vs ALREADY_SIGNED (409) vs INVALID_SIGNER_NAME (400) responses form a token-state oracle.
  - Spec: OWASP-ASVS V11.1.4, CWE-307.
  - Fix: token-bucket by client IP (5/15min); collapse token errors into a single generic 403 externally.

- **C10** — **`send-invoice` returns raw Stripe + Resend response bodies verbatim** (carry-over)
  - `supabase/functions/send-invoice/index.ts:349` (Stripe), `:467` (Resend main), `:491` (Resend viewer warnings)
  - `send-pay-app:42` implements `sanitizeProviderError()`; `send-invoice` was not updated. Bodies can include request-ids, delivery diagnostics, and header/config values.
  - Spec: OWASP-ASVS V7.4.1, OWASP A09.
  - Fix: promote `sanitizeProviderError` to `_shared/`; redact Bearer/`sk_`/`pk_`/`re_` tokens; cap at 500 chars.

- **C-send-invoice-sender** — **`send-invoice` `senderEmail` only checked for domain, not identity** (carry-over)
  - `supabase/functions/send-invoice/index.ts:278`
  - VERIFIED_DOMAINS blocks external spoofing, but any authenticated tenant member can send `From:` any co-worker's address. No cross-check that `senderEmail` matches caller's team_members row.
  - Spec: OWASP A04.
  - Fix: resolve `senderEmail` from `team_members.email WHERE auth_id = caller.userId` (mirror send-pay-app:218-223).

- **C-send-proposal-error** — **`send-proposal` echoes raw Resend body + `error.message` in error responses** (NEW — flagged as sibling of C10)
  - `supabase/functions/send-proposal/index.ts:187` (Resend body), `:225` (raw error.message)
  - Same C9/C10 pattern flagged in send-invoice. `_shared/repNotify.ts` was cleaned up in this cycle but `send-proposal` itself was not.
  - Spec: OWASP-ASVS V7.4.1, OWASP A09.
  - Fix: slice + sanitize Resend body (share the pay-app `sanitizeProviderError`); return generic error text from the outer catch, logging the detail.

- **C9** — **Raw `error.message` returned across 21 catch handlers** (carry-over — list refreshed with current line numbers)
  - Pattern across all functions with catches: `check-orphan-users:33,61`, `create-billing-session:210`, `deactivate-payment-link:98`, `deactivate-user:130`, `delete-user:120`, `extract-sov:194`, `follow-up-reminders:197`, `invite-user:190`, `proposal-approved:87`, `qb-auth:196`, `qb-create-job:316`, `qb-link-customer:148`, `qb-record-payment:193`, `qb-search-customers:122`, `qb-sync-invoice:469`, `qb-void-invoice:194`, `reset-password:145`, `scc-stripe-webhook:187`, `send-invoice:551`, `send-pay-app:507`, `stripe-webhook:143,270`.
  - Leaks Postgres error text (columns, constraint names, cross-tenant FK IDs), Supabase auth internals, QB Fault details, JSON stack fragments.
  - Spec: OWASP-ASVS V7.4.1, OWASP A09, CWE-209.
  - Fix: shared `safeError(e)` helper — log full message server-side, return generic message + request-id externally.

- **C-stripe-webhook-reclaim** — **`stripe-webhook` re-process branch can double-record QB Payment** (carry-over — root cause is C-qb-record-payment-idempotency above)
  - `supabase/functions/stripe-webhook/index.ts:101`
  - Reclaim branch reprocesses without downstream idempotency: the invoice update is idempotent, but the `qb-record-payment` invoke at :173 lacks its own `qb_payment_id` check. Combined with C-qb-record-payment-net, a reprocessed webhook double-books the wrong amount.
  - Spec: CWE-362.
  - Fix: either (a) fix `qb-record-payment` idempotency (see HIGH finding above) OR (b) skip the QB invoke when `invoices.qb_payment_id` is already set.

- **C-stripe-webhook-sendEmail-from** — **`stripe-webhook.sendEmail` passes rep email as `From:` with no VERIFIED_DOMAINS check** (NEW)
  - `supabase/functions/stripe-webhook/index.ts:47`
  - Evidence: `body: JSON.stringify({ from: from || "noreply@salescommand.app", to, subject, html }),`
  - Forwards `from` straight to Resend without the VERIFIED_DOMAINS gate every other send-fn uses. `repEmail` is currently derived from team_members (:209), so today the risk is bounded, but this is the one edge fn that skips the gate everywhere else uses. A non-verified team_members.email either loses the customer receipt (Resend rejects) or is used as a spoof vector depending on Resend config.
  - Spec: OWASP A04, defense-in-depth.
  - Fix: apply the VERIFIED_DOMAINS gate inside `sendEmail`; fall back to `noreply@salescommand.app` for unverified domains.

- **A5** — **`stripe-webhook` logs full recipient email for every outbound receipt** (carry-over)
  - `supabase/functions/stripe-webhook/index.ts:49`
  - Evidence: `console.log("Email to", to, ":", res.status);`
  - Customer email address written to function logs on every payment-receipt send. PII in logs; searchable index of who's getting paid emails.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09, GDPR Art. 5(1)(c).
  - Fix: `console.log("Email send status:", res.status);` — drop `to`.

- **A6** — **`invite-user` logs invitee + sender emails and names on every call** (carry-over)
  - `supabase/functions/invite-user/index.ts:88`
  - Evidence: `console.log("invite-user invoked", { email, name, teamMemberId, senderEmail, senderName });`
  - Every invite writes invitee email + name + inviter email + name to function logs. Same PII-in-logs class as A5; enumerable by anyone with Supabase log access. Combined with A4 (magic-link log two frames later), a log reader gets full account-takeover material.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09, GDPR Art. 5(1)(c).
  - Fix: log only `{ hasEmail: !!email, teamMemberId }`.

- **A7** — **`send-proposal` logs recipient + rep emails on every send** (carry-over)
  - `supabase/functions/send-proposal/index.ts:139`
  - Evidence: `console.log("send-proposal invoked", { proposalId, recipientEmail, repEmail, proposalNumber });`
  - Same PII-in-logs pattern. Every proposal-send writes customer + rep emails.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09.
  - Fix: log only `{ proposalId, proposalNumber, hasRecipient: !!recipientEmail }`.

- **A8** — **`send-pay-app` logs recipient email on every invocation** (NEW)
  - `supabase/functions/send-pay-app/index.ts:99`
  - Evidence: `console.log("send-pay-app invoked", { payAppId, invoiceId, recipientEmail });`
  - Same class as A5/A6/A7 — customer email written to logs on every pay-app send. Not present in the 2026-08-03 findings for this file; the log line has been at line 99 through the sales-rep-notification refactor.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09, GDPR Art. 5(1)(c).
  - Fix: `console.log("send-pay-app invoked", { payAppId, invoiceId, recipientDomain: recipientEmail?.split("@")[1] });`

- **D8** — **`CallLog` cache writes customer PII into sessionStorage** (carry-over)
  - `src/pages/CallLog.jsx:114`
  - Evidence: `sessionStorage.setItem(CACHE_KEY, JSON.stringify({ rows: allLog, team: tm || [], customers: allCx, workTypes: wt || [] }));`
  - `allCx = fetchAll("customers", "*")` — full customer directory (contact_email, contact_phone, billing_email, billing_phone, business_address) plus every call_log row is written to sessionStorage. Any XSS sink exfiltrates the whole customer book in one JSON read.
  - Spec: OWASP-ASVS V8.3.1, OWASP A02, A04, GDPR Art. 32.
  - Fix: cache only `{id, display_job_number, stage, customer_name, sales_name, bid_due, follow_up}`; refetch full detail on click; strip `customers(*)`.

- **D9** — **Email persisted to localStorage on Login regardless of Remember checkbox** (carry-over)
  - `src/pages/Login.jsx:38`
  - Evidence: `localStorage.setItem("sc_remember", remember ? "true" : "false"); localStorage.setItem("sc_last_email", email.trim());`
  - `sc_last_email` written unconditionally; the initial state reader (:11) reuses it. On shared/kiosk devices the previous user's email appears.
  - Spec: OWASP-ASVS V8.3.1, OWASP A02.
  - Fix: `if (remember) localStorage.setItem(…); else localStorage.removeItem("sc_last_email");`

- **D-NewPayAppModal-id** — **Invoice ID generated from client-side max-id scan (race + soft IDOR)** (carry-over)
  - `src/components/NewPayAppModal.jsx:170`; also `src/pages/Invoices.jsx:338` and `:2422` (void-and-replace)
  - Evidence: `const { data: recent } = await supabase.from("invoices").select("id").order("created_at", { ascending: false }).limit(50); … const nextId = String(lastNum + 1).padStart(5, "0");`
  - Concurrent creates collide on the PK constraint; the SELECT also enumerates 50 recent invoice IDs across the tenant. Now three call-sites (was two).
  - Spec: OWASP A04.
  - Fix: server-side RPC or Postgres sequence keyed on tenant.

- **A9** — **`_shared/repNotify.ts` logs sales-rep email on every successful send AND on every Resend failure** (NEW — introduced this cycle)
  - `supabase/functions/_shared/repNotify.ts:118`; also `:99`, `:115`, `:121`
  - Evidence: `console.log(\`${tag}: emailed ${to} — ${body}\`);` (`:118`) plus mirrored logs at `:99` (RESEND_API_KEY missing), `:115` (Resend non-2xx: logs status **and full response body**), `:121` (thrown).
  - New helper. Called from `proposal-approved`, `proposal-signed`, `follow-up-reminders` — every rep notification writes the rep's email + the full Resend response body (which includes the delivered message-id and can include recipient hints) to logs. The `:115` error branch pairs the rep email with the raw Resend body — same class as C10 for send-invoice.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09, GDPR Art. 5(1)(c).
  - Fix: mask `to` in every log (`${to.slice(0,2)}***@${to.split("@")[1]}`) and drop the raw Resend body — log only `res.status` and Resend's returned `id`.

### Low

- **B-soft-delete-select** — **`proposals_select` / `invoices_select` do not filter `deleted_at IS NULL` — soft-deleted rows visible to any tenant SELECT** (NEW)
  - `sql/rls_core_tables.sql:138` (proposals_select), `:185` (invoices_select)
  - Evidence: `CREATE POLICY "proposals_select" ON public.proposals FOR SELECT TO authenticated USING (tenant_id = public.get_user_tenant_id());`
  - Soft-deleted rows (deleted_at NOT NULL) remain visible under RLS; only client-side `.is('deleted_at', null)` filters keep them out of list views. Any list/report/PDF export path that forgets that filter surfaces soft-deleted proposals/invoices as active. Not cross-tenant, but weakens the deleted_at contract that Data Integrity depends on. Prod state unverifiable but the seed defines the shape.
  - Spec: `CLAUDE.md` soft-delete contract, OWASP A04.
  - Fix: add `AND deleted_at IS NULL` to both SELECT `USING` clauses; expose an admin-only view for audit access to soft-deleted rows.

- **C18** — **`buildCorsHeaders` omits `Access-Control-Allow-Methods`** (carry-over)
  - `supabase/functions/_shared/cors.ts:49`
  - Preflight responses omit Allow-Methods. Chrome/Firefox default permissively so most POSTs still succeed; strict CORS clients refuse. Comment at :12 acknowledges out-of-scope.
  - Spec: CWE-1021.
  - Fix: add `"Access-Control-Allow-Methods": "POST, OPTIONS"`.

- **C-invoice-existence-oracles** — **404-vs-403 oracle across four QB fns lets a caller enumerate cross-tenant invoiceIds** (carry-over)
  - `supabase/functions/deactivate-payment-link/index.ts:41`; `supabase/functions/qb-void-invoice/index.ts:79`; `supabase/functions/qb-sync-invoice/index.ts:127`; `supabase/functions/qb-record-payment/index.ts:95`
  - Existence check happens BEFORE tenant check — 404 for nonexistent vs 403 for other-tenant. Lets an authenticated attacker enumerate valid invoiceIds across tenants.
  - Spec: OWASP A01, CWE-204.
  - Fix: return 404 uniformly for both branches across all four fns (or 403 uniformly).

- **C-deactivate-payment-link-cors** — **`deactivate-payment-link` catch omits CORS headers** (carry-over)
  - `supabase/functions/deactivate-payment-link/index.ts:99`
  - Catch returns `Content-Type` only — browser hides the body and the UI sees an opaque network error.
  - Spec: CWE-1021.
  - Fix: spread `...corsHeaders` in the catch response.

- **C-check-orphan-error** — **`check-orphan-users` returns raw `error.message`** (carry-over)
  - `supabase/functions/check-orphan-users/index.ts:33,61`
  - Cron target — caller is Supabase scheduler with cron secret, so external exposure is low. Included for parity with C9.
  - Spec: OWASP A09.
  - Fix: return generic error; log server-side.

- **C-qb-sync-invoice-svc / C16** — **`qb-record-payment:103` and `qb-sync-invoice:134` service-role branch skips tenant check** (carry-over — merged)
  - `supabase/functions/qb-record-payment/index.ts:103`; `supabase/functions/qb-sync-invoice/index.ts:134`
  - Correct by design (comment says so) — but a leaked service-role key becomes a cross-tenant money-write with a single body param. No caller in this repo invokes `qb-sync-invoice` with service role today; only `stripe-webhook` invokes `qb-record-payment` with service role.
  - Spec: OWASP-ASVS V13.2.1, CWE-345.
  - Fix: either add a separate internal endpoint that takes tenant_id explicitly + validates, or have `stripe-webhook` do the DB updates itself and drop the fn-to-fn call.

- **C-send-fns-contacts-tenant** — **`send-proposal:96` and `send-pay-app:193` read `customer_contacts` scoped only by `customer_id` (not tenant_id)** (NEW)
  - `supabase/functions/send-proposal/index.ts:96` (`.eq("customer_id", customerId)` on `customer_contacts`); `supabase/functions/send-pay-app/index.ts:193-195` (same pattern)
  - `customer_contacts` has `tenant_id NOT NULL FK tenant_config` (CLAUDE.md contract). Both reads via service role are scoped only by `customer_id` — tenant scope is implicit via FK. `send-invoice:201` explicitly filters by `tenant_id` as belt-and-suspenders — the two send fns should mirror.
  - Spec: multi-tenant isolation, defense-in-depth.
  - Fix: add `.eq('tenant_id', caller.tenantId)` to both reads.

- **C-proposal-signed-error** — **`proposal-signed` returns raw `error.message` on the top-level catch** (carry-over)
  - `supabase/functions/proposal-signed/index.ts:107` (line moved from :144)
  - Public endpoint — any unauth caller can trigger 500 (malformed JSON) and read raw Postgres/Deno error text.
  - Spec: OWASP A09.
  - Fix: return `{ error: "Sign failed" }`; keep detail in `console.error`.

- **D-sc_calllog_cache-shape** — **sessionStorage cache parsed without schema validation** (carry-over)
  - `src/pages/CallLog.jsx:44`
  - `JSON.parse` is try/catch-wrapped, but parsed rows go straight into state without a shape check.
  - Spec: OWASP A03.
  - Fix: discard cache if `rows`/`team`/`customers`/`workTypes` aren't arrays; add a cache-version stamp.

- **D-attachment-hrefs-safety** — **Attachment anchor URLs rendered without an `isSafeAttachmentHref` allowlist across ProposalDetail, PayAppDetailModal, BillingScheduleSection, CallLogDetail, HistoryLocker** (NEW — consistency gap)
  - `src/components/ProposalDetail.jsx:1212,1240,1383`; `src/components/PayAppDetailModal.jsx:407,426-429,572,589,599,708-717`; `src/components/BillingScheduleSection.jsx:429`; `src/components/CallLogDetail.jsx:847`; `src/components/HistoryLocker/ArchiveRecordDetail.jsx`
  - `Invoices.jsx:848` guards attachment `file_url` via `isSafeAttachmentHref`. Same public-URL source (`job-attachments getPublicUrl`, or stored `contract_pdf_urls` / `pdf_url` / `waiver_url` / `sov_pdf_url`) is rendered raw in every other component. If any row's URL string were ever tampered (via RLS gap, `extract-sov` writes, or direct DB write) to a `javascript:` URI, these `<a href>` would fire on click. Defense-in-depth consistency gap.
  - Spec: OWASP A03.
  - Fix: lift `isSafeAttachmentHref` into `src/lib/utils` and wrap every attachment anchor.

- **F-stray-fix-script** — **Stray Python here-doc script tracked under `src/pages/`** (carry-over)
  - `src/pages/fix_proposals_pdf.py:1`
  - Evidence: `cat > fix_proposals_pdf.py << 'ENDOFSCRIPT'` — a here-doc wrapper around a Python patch that targets `src/pages/Proposals.jsx` (a path from before the v52 extraction) and redefines `calcMaterialRowLocal`/`calcLaborLocal` inline.
  - Inert (never imported), so no runtime bug — but it seeds a copy-paste that violates Rule 3 (calcs must use `calcMaterialRow()`, not local re-definitions), and it targets a stale path so anyone re-running it would silently write nothing. Also confuses grep results (local material-calc code turns up here misleadingly).
  - Spec: `CLAUDE.md` Data Integrity Rule 3, Workflow Rules (hygiene).
  - Fix: `git rm src/pages/fix_proposals_pdf.py`.

- **B-config-wide-select** — **`getTenantConfig` uses `select("*")` on the default client from public / pre-auth code paths** (NEW)
  - `src/lib/config.js:26`
  - Evidence: `const { data } = await supabase.from("tenant_config").select("*").limit(1).single();`
  - Called from `Login.jsx` (pre-auth) and `InvoicePaidPage.jsx` (public). Currently returns empty because tenant_config has no anon SELECT policy — no data leaked today. But (a) it uses the default `supabase` client, not `createPublicClient`, breaking the discipline that public pages MUST use the token-carrying client; (b) uses `select("*")`. If a future policy exposes tenant_config to anon for landing-page branding, this immediately leaks `stripe_customer_id`, `stripe_subscription_id`, `monthly_billing_goal`, `license_number`, `subscription_status`, `apps[]` — every sensitive column on the table. Defense-in-depth failure gated only by the current policy.
  - Spec: `CLAUDE_RLS.md`.
  - Fix: route the branding read through a dedicated RPC (`public.get_public_tenant_config` already exists); allow-list columns in the direct select for authenticated callers.

- **A1** — **Hardcoded Supabase project URL fallback in `supabasePublic.js`** (carry-over)
  - `src/lib/supabasePublic.js:16`
  - Evidence: `const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL || "https://pbgvgjjuhnpsumnowuym.supabase.co";`
  - A misbuilt non-prod deploy silently routes to prod; project ref baked into every browser bundle. Defeats env-driven rotation.
  - Spec: OWASP-ASVS V14.3.2, 12-factor III.
  - Fix: drop the `||` fallback and throw at module load like `src/lib/supabase.js:7-9` already does.

- **A2** — **Hardcoded Supabase publishable-key fallback in `supabasePublic.js`** (carry-over)
  - `src/lib/supabasePublic.js:20`
  - Evidence: `const SUPABASE_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY || "sb_publishable_LL6wYC7kJDPQfGYeKAaLaWKoK";`
  - After anon-key rotation, stale bundles keep authenticating with the OLD key. Not a bearer secret, but defeats the whole point of env-var rotation.
  - Spec: OWASP-ASVS V14.3.2, OWASP A05.
  - Fix: remove the literal; require the env var (throw on missing).

- **A1b** — **Hardcoded URL fallback on Login forgot-password path** (carry-over)
  - `src/pages/Login.jsx:59`
  - Evidence: `` `${import.meta.env.VITE_SUPABASE_URL || "https://pbgvgjjuhnpsumnowuym.supabase.co"}/functions/v1/reset-password` ``
  - Same rotation hazard as A1.
  - Spec: OWASP-ASVS V14.3.2.
  - Fix: import from `src/lib/supabase.js`.

- **A2b** — **Hardcoded publishable-key fallback on Login (DIFFERENT literal than A2 — key drift)** (carry-over)
  - `src/pages/Login.jsx:62`
  - Evidence: `apikey: import.meta.env.VITE_SUPABASE_ANON_KEY || "sb_publishable_v7XktVvkAlX7y5f6xoFjng_AaLaWKoK"`
  - The literal here (`v7XktVvkAlX7y5f6xoFjng_…`) still differs from the literal in `supabasePublic.js:20` (`LL6wYC7kJDPQfGYeKAaLaWKoK`). Exactly the rotation-drift the audit has been calling out for months.
  - Spec: OWASP-ASVS V14.3.2, OWASP A05.
  - Fix: remove both literals; require the env var everywhere.

- **E1** — **`ws@8.19.0` DoS + uninit-memory advisory (dev-only via Vite HMR)** (carry-over)
  - `package-lock.json` (transitive via `vite` HMR socket)
  - GHSA-96hv-2xvq-fx4p (HIGH mem-exhaustion DoS, CVSS 7.5) + GHSA-58qx-3vcg-4xpx (uninit-memory MODERATE). Fixed in 8.21.0+.
  - Only runs in `npm run dev` HMR socket; production is prebuilt static assets on Vercel — never ships. Held at MEDIUM per SPA-only reachability.
  - Spec: OWASP A06.
  - Fix: `npm audit fix` bumps vite→ws transitively.

- **E3** — **`vite@8.0.10` fs.deny bypass + launch-editor NTLM (Windows dev-only)** (carry-over)
  - `package.json:31` devDep `vite: ^8.0.10`
  - GHSA-fx2h-pf6j-xcff (HIGH server.fs.deny bypass) + GHSA-v6wh-96g9-6wx3 (launch-editor NTLM disclosure). Fixed in 8.0.16+.
  - Dev-server-only path traversal on Windows alternate paths; production build is static via Vercel.
  - Spec: OWASP A06.
  - Fix: bump `vite` to `^8.0.16` in devDependencies.

- **E-react-router** — **`react-router-dom@7.13.1` — 14 advisories, SSR/RSC features unused by SPA** (carry-over — advisory count up 2)
  - `package.json:19` (`react-router-dom: ^7.13.1`); `package-lock.json`
  - GHSA-49rj-9fvp-4h2h (turbo-stream RCE, SSR-only), GHSA-8646-j5j9-6r62 (RSC redirect XSS), GHSA-f22v-gfqf-p8f3 (prerendered redirect XSS), GHSA-8x6r-g9mw-2r78 (__manifest DoS, SSR-only), GHSA-rxv8-25v2-qmq8 (single-fetch DoS, SSR-only), GHSA-337j-9hxr-rhxg (SSR hydration), GHSA-chx6-hx7r-mcp5 (route-matching DoS, client), GHSA-qwww-vcr4-c8h2 (RSC CSRF, RSC-only), GHSA-wrjc-x8rr-h8h6 / GHSA-2j2x-hqr9-3h42 (open redirect), GHSA-h8fp-f39c-q6mh (RSCErrorHandler), GHSA-84g9-w2xq-vcv6 (CSRF PUT/PATCH/DELETE document requests), plus 7 moderate XSS/open-redirect. Fixed in 7.18.2+.
  - `vite build` produces static assets; no server runtime, no RSC, no single-fetch, no __manifest endpoint. Client-side items don't reach a user-controlled sink (routes hardcoded in `src/App.jsx`; only useNavigate/useParams/useSearchParams/useLocation across 17 files).
  - Spec: OWASP A06.
  - Fix: bump `react-router-dom` to `^7.18.2` (minor-only in v7, drop-in).

- **E4** — **`postcss@8.5.12` path-traversal via sourceMappingURL (build-only)** (carry-over)
  - `package-lock.json:2530`
  - GHSA-r28c-9q8g-f849 (HIGH, CVSS 7.5, path traversal in previous-source-map auto-loading) + GHSA-fxqj-rqcc-2cmp (MODERATE incomplete-fix follow-up). Fixed in 8.5.22+.
  - Only exploitable if PostCSS ingests a malicious CSS file with a crafted `sourceMappingURL` comment during build. All CSS in this repo is inline JS strings via `src/lib/tokens.js` — no untrusted CSS files enter the build. Dev/CI-only reachability.
  - Spec: OWASP A06.
  - Fix: `npm audit fix` bumps vite→postcss to 8.5.22+ transitively.

- **E5** — **`brace-expansion` + `js-yaml` DoS advisories (dev-only via ESLint)** (carry-over — brace-expansion advisory count up 1)
  - `package-lock.json` (transitives of ESLint)
  - `brace-expansion@1.1.14` — GHSA-mh99-v99m-4gvg (HIGH OOM) + GHSA-3jxr-9vmj-r5cp (HIGH DoS) + GHSA-rgw5-rvv9-x895 (HIGH bypass of the CVE-2026-14257 mitigation). `js-yaml@4.1.1` — GHSA-52cp-r559-cp3m + GHSA-5p4m-2wfm-xmqj + GHSA-h67p-54hq-rp68 (merge-key quadratic-CPU DoS).
  - Only triggered when ESLint expands attacker-controlled glob patterns or ingests adversarial YAML config. Not runtime-reachable.
  - Spec: OWASP A06.
  - Fix: `npm audit fix`.

- **E7** — **`nanoid@3.3.11` infinite-loop DoS on non-positive size (build-only via postcss)** (NEW)
  - `package-lock.json:2351` (marked `"dev": true`)
  - GHSA-28wg-ghj8-5hjv + GHSA-2v37-7h3g-55p8. Range <3.3.18; installed 3.3.11. Transitive of postcss (which is transitive of vite).
  - Build-time only. `size` argument to nanoid() inside postcss internals is fixed and never crosses an attacker-controlled boundary during static SPA builds on Vercel. Not runtime-reachable in prod. NEW since prior audit — nanoid was not previously in the E-list.
  - Spec: OWASP A06.
  - Fix: `npm audit fix` bundled with the vite/postcss bump above.

- **E2** — **`pdf-lib@1.17.1` unmaintained (~4 years since last publish)** (carry-over)
  - `package.json:16` (`"pdf-lib": "^1.17.1"`)
  - Last release Oct 2021. Used in `src/lib/payAppPdf.js:1`. No open CVEs but no security response channel; vendors old `tslib@1.14.1` + `pako@1.0.11`. Today's templates are trusted internal files, so parser bugs aren't currently exploitable.
  - Spec: OWASP A06.
  - Fix: track for migration to `@cantoo/pdf-lib` fork or `pdfme` if pay-app templating ever accepts user-uploaded template files.

- **E6** — **`dompurify@3.4.1` 10 advisories (optional transitive of jspdf; unused path)** (carry-over — advisory count up 5)
  - `package-lock.json:1424` (marked `optional: true`)
  - GHSA-hpcv-96wg-7vj8, GHSA-r47g-fvhr-h676, GHSA-rp9w-3fw7-7cwq, GHSA-c2j3-45gr-mqc4, GHSA-cmwh-pvxp-8882, GHSA-vxr8-fq34-vvx9, GHSA-gvmj-g25r-r7wr, GHSA-x4vx-rjvf-j5p4, GHSA-76mc-f452-cxcm, GHSA-55q2-fjhq-7xh7. All XSS bypasses or attribute-pollution. Fixed in 3.4.13+.
  - jspdf pulls dompurify only via its `.html()` / html2canvas path. Grep of `src/` for `\bdompurify\b|DOMPurify|\.html\(` returns no matches — DOMPurify is installed but not exercised.
  - Spec: OWASP A03, A06.
  - Fix: add explicit `overrides` in package.json forcing dompurify ≥3.4.13, or `npm audit fix`.

## Diff vs last audit

Prior audit: `audit-reports/2026-08-03.md` on branch `claude/audit-2026-08-03` (merged to main 496db99).

Posture then (2026-08-03): 0 critical · 20 high · 31 medium · 16 low.
Posture now (2026-08-17): 0 critical · 21 high · 32 medium · 24 low.

### Resolved since 2026-08-03

- **PublicInvoicePage DOM allow-list installed** (commit 2026-08-10, source-annotated in-file at `src/pages/PublicInvoicePage.jsx:52-72`). The direct `.select("*, proposal_wtc(*, work_types(name))")` at prior line 51 no longer ships cost basis via THIS page's fetch. NOTE: the underlying anon SELECT grant on `proposal_wtc` remains; cost basis is still reachable via a direct PostgREST call carrying the viewing_token — see **B-PublicInvoicePage-postgrest (HIGH)** for the persistent primitive at the DB layer.

No other prior finding was cleanly closed this cycle.

### Persistent (same root cause; new lines / dedupes reflected)

- **A1/A2/A1b/A2b** — Hardcoded URL + publishable-key fallbacks in `supabasePublic.js:16,20` and `Login.jsx:59,62`. Literal drift between publishable-key fallbacks unchanged.
- **A4** — `invite-user` logs `linkData` action_link at `:128`. Fourth audit cycle.
- **A5** — `stripe-webhook` logs recipient email at `:49`.
- **A6** — `invite-user` logs invitee + inviter emails/names at `:88`.
- **A7** — `send-proposal` logs recipient + rep emails at `:139`.
- **B-archive-grant-anon** — `sql/history_locker_phase1.sql:118` GRANT ALL to anon on archive schema.
- **B-archive-tenant-helper** — `sql/history_locker_phase1.sql:76` `active = true` filter still missing (search_path was added since prior audit).
- **B-legacy-search-path** — `sql/db_hardening_phase1.sql:12` seed body of `get_user_tenant_id()`.
- **B10 (legacy)** — `sql/rls_identity_tables.sql:94` `tenant_config_update` still permissive.
- **B11 (legacy)** — `sql/rls_identity_tables.sql:64` `team_members_update` still ships the pre-lockdown shape.
- **B13 (legacy)** — `sql/rls_child_tables.sql:261` `proposal_signatures_delete` still open.
- **B-PublicInvoicePage-wide-select → B-PublicInvoicePage-postgrest** — DOM layer closed 2026-08-10; DB layer (proposal_wtc anon SELECT grant leaking `burden_rate` / `markup_pct` / `pw_rate` / `materials` / `discount` via direct PostgREST with the viewing/signing token) remains. Third audit cycle for the underlying leak.
- **C-qb-auth** — `verify_jwt = false` at `supabase/config.toml:14`.
- **C-reset-password** — undeclared `origin` at `reset-password/index.ts:66`. Fourth cycle.
- **C-qb-record-payment-net** — retention excluded from netAmount at `qb-record-payment/index.ts:153`.
- **C9** — 21 raw error.message returns across functions; expanded list this cycle (added `follow-up-reminders:197`, `proposal-approved:87`, current lines).
- **C10** — send-invoice raw Stripe/Resend bodies at `:349,467,491`.
- **C16 (renamed C-qb-sync-invoice-svc / C-qb-record-payment-svc)** — service-role branch skips tenant check in both `qb-record-payment:103` and `qb-sync-invoice:134`.
- **C-invite-user-sender**, **C-invite-user-listusers**, **C-reset-password-listusers**, **C-deactivate-user-count**, **C-create-billing-session**, **C-qb-search-customers**, **C-qb-void-invoice**, **C-extract-sov**, **C-proposal-signed-rate**, **C-proposal-signed-error**, **C-stripe-webhook-reclaim**, **C-send-invoice-sender**, **C-check-orphan-error**, **C-deactivate-payment-link-cors**, **C18**, **C-invoice-existence-oracles** — all edge-fn carry-overs at their current lines.
- **D-WTCCalculator** — `WTCCalculator.jsx:2249,2257` (lines moved from 2114, 2173 as F44 refactored the handler).
- **D3** — `Settings.jsx:667` (line moved 654→667).
- **D4** — folded into **D-ProposalDetail-batch** at `ProposalDetail.jsx:535`.
- **D5** — `CallLogDetail.jsx:285` (line moved 235→285).
- **D6**, **D7** — Home.jsx / SalesDash.jsx client-only rep filters unchanged shape.
- **D8** — CallLog sessionStorage cache holds full customer PII at `:114` (severity: HIGH → MEDIUM this cycle — see below).
- **D9** — Login localStorage email persist unchanged.
- **D10** — Invoices destructive/money handlers, EXPANDED from 6 to 11 handlers as T&M ships (added `NewInvoiceModal.handleCreate`, `updateStatus`, `handleQBSync`, T&M `handlePullBack` branch at `:2295`, void-and-replace mint at `:2428`).
- **D-MobilizationsEditor**, **D-ArchiveProposalPanel**, **D-ProposalDetail-batch** — all folded into consolidated **D-ProposalDetail-batch** at `ProposalDetail.jsx:302,310,535,558,631,790,1611,1726`.
- **D-PayAppDetailModal-delete**, **D-NewPayAppModal-create**, **D-PayAppDetailModal-savelines**, **D-NewPayAppModal-id** — pay-app carry-overs. D-NewPayAppModal-id now three call-sites (added `Invoices.jsx:2422` for void-and-replace).
- **D-handleToggleDeposit** — folded into **D10** at `Invoices.jsx:2332` (line moved 2003→2332).
- **D-MultiGCWizard-server-verify** — `MultiGCWizard.jsx:188` unchanged.
- **D-materials-catalog-wtc** — `src/lib/materialsCatalog.js:48-87` unchanged (severity: LOW → HIGH this cycle — see below).
- **D-sc_calllog_cache-shape** — CallLog.jsx:44 unchanged.
- **F1** — `ProposalDetail.jsx:1260` (line moved 1271→1260).
- **F-PayAppDetailModal-cascade** — `PayAppDetailModal.jsx:233` unchanged. FOURTH audit cycle unfixed.
- **F-stray-fix-script** — `src/pages/fix_proposals_pdf.py` still tracked.
- **E1, E2, E3, E-react-router, E4, E5, E6** — dep advisories at same versions; E-react-router grew from 12 → 14 advisories, E6 dompurify grew from 5 → 10.

### Severity changes this cycle

- **C-qb-record-payment-net**: MEDIUM → **HIGH**. Retention drift is a money-reconciliation defect on a shipped path (over-books QB by retention amount on every retention invoice). Elevated per rubric ("revenue-affecting: failed invoice, broken QB sync").
- **D-materials-catalog-wtc**: LOW → **HIGH**. Write path via `WTCCalculator` is more open than prior audit reflected — every rep can insert/fork/update `materials_catalog` rows via the WTC materials tab, bypassing the Settings `canManage` gate. Multi-user cost-basis mutation surface.
- **B-legacy-search-path**: LOW → **MEDIUM**. `CREATE OR REPLACE` from the earliest seed can clobber the hardened body if seed order slips.
- **D-NewPayAppModal-create**: MEDIUM → **HIGH**. Matches D10-class role gate hole on a money-minting path.
- **D-PayAppDetailModal-savelines**: MEDIUM → **HIGH**. Matches D10-class role gate hole AND lacks the syncedLock guard that protects `Invoices.handleSaveEdit` — a QB-synced pay-app invoice can be silently changed here.
- **B10 (legacy)**: MEDIUM → **HIGH**. Re-elevated because `stripe_customer_id` / `stripe_subscription_id` / `default_burden_rate` on `tenant_config` are exposed to every tenant member if the seed is deployed.
- **C-qb-auth**: HIGH → **MEDIUM**. Handler enforces auth at the app layer via `requireAdminOrManager` — the platform `verify_jwt = false` is a defense-in-depth failure, not an exploitable primitive today.
- **C-qb-sync-invoice-svc / C-qb-record-payment-svc (formerly C16)**: HIGH → **LOW**. Service-role tenant-check skip is a documented design point; only exploitable via leaked service-role key.
- **D8**: HIGH → **MEDIUM**. Requires XSS-adjacent script or local-device access to exfil; PII in sessionStorage is defense-in-depth failure.
- **F-PayAppDetailModal-cascade**: HIGH → **MEDIUM**. Customer-visible UX mismatch + QB re-sync divergence; per rubric that maps to MEDIUM.
- **A1 / A2 / A1b / A2b**: MEDIUM → **LOW**. Stale-literal rotation drift; the key is publishable so not a bearer secret.

### Regressed

- **None.** No finding that was Resolved is now Open.
- Note: the F44 refactor (rate-card exclusion) shipped a **new** consistency gap at `Invoices.jsx:148-165` — `F-Invoices-auto-schedule-rate-card` (HIGH). Not a regression against the audit corpus, but a regression against the F44 invariant that six sibling sum-sites all correctly enforce.

### New this cycle

- **F-Invoices-auto-schedule-rate-card (HIGH)** — `src/pages/Invoices.jsx:148-165` — F44 rate-card exclusion missing on auto-create-billing_schedule fallback. Both Pass D and Pass F flagged independently.
- **C-send-proposal-allowlist-bypass (HIGH)** — `supabase/functions/send-proposal/index.ts:94` — allowlist skipped when proposal has no `customer_id`; any authenticated tenant member can send a signed proposal link to any recipient.
- **C-qb-record-payment-idempotency (HIGH)** — `supabase/functions/qb-record-payment/index.ts:141` — no `qb_payment_id` check before POSTing to QB; reprocessed Stripe events book duplicate QB Payments (root cause of prior LOW `C-stripe-webhook-reclaim`).
- **A8 (MEDIUM)** — `supabase/functions/send-pay-app/index.ts:99` — logs recipient email on every pay-app send. Same PII-in-logs class as A5/A6/A7.
- **A9 (MEDIUM)** — `supabase/functions/_shared/repNotify.ts:118` (+ :99, :115, :121) — new helper logs sales-rep email + full Resend response body on every successful send AND every failure. Dominant surface for internal-PII-in-logs across the notification cluster.
- **C-repNotify-signerName-injection (MEDIUM)** — `supabase/functions/_shared/repNotify.ts:150-165` — customer-supplied `signerName` (from anonymous public signing page) interpolated raw into internal sales-rep HTML email. HTML injection into every "Proposal Signed" notification.
- **C-send-proposal-error (MEDIUM)** — `supabase/functions/send-proposal/index.ts:187,225` — same C9/C10 pattern (raw Resend body + raw `error.message`) sibling of `send-invoice` C10.
- **C-stripe-webhook-sendEmail-from (MEDIUM)** — `supabase/functions/stripe-webhook/index.ts:47` — `sendEmail` forwards `from` to Resend without the VERIFIED_DOMAINS gate every other send-fn uses.
- **D-senderEmail-client-supplied (MEDIUM)** — `src/pages/Invoices.jsx:1003`, `Team.jsx:84`, `PayAppDetailModal.jsx:353`, `ProposalPDFModal.jsx:118` — client-side flag of the cross-cut `C-*-sender` server-side pattern; client should not send `senderEmail` at all.
- **C-send-fns-contacts-tenant (LOW)** — `send-proposal:96`, `send-pay-app:193` — customer_contacts reads scoped only by `customer_id`, not `tenant_id`. Belt-and-suspenders match with `send-invoice:201`.
- **D-attachment-hrefs-safety (LOW)** — attachment `<a href>` URLs rendered without `isSafeAttachmentHref` in ProposalDetail, PayAppDetailModal, BillingScheduleSection, CallLogDetail, HistoryLocker components. Consistency gap vs `Invoices.jsx:848`.
- **B-soft-delete-select (LOW)** — `sql/rls_core_tables.sql:138,185` — `proposals_select` / `invoices_select` do not filter `deleted_at IS NULL`; weakens the deleted_at contract.
- **B-config-wide-select (LOW)** — `src/lib/config.js:26` — `getTenantConfig` uses `select("*")` on the default client from pre-auth/public paths; safe today only because no anon SELECT policy exists on `tenant_config`.
- **E7 (MEDIUM)** — `package-lock.json:2351` — `nanoid@3.3.11` transitive via postcss (GHSA-28wg-ghj8-5hjv + GHSA-2v37-7h3g-55p8, DoS on non-positive size). Build-only.

### Not re-flagged this audit (carry-over triage from 2026-08-03)

These were on the 2026-08-03 list, live in prod-applied migrations moved to `command-suite-db` on 2026-06-29. Cannot verify from this repo without cross-repo access:
- B2 / B2b / B2c — anon signing-token SELECT policies missing `deleted_at IS NULL`.
- B3 — `invoices_public_view_token` missing `deleted_at IS NULL`.
- B4 — `get_public_tenant_config` no tenant scope.
- B5 — `get_rep_contact` cross-tenant enumeration.
- B7 — `qb_connection.tenant_id` nullable.
- B8 — customers viewing/signing tokens missing `deleted_at`.
- B12 — `mark_proposal_signed` accepts caller-supplied IP.
- B15 — `mark_proposal_signed` initial SELECT missing `deleted_at`.
- B16 — `clone_proposal_to_gcs` SECURITY DEFINER no admin gate (this cycle's `D-MultiGCWizard-server-verify` is the client-side view of the same primitive).
- B17 — `job-attachments` storage DELETE policy lacks tenant scope.
- B-invoice_recipients-update — role-flip via UPDATE.
- B-invoice_lines-token — parent `deleted_at` missing.

Parent tracker in `command-suite-db` should re-audit.

## Coverage
_pending_
