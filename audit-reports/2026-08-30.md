# Sales Command Security Audit — 2026-08-30

## Summary
_pending_

## Top 3 Urgent
_pending_

## Findings by Severity
### Critical

_pending — awaiting passes C-F_

### High

- **B-archive-grant-anon** — **`archive` schema `GRANT ALL … TO anon` with `ALTER DEFAULT PRIVILEGES`** (carry-over)
  - `sql/history_locker_phase1.sql:118-122`
  - Evidence: `GRANT USAGE ON SCHEMA archive TO anon, authenticated; GRANT ALL ON ALL TABLES IN SCHEMA archive TO anon, authenticated; ALTER DEFAULT PRIVILEGES IN SCHEMA archive GRANT ALL ON TABLES TO anon, authenticated;`
  - Anon is granted ALL DML on every current AND future archive table. Today RLS + `archive.get_user_tenant_id()` returning NULL for anon deny reads, but the grants remain the only backstop. Any future `archive.*` table added without `ENABLE ROW LEVEL SECURITY` (e.g. a helper/staging table) ships wide open — direct 2026-04-26 class exposure. There is no legitimate anon use case for the archive schema.
  - Spec: `CLAUDE_RLS.md`, OWASP-ASVS V8.2.1, OWASP A01.
  - Fix: `REVOKE ALL ON ALL TABLES/SEQUENCES IN SCHEMA archive FROM anon; ALTER DEFAULT PRIVILEGES IN SCHEMA archive REVOKE ALL ON TABLES/SEQUENCES FROM anon;` keep only authenticated grants.

- **B-proposal_wtc-anon-select** — **Anon signing/viewing tokens expose `proposal_wtc` cost basis via direct PostgREST query** (RE-SCOPED from B-PublicInvoicePage-wide-select)
  - `src/pages/PublicInvoicePage.jsx:60-67` (in-file acknowledgement); policy body lives in `command-suite-db` migration `20260427180000_add_token_gated_policies.sql`
  - Evidence: `// BUT THIS ALLOW-LIST IS NOT THE SECURITY BOUNDARY — corrected 2026-08-10 by security review. proposal_wtc carries a TABLE-level anon SELECT grant, so a link holder can query burden_rate / markup_pct / pw_rate / materials / discount straight from PostgREST regardless of what this page selects.`
  - The prior wide-select at `:51` was resolved (now explicit column allow-list at `:73-79`), which keeps cost basis out of the DOM — but the underlying anon SELECT policy on `proposal_wtc` (via `x-signing-token` / `x-viewing-token` match) still returns every column. Anyone the signing or viewing link is shared with (customer, forwarded email recipient) can hit `/rest/v1/proposal_wtc?select=*` with the publishable anon key + the token header and read `burden_rate`, `ot_burden_rate`, `markup_pct`, `pw_rate`, `materials`, `discount`. Explicitly acknowledged as unfixed (backlog S13). Fourth audit cycle — the DOM mitigation ships but the DB fix is still in command-suite-db.
  - Spec: `CLAUDE_RLS.md`, OWASP-ASVS V8.3.4, OWASP A01.
  - Fix: in `command-suite-db`, replace `proposal_wtc`'s anon SELECT policy with a column-restricted view (or `REVOKE SELECT ON burden_rate, ot_burden_rate, pw_rate, pw_ot_rate, markup_pct, materials, discount FROM anon`); the public pages already read `locked_line_total` snapshots and don't need cost inputs.

- **F1** — **`ProposalDetail` Summary total uses stale `parseFloat(p.total)` fallback** (carry-over, line moved 1271→1454)
  - `src/components/ProposalDetail.jsx:1454`
  - Evidence: `const sov = sovContractSum != null && sovContractSum > 0 ? sovContractSum : null; const fallback = parseFloat(p.total) || 0; const value = sov != null ? sov : fallback;`
  - Data Integrity Rule 2 forbids reading `proposals.total` for display — must compute via `calcWtcPrice()` on live WTC data. `proposals.total` is only written on `handleLock`/`handleSave` and goes stale between edits. The breakdown block at `:1465-1468` a few lines below already computes `calcWtcBreakdown` for the same WTCs; the Total row and the breakdown row disagree whenever they've drifted.
  - Spec: `CLAUDE.md` Data Integrity Rule 2.
  - Fix: gate the fallback — when `!p.is_archive_proposal && wtcs.length > 0`, compute value from `(wtcs || []).reduce((s, w) => s + calcWtcPrice(w, undefined, usesExactPricing(p)), 0)` (matches the breakdown block at `:1468`).

- **F-followUp-dormantCustomers** — **`followUp.dormantCustomers` computes `soldValue` from stale `proposals.total`** (NEW — Rule 2 violation in shared lib)
  - `src/lib/followUp.js:369`
  - Evidence: `if (eff) { sold.add(eff); soldValue.set(eff, (soldValue.get(eff) || 0) + (p.total || 0)); }`
  - `soldValue` is rendered as the `value` on Zone-3 Outbound cards (`OutboundCard.jsx:30` via `fmt$(item.value)`) and the "Largest quiet bid" in `HuntBox`. The snapshot fetches `proposal_wtc` for every proposal (line 244), and the file already defines the canonical `bidValue(p)` at `:104` with an explicit comment: `// NEVER proposals.total (stale, Data Rule #2)`. This reducer ignores its own guidance. Every dormant-customer card can show a wrong dollar tag whenever `proposals.total` has drifted from live WTC.
  - Spec: `CLAUDE.md` Data Integrity Rule 2.
  - Fix: `soldValue.set(eff, (soldValue.get(eff) || 0) + bidValue(p));` — use the canonical helper defined a few lines above.

- **F-followUp-footerStats** — **`followUp.footerStats` monthly billings summed from stale `proposals.total`** (NEW — Rule 2 violation in shared lib)
  - `src/lib/followUp.js:457`
  - Evidence: `const monthBill = snap.proposals.filter(p => p.status === "Sold" && endDate(p)?.startsWith(month)).reduce((s, p) => s + (p.total || 0), 0); const billingsPct = monthlyGoal ? Math.round((monthBill / monthlyGoal) * 100) : 0;`
  - The "billings % of monthly goal" shown in the footer thermometer is derived from `p.total`. `proposal_wtc` is already embedded in the same fetch (line 244), so `bidValue(p)` is available. Same fail-mode as F1 and F-followUp-dormantCustomers — drifts silently whenever `proposals.total` is not perfectly synced.
  - Spec: `CLAUDE.md` Data Integrity Rule 2.
  - Fix: replace `(p.total || 0)` with `bidValue(p)`.

- **F-PayAppDetailModal-cascade** — **`handleSaveLines` updates `invoices.amount` but never propagates to `invoice_lines.amount`** (carry-over — FOURTH cycle)
  - `src/components/PayAppDetailModal.jsx:233`
  - Evidence (lines 238-253): updates `billing_schedule_pay_app_lines` rows, updates `billing_schedule_pay_apps` header (`this_app_amount`, `retainage_withheld`, `current_payment_due`), updates `invoices` (`amount`, `retention_amount`) — but does NOT update `invoice_lines.amount` for the same invoice.
  - `NewPayAppModal.jsx:198-211` inserts one `invoice_lines` row per SOV line at pay-app creation keyed by billing_schedule_line_id; those rows are read by `Invoices.jsx` for line-item rendering and by `PublicInvoicePage.jsx`. After any pay-app line edit, `invoices.amount` reflects the new gross but `invoice_lines.amount` still holds pre-edit numbers — customer's public page and PDF show stale per-line dollars while the header total is fresh. Canonical Rule 6 trap. Fourth cycle unfixed.
  - Spec: `CLAUDE.md` Data Integrity Rule 6.
  - Fix: after updating `billing_schedule_pay_app_lines`, look up matching `invoice_lines` by `(billing_schedule_line_id, invoice_id)` and UPDATE each row's `amount` and `billing_pct` to match.

- **D-WTCCalculator** — **`handleSave` + `handleLock` money-writes have NO role gate** (carry-over, lines moved 2114→2269, 2173→2360)
  - `src/pages/WTCCalculator.jsx:2269` (handleSave); `:2360` (handleLock)
  - Both handlers recompute and write `proposals.total` (Rule 4). Component takes no `teamMember` prop, no role check. `handleSave` writes `proposal_wtc` (labor, materials, markup, discount); `handleLock` snapshots `locked_line_total` and syncs `proposals.total`. Any authenticated user who can open a WTC can rewrite money on any proposal in the tenant.
  - Spec: `CLAUDE_RLS.md`, `CLAUDE.md` Data Integrity Rule 4, OWASP A01.
  - Fix: pass `teamMember` into WTCCalculator; gate `handleSave`/`handleLock` on `["Admin","Manager"].includes(role) OR sales_name` match; backstop with RLS on `proposal_wtc` UPDATE/INSERT and `proposals.total` UPDATE.

- **D-MobilizationsEditor** — **`persist` writes `proposals.mobilizations` with NO role gate; propagates cross-driver to Schedule Command** (carry-over, extracted from ProposalDetail.jsx into own file)
  - `src/components/MobilizationsEditor.jsx:80` (persist), also `applyStandardJob()` + `deleteMob()` write `proposal_wtc.field_sow`
  - Evidence: `const { error: e } = await supabase.from("proposals").update({ mobilizations: next }).eq("id", proposalId);`
  - Component was extracted from `ProposalDetail.jsx:1614` into its own file. Still takes no `teamMember` prop and does no role check. `proposals.mobilizations` flows into Schedule Command via `commitSendToSchedule` — writing here creates a cross-driver contamination vector.
  - Spec: `CLAUDE_RLS.md`, `docs/plans/command_suite_shared_data_contract.md`.
  - Fix: pass `teamMember`; gate `persist`/`applyStandardJob`/`deleteMob` on `canDelete`-style predicate. RLS on `proposals.mobilizations` UPDATE.

- **D-ProposalDetail-batch** — **`setJobWalkType`, `deleteWtc`, `handlePullBack`, `commitSendToSchedule` rollback — destructive + cross-driver + money-write handlers, all client-gated only** (carry-over, expanded)
  - `src/components/ProposalDetail.jsx:302` (setJobWalkType), `:310` (deleteWtc), `:611-628` (handlePullBack), `:797` (commitSendToSchedule rollback)
  - None of these handlers check `teamMember.role` before writing. `canDelete` (:565) only wraps the Delete Proposal button (:1081). `handlePullBack` hard-deletes `proposal_signatures` (destroys legal evidence, pairs with B13). `commitSendToSchedule` inserts `jobs` + `job_wtcs` (cross-driver write into Schedule Command).
  - Spec: `CLAUDE_RLS.md`, `CLAUDE.md` Data Integrity Rule 4, OWASP A01, A08.
  - Fix: role/ownership check at each handler entry; RLS on `proposal_wtc` (INSERT/UPDATE/DELETE), `proposals` (UPDATE), `jobs` (INSERT), `job_wtcs` (INSERT), `proposal_signatures` (DELETE).

- **D-ArchiveProposalPanel** — **`handleSave` rewrites `proposals.total` and `historical_billed_amount` with NO role gate** (carry-over, line moved 1726→1799)
  - `src/components/ProposalDetail.jsx:1799`
  - Evidence: `async function handleSave() { … const { error } = await supabase.from("proposals").update({ total: n, historical_billed_amount: h }).eq("id", p.id); }`
  - Archive-proposal money path — user types a Sold Amount and writes it directly to `proposals.total`. Component takes no `teamMember`. Feeds the linked-invoice `handleSaveEdit` recompute path (the exact trap fixed in commit 14000c5). Durable upstream money-motion primitive.
  - Spec: `CLAUDE.md` Data Integrity Rule 6, `CLAUDE_RLS.md`.
  - Fix: pass `teamMember` to ArchiveProposalPanel; gate the Edit button (:1851) + `handleSave` on canManage; backstop with RLS on `proposals.total` UPDATE.

- **D4** — **`ProposalDetail.handleDelete` is client-gated only** (carry-over, line moved 533→568)
  - `src/components/ProposalDetail.jsx:568`
  - Evidence: `const canDelete = teamMember && (["Admin","Manager"].includes(teamMember.role) || teamMember.name === p.call_log?.sales_name); async function handleDelete() { … await supabase.from("proposals").update({ deleted_at: … }).eq("id", p.id); … }`
  - `canDelete` checked only on the button render. Handler itself has no re-check — a low-priv user can call it via console. RLS is the sole real gate. Handler also renumbers sibling proposals (`:582-586`) with no per-write gate.
  - Spec: `CLAUDE_RLS.md`, OWASP A01.
  - Fix: re-check `canDelete` inside `handleDelete` (early return); enforce in RLS on `proposals` UPDATE: `(is_admin_or_manager() OR sales_name = current_user_name)`.

- **D5** — **`CallLogDetail.handleDelete` is client-gated only** (carry-over, line moved 235→285)
  - `src/components/CallLogDetail.jsx:285`
  - Evidence: `const canDelete = teamMember && (teamMember.role === "Admin" || teamMember.name === job.sales_name); async function handleDelete() { … }`
  - `call_log` + `job_work_types` have authenticated DELETE policies; any tenant member can delete any job via direct supabase call. Handler hard-deletes invoices, job_work_types, and call_log rows (`:312-322`).
  - Spec: `CLAUDE_RLS.md`, OWASP A01.
  - Fix: re-check `canDelete` inside `handleDelete`; restrict `call_log` DELETE in RLS to `(is_admin_or_manager() OR sales_name = current_user_name)`.

- **D8** — **`CallLog` cache writes customer PII into sessionStorage** (carry-over, line moved 114→142)
  - `src/pages/CallLog.jsx:142`
  - Evidence: `sessionStorage.setItem(CACHE_KEY, JSON.stringify({ rows: allLog, team: tm || [], customers: allCx, workTypes: wt || [] }));`
  - `allCx = fetchAll("customers", "*")` — full customer directory (`contact_email`, `contact_phone`, `billing_email`, `billing_phone`, `business_address`) plus every `call_log` row is written to sessionStorage. Any XSS sink exfiltrates the whole customer book in one JSON read.
  - Spec: OWASP-ASVS V8.3.1, OWASP A02, A04, GDPR Art. 32.
  - Fix: cache only `{id, display_job_number, stage, customer_name, sales_name, bid_due, follow_up}`; refetch full detail on click; strip `customers(*)`.

- **D10** — **`Invoices` destructive/money handlers rendered without a role gate on any of them** (carry-over, expanded)
  - `src/pages/Invoices.jsx:2082` (handleDelete), `:2128` (handleSaveEdit), `:2416` (handleToggleDeposit), `:1930` (handleQBSync), `:1909` (updateStatus), `:1714` (saveRecipient), `:1727` (deleteRecipient), `:1733` (saveToCustomerFile)
  - Component receives `teamMember` but never checks role. Every destructive/money-write handler on InvoiceDetail is reachable by every Sales Rep. A rep can void, delete, edit line amounts, mint retention-release invoices, flip the deposit toggle, or push to QuickBooks.
  - Spec: `CLAUDE.md` Data Integrity Rule 6, OWASP-ASVS V4.1.3.
  - Fix: define `const canManage = teamMember && ["Admin","Manager"].includes(teamMember.role);`; gate every InvoiceDetail button + handler entry; add matching RLS UPDATE/DELETE policies on `invoices`.

- **D-PayAppDetailModal-delete** — **`handleDeletePayApp` no role gate** (carry-over)
  - `src/components/PayAppDetailModal.jsx:264`
  - Soft-deletes linked invoice (:275) and hard-deletes pay-app row (:283); unlocks the schedule (:294). Only guard is `invoice?.qb_invoice_id` short-circuit at `:265`. No `teamMember` prop.
  - Spec: `CLAUDE.md` Data Integrity Rule 6, OWASP-ASVS V4.1.3.
  - Fix: accept `teamMember`; role gate before writes; RLS on `billing_schedule_pay_apps` DELETE + `invoices` UPDATE(deleted_at) restricted to admin/manager.

- **B11 (legacy-seed)** — **`team_members_update` policy is UNGATED — role-escalation + account-takeover primitive within tenant** (carry-over)
  - `sql/rls_identity_tables.sql:64`
  - Evidence: `CREATE POLICY "team_members_update" ON public.team_members FOR UPDATE TO authenticated USING (tenant_id = public.get_user_tenant_id()) WITH CHECK (tenant_id = public.get_user_tenant_id());`
  - Any authenticated user in a tenant can UPDATE every row of `team_members`. Because `get_user_tenant_id()` reads `team_members WHERE auth_id = auth.uid() AND active = true`, an ordinary rep can rewrite their own `role` to Admin/Manager, flip a colleague's `active` flag, or set `auth_id` on a colleague's row to their own `auth.uid()` — full role-escalation + takeover. Prod state claimed fixed via `command-suite-db` migration `20260722120000_team_members_role_lockdown.sql` (§8 smoke-verified 2026-07-22, unverifiable from this repo). Legacy seed remains a landing hazard on a fresh deploy.
  - Spec: OWASP-ASVS V4.1.3, OWASP A01.
  - Fix: split into a self-update policy (`auth_id = auth.uid()` with `WITH CHECK` on role/active/tenant_id/auth_id unchanged) + admin-only policy gated on `is_admin(auth.uid())`; or delete this seed file with a comment pointing at the command-suite-db migration.

- **A1** — **Hardcoded prod Supabase URL fallback in public client** (carry-over)
  - `src/lib/supabasePublic.js:16`
  - Evidence: `const SUPABASE_URL = import.meta.env.VITE_SUPABASE_URL || "https://pbgvgjjuhnpsumnowuym.supabase.co";`
  - Any staging/preview build shipped without `VITE_SUPABASE_URL` silently hits production. Every public signing/viewing page (which uses `createPublicClient`) inherits the fallback, defeating env-driven environment separation and hiding misconfigurations.
  - Spec: OWASP-ASVS V14.2.1, OWASP A05.
  - Fix: drop the `||` fallback; throw at module load if `VITE_SUPABASE_URL` is unset, matching `src/lib/supabase.js:8`.

- **A2** — **Hardcoded publishable anon key fallback in public client** (carry-over)
  - `src/lib/supabasePublic.js:20`
  - Evidence: `const SUPABASE_KEY = import.meta.env.VITE_SUPABASE_ANON_KEY || "sb_publishable_LL6wYC7kJDPQfGYeKAaLaWKoK";`
  - A prod anon key is baked into source so every commit/checkout redistributes it; rotation becomes a code push. A misbuilt bundle still authenticates against prod. Carry-over.
  - Spec: OWASP-ASVS V2.10.4, OWASP A05.
  - Fix: remove the string fallback; require `VITE_SUPABASE_ANON_KEY` via env and fail loudly if missing.

- **A1b** — **Hardcoded prod Supabase URL fallback in Login reset flow** (carry-over)
  - `src/pages/Login.jsx:59`
  - Evidence: `` `${import.meta.env.VITE_SUPABASE_URL || "https://pbgvgjjuhnpsumnowuym.supabase.co"}/functions/v1/reset-password` ``
  - Second hardcoded prod URL, in the password-reset fetch path — same env-separation risk as A1. A staging/misbuilt Login page POSTs to prod's reset-password endpoint. Carry-over.
  - Spec: OWASP-ASVS V14.2.1, OWASP A05.
  - Fix: route through the central client / a validated env constant; drop the string fallback.

- **A2b** — **Hardcoded publishable anon key fallback in Login reset flow (DIFFERENT literal from A2)** (carry-over)
  - `src/pages/Login.jsx:62`
  - Evidence: `apikey: import.meta.env.VITE_SUPABASE_ANON_KEY || "sb_publishable_v7XktVvkAlX7y5f6xoFjng_AaLaWKoK"`
  - A different literal from A2 — two distinct hardcoded publishable keys live in the tree, so rotating one still leaves the other. Carry-over.
  - Spec: OWASP-ASVS V2.10.4, OWASP A05.
  - Fix: remove the string fallback; use the same env-driven value the rest of the app reads.

- **A4** — **Magic-link auth token logged in `invite-user`** (carry-over — FOURTH cycle)
  - `supabase/functions/invite-user/index.ts:128`
  - Evidence: `console.log("generateLink result:", { linkData, linkErr: linkErr?.message });`
  - `linkData.properties.action_link` is a single-use magic-link credential that logs the recipient straight in. Anyone with edge-function log access (Supabase dashboard viewer, log-shipper sink, third-party observability integration) can hijack the invitee's session — worse than PII, this is a live credential in the logs. Fourth cycle unfixed.
  - Spec: OWASP-ASVS V7.1.1, OWASP A09, A07.
  - Fix: `console.log("generateLink result:", { hasLink: !!linkData?.properties?.action_link, linkErr: linkErr?.message });`

- **C-qb-auth** — **`qb-auth` has `verify_jwt = false` at the Supabase platform layer** (carry-over, line moved 14→44)
  - `supabase/config.toml:44`
  - Evidence: `[functions.qb-auth]\nenabled = true\nverify_jwt = false`
  - Platform JWT gate off; handler calls `authenticateCaller`/`requireAdminOrManager` so unauth callers are rejected at the app layer, but every other admin function relies on the platform gate as defense in depth. A future handler-auth refactor becomes a public service-role OAuth endpoint that writes `qb_connection` across tenants. Carry-over.
  - Spec: OWASP-ASVS V1.4.1, OWASP A01, Supabase production checklist.
  - Fix: set `verify_jwt = true`; keep in-handler role gate as additional defense.

- **C-reset-password** — **`reset-password` references undeclared `origin` — branding always defaults to Sales Command; ReferenceError if runtime tightens** (carry-over — FOURTH cycle)
  - `supabase/functions/reset-password/index.ts:66`
  - Evidence: `const isSchedule = origin.includes("schmybiz") || origin.includes("schedulecommand");`
  - `origin` is never declared, imported, or read from `req.headers`. In Deno Edge `globalThis.origin` resolves to `""` unless `--location` is set, so every reset link is branded Sales Command and points to `salescommand.app`. Schedule Command users always get the wrong branding. If Deno ever tightens the resolution to throw ReferenceError, the outer catch (line 145) leaks the raw error via `error.message`. Fourth cycle unfixed.
  - Spec: OWASP-ASVS V5.1.1, CWE-476.
  - Fix: `const origin = req.headers.get("origin") || "";` at the top of the handler; also collapse `error.message` returns to `"Internal error"` with a correlation ID.

- **C-leads-intake-rate-limit** — **`leads-intake` public endpoint has no rate limit and no body-size cap** (NEW)
  - `supabase/functions/leads-intake/index.ts:47` (handler entry), `:69` (unbounded `req.json()`)
  - Evidence: `serve(async (req) => { … payload = await req.json();`
  - `leads-intake` is deployed `--no-verify-jwt` and is publicly reachable, gated by a shared secret. No rate limit — a bad-secret bruteforce (or partner infinite-retry loop with a stale secret) can be sustained at Deno's throughput. `req.json()` consumes the full body before validation; there is no per-IP counter, no burst window, no `Content-Length` check. A caller with the secret (or bruteforcing it) can push arbitrarily large bodies and burn worker memory + CPU per request; even authenticated, a malicious partner or leaked-secret holder can flood the leads table.
  - Spec: OWASP-ASVS V13.1.4, V13.4.1, OWASP A05.
  - Fix: add per-IP + per-secret sliding-window rate limit (mirror `reset-password:10-13`); pre-parse `Content-Length` reject >~25KB before `req.json()`; hard byte-limit on stream reads if Content-Length is missing.

### Medium

- **D-handleToggleDeposit** — **`handleToggleDeposit` can silently zero retention with no role gate** (carry-over, line moved 2003→2416)
  - `src/pages/Invoices.jsx:2416`
  - Evidence: `const zeroRetention = turningOn && !linkedPayApp && !syncedLock && ((parseFloat(inv.retention_pct) || 0) > 0 || …); const updates = zeroRetention ? { is_deposit: turningOn, retention_pct: 0, retention_amount: 0 } : { is_deposit: turningOn };`
  - Toggle the deposit checkbox and retention silently zeros on non-pay-app invoices. No role check. Combined with the missing gate on `handleSaveEdit`/`handleBillRetention` (D10), the deposit toggle is the most-exposed money-motion surface on the per-invoice deposit flow.
  - Spec: `CLAUDE.md` Data Integrity Rule 6, Rule 7.
  - Fix: role gate at handler head; disable the Checkbox for non-admins; enforce in RLS.

- **D-NewPayAppModal-create** — **`NewPayAppModal.handleCreate` mints invoices with no role gate** (carry-over, line moved 123→125)
  - `src/components/NewPayAppModal.jsx:125`
  - Inserts `billing_schedule_pay_apps` (:135), `billing_schedule_pay_app_lines` (:165), and a linked invoice with amount, retention_amount (:180). No `teamMember`, no role symbol.
  - Spec: `CLAUDE.md` Data Integrity Rule 6, OWASP-ASVS V4.1.3.
  - Fix: gate handler + Create button on canManage; RLS INSERT on `billing_schedule_pay_apps` + `invoices` restricted to admin/manager.

- **D-PayAppDetailModal-savelines** — **`handleSaveLines` rewrites pay-app + invoice money with no role gate AND no syncedLock check** (carry-over)
  - `src/components/PayAppDetailModal.jsx:233`
  - Rewrites `billed_pct_this_app`, `billed_amount_this_app`, `invoices.amount`, `invoices.retention_amount`. No role gate. Not protected by `syncedLock` the way `Invoices.handleSaveEdit` is — a QB-synced invoice's amount can still move here. (Cascade bug to `invoice_lines.amount` documented separately as F-PayAppDetailModal-cascade.)
  - Spec: `CLAUDE.md` Data Integrity Rule 6, OWASP-ASVS V4.1.3.
  - Fix: role gate + syncedLock check at handler head; restrict UPDATE on these columns in RLS to admin/manager.

- **D9** — **Email persisted to localStorage on Login regardless of Remember checkbox** (carry-over)
  - `src/pages/Login.jsx:38`
  - Evidence: `localStorage.setItem("sc_remember", remember ? "true" : "false"); localStorage.setItem("sc_last_email", email.trim());`
  - `sc_last_email` written unconditionally; the initial state reader (`:11`) reuses it. On shared/kiosk devices the previous user's email appears.
  - Spec: OWASP-ASVS V3.4.1, V8.3.1, OWASP A02.
  - Fix: `if (remember) localStorage.setItem(…); else localStorage.removeItem("sc_last_email");`

- **D-MultiGCWizard-server-verify** — **MultiGC clone RPC has no client-side role gate — client-server contract needs server-side check verified in `command-suite-db`** (carry-over)
  - `src/components/MultiGCWizard.jsx:184`
  - Evidence: `const { data, error: rpcErr } = await supabase.rpc("clone_proposal_to_gcs", { p_source_proposal_id, p_targets });`
  - No client-side role gate; targets and per-target `markup_override_pct` are attacker-controlled. Error surface at `:157` shows the RPC returns FORBIDDEN so server-side auth is in place — but the exact predicate lives in command-suite-db and cannot be verified from this repo. Requires: (1) `role IN ('Admin','Manager')` OR source proposal's sales_name match, (2) target `customer_ids` belong to caller's tenant, (3) `markup_override_pct` floor.
  - Spec: `CLAUDE_RLS.md`, OWASP A01.
  - Fix: verify `clone_proposal_to_gcs` in command-suite-db; add client-side `canManage` gate on the modal to match the server contract.

- **B10 (legacy-seed)** — **`tenant_config_update` lets any authenticated tenant user rewrite billing goals, Stripe IDs, defaults** (carry-over)
  - `sql/rls_identity_tables.sql:94`
  - Evidence: `CREATE POLICY "tenant_config_update" ON public.tenant_config FOR UPDATE TO authenticated USING (id = public.get_user_tenant_id()) WITH CHECK (id = public.get_user_tenant_id());`
  - Any authenticated tenant user (rep or otherwise) can UPDATE every column of their tenant_config — `company_name`, `monthly/yearly_billing_goal`, `default_burden_rate`, `default_tax_rate`, `stripe_customer_id`, `stripe_subscription_id`, `subscription_status`, `apps[]`, `logo_url`, `license_number`. Silent tampering with billing defaults + Stripe linkage; no admin gate at row level. Frontend only hides the UI; PostgREST accepts it. Prod state unverifiable (migrations moved to command-suite-db). Pairs with D3.
  - Spec: OWASP-ASVS V4.1.3, OWASP A01.
  - Fix: same lockdown pattern as B11 — add `USING is_admin(auth.uid())` or route sensitive writes through a SECURITY DEFINER RPC.

- **B13 (legacy-seed)** — **`proposal_signatures_delete` allows any tenant user to destroy signed proposal evidence** (carry-over)
  - `sql/rls_child_tables.sql:261`
  - Evidence: `CREATE POLICY "proposal_signatures_delete" ON public.proposal_signatures FOR DELETE TO authenticated USING (tenant_id = public.get_user_tenant_id());`
  - Any authenticated tenant user can DELETE the signature record for any proposal in their tenant. `proposal_signatures` is the legal-binding electronic-signature audit trail (`signer_name`, `signed_at`, `ip_address`, `pdf_url`); deleting is repudiation-friendly + irreversible. Pull-back today already exercises this DELETE path via ProposalDetail.handlePullBack (D-ProposalDetail-batch).
  - Spec: OWASP-ASVS V7.3.1, OWASP A08, A09.
  - Fix: drop the DELETE policy (append-only signatures) or restrict `USING is_admin(auth.uid())`; if pull-back needs to void, add a `voided_at` column instead of DELETE.

- **D-Team-route-ungated** — **`/team` route + Add/Edit/Deactivate/Invite reachable to every authenticated user** (NEW — HIGH)
  - `src/App.jsx` (Team route unguarded); `src/pages/Team.jsx:432` (+ Add Member button always visible)
  - Evidence: `<SectionHeader title="Our Team" action={<Btn sz="sm" onClick={() => setModal("add")}>+ Add Member</Btn>} />`
  - `/team` is not role-gated (compare `/import` at `App.jsx:230` which requires Admin). Any authenticated Sales Rep can open the Add / Edit modal and trigger `team_members` insert/update/delete (Team.jsx `:58, :64, :92`) plus `invite-user` edge fn (`:83`). No client-side role check anywhere. Chained with B11 (`team_members_update` policy legacy-seed permissive, prod claimed fixed via `command-suite-db` migration), a rep can create themselves an Admin row unless the prod migration truly landed and stuck. Independent of B11, the invite-user path lets any rep invite arbitrary email addresses to the tenant.
  - Spec: `CLAUDE_RLS.md` (defense-in-depth), OWASP-ASVS V4.1.3, OWASP A01.
  - Fix: route-guard `/team` like `/settings` (`userRole === 'Admin'/'Manager'`); gate the Add/Edit/Deactivate/Invite buttons + save handlers on `teamMember.role` in `Team.jsx`.

- **D-Archive-admin-tabs-css** — **History Locker admin tabs are CSS-hide only; ArchiveBatchManager/ImportWizard reachable via bypass** (NEW — HIGH)
  - `src/pages/Archive.jsx:35` (tab visibility); `src/components/HistoryLocker/ArchiveBatchManager.jsx:27` (hard-delete import_batches); `src/components/HistoryLocker/ArchiveImportWizard.jsx` (bulk insert legacy_records)
  - Evidence: `{userRole === "Admin" && !viewingDetail && ( <div … Admin tabs … )}`
  - Tab visibility is a CSS hide only. `tab` state is component-local — a `setState` via console renders `ArchiveImportWizard` (bulk INSERT into `legacy_records`/`import_batches`) and `ArchiveBatchManager` (hard-DELETE `import_batches` at `ArchiveBatchManager.jsx:27`, cascades `legacy_records` for whole-tenant batches). Neither child re-checks role. Only RLS backstops (and archive schema RLS scopes by tenant, not by admin role).
  - Spec: `CLAUDE_RLS.md` (defense-in-depth), OWASP A01.
  - Fix: route-guard the admin tabs at `Archive.jsx`; require `userRole` prop inside `ArchiveImportWizard`/`ArchiveBatchManager` (early return + guard on write handlers). Add role gate to `archive.*` RLS policies.

- **D-Import-route-client-only** — **`/import` wizard bulk-inserts customers/call_log/proposals/invoices behind client-only role check** (NEW — HIGH)
  - `src/App.jsx:230` (client-side route gate); `src/pages/Import/importApi.js:248` (bulk `importRows`)
  - Evidence: `displayRole === "Admin" ? <><style>{GLOBAL_CSS}</style><Import /></> : <div>Not authorized</div>`
  - The `/import` route gate is client-side. Import wizard's `importRows` does batched INSERT/UPDATE across `customers`/`call_log`/`proposals`/`invoices` and `job_work_types` with no per-row role check. A bypass of the client route gate (or DevTools override of `displayRole`) lets a lower-priv user perform bulk cross-driver writes. RLS is the sole backstop, and none of `call_log`/`proposals`/`invoices` INSERT policies check role.
  - Spec: `CLAUDE_RLS.md` (defense-in-depth), OWASP A01.
  - Fix: server-side enforce admin-only writes via RLS on `customers`/`call_log`/`proposals`/`invoices` INSERT (or a role-gated RPC); keep client route gate as UX only.

- **D-ImportToLiveWizard-delete** — **`ImportToLiveWizard` hard-deletes and rewrites `call_log`/`customers`/`job_work_types` with no role check** (NEW — HIGH)
  - `src/components/HistoryLocker/ImportToLiveWizard.jsx:402`
  - Evidence: `await supabase.from("job_work_types").delete().eq("call_log_id", legacyTakenBy.id); ... await supabase.from("call_log").delete().eq("id", legacyTakenBy.id);`
  - Wizard deletes `call_log` + `job_work_types` (:402-404), inserts `customers`, `customer_contacts`, and `call_log` (`:413, :436, :464, :537`). No `teamMember`/`userRole` prop. Even though reachable only via the Admin-hidden Archive tab, both gates are client-only (see D-Archive-admin-tabs-css). RLS is the sole real check on `call_log` DELETE (and `call_log` policies allow authenticated tenant DELETE).
  - Spec: `CLAUDE_RLS.md` (defense-in-depth), OWASP A01.
  - Fix: accept `userRole` from `ArchiveRecordDetail` parent and refuse writes unless Admin; move destructive logic behind a server-side RPC.

- **D-alerts-rep-scoping** — **`AlertsProvider` rep-scoping is client-only over a whole-tenant snapshot** (NEW — HIGH; supersedes D6/D7)
  - `src/lib/alerts.jsx:50`
  - Evidence: `const isRep = !["Admin", "Manager"].includes(displayRole);`
  - `loadSnapshot` (`src/lib/followUp.js:227`) fetches every `call_log`, `proposal`, `customer`, and `outreach_log` row the tenant RLS allows — the whole tenant pipeline. `AlertsProvider` then filters by rep name client-side using a client-derived `displayRole`. A Sales Rep who overrides `isRep` in memory (React DevTools, `!isRep` in the reducer) sees every rep's pipeline + $ figures. Backstop: tenant-wide RLS; but this leaks every rep's funnel to any team member. Prior D6 (Home isRep) + D7 (SalesDash selectedRep) collapsed into this shared source after the alerts/Home refactor.
  - Spec: `CLAUDE_RLS.md` (defense-in-depth), OWASP A01.
  - Fix: move per-rep visibility into RLS (rep can only read own `call_log`/`proposals` rows) or scope `loadSnapshot` queries with `.eq('sales_name', repName)` server-side when role is Sales Rep — do not rely on client-side filter.

- **F-CallLogDetail-jobtotals** — **`CallLogDetail` Job Totals falls back to `parseFloat(p.total)` for non-pay-app jobs** (NEW — Rule 2 violation)
  - `src/components/CallLogDetail.jsx:898`
  - Evidence: `const sov = contractSumByProposalId[p.id] || 0; return s + (sov > 0 ? sov : parseFloat(p.total) || 0);`
  - Job Totals renders `sold` from `p.total` whenever there is no billing_schedule row. The `linkedProposals` query at line 221 does NOT embed `proposal_wtc`, so this cannot compute live — must either (a) embed `proposal_wtc(*, work_types(name))` and use `calcProposalTotal`, or (b) accept the stale-total risk. Same Rule 2 mechanism as F1 and the followUp findings; displayed to users on the Job detail "Job Totals" box.
  - Spec: `CLAUDE.md` Data Integrity Rule 2.
  - Fix: add `proposal_wtc(*, work_types(name))` to the `linkedProposals` select and compute `sold` via `calcProposalTotal(p.proposal_wtc, ..., usesExactPricing(p))` with SOV overriding when present.

- **D3 (Settings)** — **`Settings.handleSave` in-handler canManage check exists; downgrade from HIGH to MEDIUM as defense-in-depth-only** (carry-over, downgraded — Settings.jsx now checks canManage in-handler)
  - `src/pages/Settings.jsx:667`
  - Evidence: `const handleSave = async () => { if (!canManage) { setError("Only an admin or manager can change settings."); return; } … }`
  - Prior audit (2026-08-03) reported the top-level form + Save button ships to every role — the check is now present in the handler at `:670`. Still relies on client-side `canManage` (a DevTools override bypasses); server RLS on `tenant_config` (B10) is the real backstop, and B10 still ships permissive in the legacy seed.
  - Spec: `CLAUDE_RLS.md` (defense-in-depth).
  - Fix: confirm `tenant_config` UPDATE RLS restricts to Admin/Manager (see B10 fix); keep the client `canManage` check as UX.

- **D-RunwayBar-save** — **`RunwayBar.save` writes `tenant_config.schedule_runway_*` with client-only canManage gate** (NEW)
  - `src/components/followup/RunwayBar.jsx:60`
  - Evidence: `await updateTenantConfig({ schedule_runway_weeks: weeksVal, schedule_runway_note: nInput, … });`
  - `canManage` prop is only used to gate the Edit button (`:80`). `save()` itself has no in-handler role check, so bypassing the button reveal invokes the write. RLS on `tenant_config` backstops it; defense-in-depth gap.
  - Spec: `CLAUDE_RLS.md` (defense-in-depth).
  - Fix: guard `save()` with `if (!canManage) return;` and rely on the server RLS Admin/Manager rule (B10 fix).

- **B-archive-tenant-helper** — **`archive.get_user_tenant_id()` doesn't filter `active = true` — deactivated employee retains archive access** (carry-over)
  - `sql/history_locker_phase1.sql:76`
  - Evidence: `SELECT tenant_id FROM public.team_members WHERE auth_id = auth.uid() LIMIT 1;` — no `AND active = true`.
  - Divergent from `public.get_user_tenant_id()` at `sql/rls_identity_tables.sql:129-137` (or `rls_cleanup_and_remaining.sql:377`) which restricts to `active = true`. A deactivated employee with a still-valid refresh token retains read/insert/delete access to their old tenant's `archive.import_batches` and `archive.legacy_records`. Offboarding hole.
  - Spec: OWASP-ASVS V3.2.3, OWASP A07.
  - Fix: add `AND active = true` to the WHERE clause, matching the public helper.

- **A5** — **Recipient email address logged in `stripe-webhook` sendEmail** (carry-over)
  - `supabase/functions/stripe-webhook/index.ts:49`
  - Evidence: `console.log("Email to", to, ":", res.status);`
  - Customer email addresses (payment receipts) are logged on every send; log-store retention effectively becomes indefinite PII retention outside the primary DB and its RLS.
  - Spec: OWASP-ASVS V7.1.3, OWASP A09, GDPR data-minimization.
  - Fix: `console.log("Email send status:", res.status);` — drop `to`.

- **A6** — **Invitee + inviter emails and names logged in `invite-user`** (carry-over)
  - `supabase/functions/invite-user/index.ts:88`
  - Evidence: `console.log("invite-user invoked", { email, name, teamMemberId, senderEmail, senderName });`
  - Every invite writes four PII fields to plain edge logs. Same log-retention concern as A5.
  - Spec: OWASP-ASVS V7.1.3, OWASP A09.
  - Fix: log only `teamMemberId` + a hashed email; drop names and sender identity.

- **A7** — **Recipient + rep emails logged in `send-proposal`** (carry-over)
  - `supabase/functions/send-proposal/index.ts:139`
  - Evidence: `console.log("send-proposal invoked", { proposalId, recipientEmail, repEmail, proposalNumber });`
  - Customer recipient and internal rep email land in the log on every proposal send.
  - Spec: OWASP-ASVS V7.1.3, OWASP A09.
  - Fix: log `proposalId` + `proposalNumber` only; drop `recipientEmail`/`repEmail`.

- **A8** — **Recipient email logged in `send-pay-app` invocation** (NEW)
  - `supabase/functions/send-pay-app/index.ts:99`
  - Evidence: `console.log("send-pay-app invoked", { payAppId, invoiceId, recipientEmail });`
  - Same shape as A5/A7: customer email is written to plain edge logs on every pay-app send. PII exposure in logs.
  - Spec: OWASP-ASVS V7.1.3, OWASP A09.
  - Fix: log `payAppId` + `invoiceId` only; drop or hash `recipientEmail`.

- **A9** — **Recipient email logged by shared `repNotify.sendEmail`** (NEW)
  - `supabase/functions/_shared/repNotify.ts:118` (also `:115` rejection + `:121` throw)
  - Evidence: `console.log(\`${tag}: emailed ${to} — ${body}\`);`
  - Shared helper used by `proposal-approved` and `follow-up-reminders` logs the rep's raw email address on every successful send (and again on rejection + throw). Every new edge function using this helper inherits the leak.
  - Spec: OWASP-ASVS V7.1.3, OWASP A09.
  - Fix: redact `to` (mask local-part) in all three sites; keep tag + status + body-slice for observability.

- **C-invite-user-sender** — **`invite-user` accepts caller-supplied `senderEmail` without verified-domain gate** (carry-over)
  - `supabase/functions/invite-user/index.ts:153`
  - Evidence: `from: senderEmail || "noreply@salescommand.app",`
  - `send-invoice`/`send-pay-app`/`send-proposal` all gate the `From:` to VERIFIED_DOMAINS; `invite-user` doesn't. Resend blocks fully unverified domains, so the exploitable case is picking any Resend-verified tenant domain to spoof a colleague's invite.
  - Spec: OWASP-ASVS V5.1.4, OWASP A08, CWE-345.
  - Fix: apply the VERIFIED_DOMAINS gate; fall back to `noreply@salescommand.app` for unverified domains.

- **C-invite-user-listusers** — **`invite-user` lists auth.users with default pagination (unbounded)** (carry-over)
  - `supabase/functions/invite-user/index.ts:102`
  - Evidence: `const { data: { users } } = await supabase.auth.admin.listUsers(); const existing = users?.find((u: any) => u.email === email);`
  - Default page size caps at 1000 across the whole Supabase project. Beyond it, existing users appear absent → invite links to `authId = undefined`, leaving the row broken. Also cross-tenant enumeration primitive since `listUsers` returns global `auth.users`.
  - Spec: OWASP-ASVS V8.3.1, OWASP A01, CWE-1284.
  - Fix: `supabase.auth.admin.getUserByEmail(email)` or a direct service-role SELECT against `auth.users`.

- **C-reset-password-listusers** — **`reset-password` uses the same unpaginated `listUsers()` pattern** (carry-over)
  - `supabase/functions/reset-password/index.ts:55`
  - Same failure mode as invite-user: users past the first page appear to not exist; function silently returns success without sending a reset email. Persistent hard-block on password reset at scale.
  - Spec: OWASP-ASVS V8.3.1, CWE-1284.
  - Fix: same as invite-user — direct-lookup call.

- **C-deactivate-user-count** — **`deactivate-user` job-count query not tenant-scoped** (carry-over)
  - `supabase/functions/deactivate-user/index.ts:119`
  - Evidence: `.from("call_log").select("id", { count: "exact", head: true }).eq("sales_name", member.name);`
  - Service-role client with no tenant filter. Same-named reps in different tenants leak each other's row counts.
  - Spec: OWASP-ASVS V4.1.3, OWASP A01.
  - Fix: `.eq("tenant_id", callerTenantId)`.

- **C-qb-record-payment-net** — **`qb-record-payment` `netAmount` ignores `retention_amount`** (carry-over, line moved 153→167)
  - `supabase/functions/qb-record-payment/index.ts:167`
  - Evidence: `const netAmount = (invoice.amount || 0) - (invoice.discount || 0);`
  - `send-invoice` computes Stripe-charged amount as `amount - discount - retentionAmount`; qb-record-payment reads directly from `invoices` and excludes retention. QB Payment recorded at the wrong amount on any invoice with retention → credit balance in QB or LinkedTxn validation failure. Money-reconciliation issue, not unauth exploit.
  - Spec: OWASP-ASVS V10.1.1, CWE-682.
  - Fix: `netAmount = amount - discount - (invoice.retention_amount || 0);` or use `session.amount_total / 100` from the webhook payload.

- **C-create-billing-session** — **`create-billing-session` lacks admin/manager gate** (carry-over)
  - `supabase/functions/create-billing-session/index.ts:57`
  - Any tenant member can open the Stripe billing portal or initiate a checkout that writes `tenant_config.stripe_customer_id`. Portal enables self-service cancel / plan-change / payment-method rotation.
  - Spec: OWASP-ASVS V4.1.3, OWASP A01.
  - Fix: switch to `requireAdminOrManager` on the checkout/portal branches; leave `status` branch open to any member.

- **C-qb-search-customers** — **`qb-search-customers` has no role gate** (carry-over)
  - `supabase/functions/qb-search-customers/index.ts:86`
  - Any active team_member can enumerate the tenant's full QB customer list (customer names + subs = job pipeline).
  - Spec: OWASP-ASVS V4.1.3, OWASP A01.
  - Fix: `requireAdminOrManager`.

- **C-qb-void-invoice** — **`qb-void-invoice` does not require admin/manager** (carry-over)
  - `supabase/functions/qb-void-invoice/index.ts:63`
  - Voiding mutates the QB audit trail permanently. Any active team_member can void any tenant invoice.
  - Spec: OWASP-ASVS V4.1.3, CWE-863.
  - Fix: `requireAdminOrManager`; also explicitly reject `caller.isServiceRole` unless a documented internal call needs it.

- **C-extract-sov** — **`extract-sov` has no rate limit / cost gate on paid Anthropic API** (carry-over)
  - `supabase/functions/extract-sov/index.ts:159`
  - `claude-opus-4-7` + `max_tokens: 16000`. Any tenant user can loop this over their own attached PDFs unbounded — a compromised session drains Anthropic budget in minutes.
  - Spec: OWASP-ASVS V11.1.4, CWE-770.
  - Fix: per-user + per-tenant token-bucket (~10/hr), daily cap, cache by SHA-256 of PDF bytes.

- **C-proposal-signed-rate** — **`proposal-signed` has no rate limit + response-code oracle** (carry-over)
  - `supabase/functions/proposal-signed/index.ts:30`
  - Public endpoint. No IP throttle; INVALID_TOKEN (403) vs ALREADY_SIGNED (409) vs INVALID_SIGNER_NAME (400) responses form a token-state oracle.
  - Spec: OWASP-ASVS V11.1.1, CWE-307.
  - Fix: token-bucket by client IP (5/15min); collapse token errors into a single generic 403 externally.

- **C-send-invoice-sender** — **`send-invoice` `senderEmail` only checked for domain, not identity** (carry-over)
  - `supabase/functions/send-invoice/index.ts:278`
  - VERIFIED_DOMAINS blocks external spoofing, but any authenticated tenant member can send `From:` any co-worker's address by supplying that string. No cross-check that senderEmail matches caller's team_members row.
  - Spec: OWASP-ASVS V5.1.4, OWASP A08.
  - Fix: resolve senderEmail from `team_members.email WHERE auth_id = caller.userId` (mirror send-pay-app:218-223).

- **C9** — **Raw `error.message` returned across 24 catch handlers** (carry-over, count grew)
  - Pattern across all catches: `invite-user:191`, `deactivate-user:131`, `delete-user:121`, `qb-auth:196`, `qb-record-payment:207`, `qb-search-customers:123`, `qb-void-invoice:195`, `qb-sync-invoice:469`, `qb-link-customer:149`, `extract-sov:194`, `reset-password:145`, `send-proposal:225`, `send-invoice:551`, `send-pay-app:508`, `stripe-webhook:270`, `scc-stripe-webhook:188`, `proposal-signed:108`, `create-billing-session:211`, `qb-create-job:317`, `follow-up-reminders:198`, `proposal-approved:87`, `qb-reflect-payments:73`, `deactivate-payment-link:99`, `check-orphan-users:33,60`.
  - Leaks Postgres error text (columns, constraint names, cross-tenant FK IDs), Supabase auth internals, QB Fault details, JSON stack fragments. Three new functions (`follow-up-reminders`, `proposal-approved`, `qb-reflect-payments`) added to the list.
  - Spec: OWASP-ASVS V7.4.1, OWASP A09, CWE-209.
  - Fix: shared `safeError(e)` helper — log full message server-side, return generic message + request-id externally.

- **C10** — **`send-invoice` returns raw Stripe + Resend response bodies verbatim** (carry-over)
  - `supabase/functions/send-invoice/index.ts:349` (Stripe), `:467` (Resend main), `:491` (Resend viewer warnings)
  - `send-pay-app:42` implements `sanitizeProviderError()`; `send-invoice` was not updated. Bodies can include request-ids, delivery diagnostics, and header/config values.
  - Spec: OWASP-ASVS V7.4.1, OWASP A09.
  - Fix: promote `sanitizeProviderError` to `_shared/`; redact Bearer/`sk_`/`pk_`/`re_` tokens; cap at 500 chars.

### Low

- **D-NewPayAppModal-id** — **Invoice ID generated from client-side max-id scan (race + soft IDOR)** (carry-over, line moved 170→176)
  - `src/components/NewPayAppModal.jsx:176` (also `src/pages/Invoices.jsx:1109` `mintNextInvoiceId`, `:2088` in `handleVoidConfirm`)
  - Evidence: `const nextId = String(lastNum + 1).padStart(5, "0");`
  - Concurrent creates collide on the PK constraint; the SELECT also enumerates 50 recent invoice IDs across the tenant.
  - Spec: OWASP-ASVS V11.1.4, OWASP A04.
  - Fix: server-side RPC or Postgres sequence keyed on tenant.

- **D-materials-catalog-wtc** — **`WTCCalculator` inline materials editor writes `materials_catalog` with no role gate** (carry-over)
  - `src/lib/materialsCatalog.js:48-87` (`saveCatalogRow`, no role check); called from `WTCCalculator` materials tab
  - `Settings.jsx` UI wraps WorkTypesSection + MaterialsCatalogSection in `{canManage && …}`, but the shared writer in `materialsCatalog.js` is also called from WTCCalculator's inline material picker with no gate. Any tenant member can insert/fork/update `materials_catalog` rows via the WTCCalculator materials tab, bypassing the Settings UI gate.
  - Spec: `CLAUDE_RLS.md`.
  - Fix: enforce write-role on `materials_catalog` + `work_types` via RLS; UI gating alone is insufficient because WTCCalculator is a second call site.

- **D-sc_calllog_cache-shape** — **sessionStorage cache parsed without schema validation** (carry-over, line moved 44→60)
  - `src/pages/CallLog.jsx:60`
  - `JSON.parse` is try/catch-wrapped, but parsed rows go straight into state without a shape check. A code-execution primitive can seed the cache to alter list contents.
  - Spec: OWASP-ASVS V5.1.3, OWASP A03.
  - Fix: discard cache if `rows`/`team`/`customers`/`workTypes` aren't arrays; add a cache-version stamp.

- **B-legacy-search-path** — **`sql/db_hardening_phase1.sql:12` `get_user_tenant_id()` seed body has no `SET search_path` AND uses `tenant_config` fallback** (carry-over)
  - `sql/db_hardening_phase1.sql:12`
  - Evidence: `CREATE OR REPLACE FUNCTION public.get_user_tenant_id() RETURNS uuid AS $$ SELECT id FROM public.tenant_config LIMIT 1; $$ LANGUAGE sql SECURITY DEFINER STABLE;`
  - Earliest SECURITY DEFINER body has no `SET search_path` and returns the first tenant regardless of caller. Overwritten by the hardened version in `rls_identity_tables.sql:129-137` and `rls_cleanup_and_remaining.sql:377`, but between the two runs (or if the second file is skipped) the function is exposed to search_path injection AND collapses tenant isolation to 'first tenant wins'.
  - Spec: OWASP-ASVS V5.3.5, OWASP A08 — Supabase function search_path hardening.
  - Fix: replace the body with the auth-aware version + `SET search_path = public`; also add `SET search_path` to `set_updated_at()` and `fn_auto_in_progress()` in the same file.

- **B-soft-delete-select** — **SELECT policies on `proposals`/`invoices` don't filter `deleted_at IS NULL`** (NEW)
  - `sql/rls_core_tables.sql:138` (proposals), `:185` (invoices)
  - Evidence: `CREATE POLICY "proposals_select" ON public.proposals FOR SELECT TO authenticated USING (tenant_id = public.get_user_tenant_id());`
  - `proposals` and `invoices` both carry a `deleted_at` soft-delete column, but the SELECT policies don't add `AND deleted_at IS NULL`. Every list/detail query in the app must remember to filter — one missed spot and a soft-deleted row becomes visible again. Defense-in-depth only (same tenant), not a confidentiality leak.
  - Spec: OWASP-ASVS V8.3.4, OWASP A04.
  - Fix: add `AND deleted_at IS NULL` to the SELECT USING clause on `proposals` and `invoices`; expose an explicit view / RPC for trash-recovery UI admins can query.

- **A10** — **Full Resend response body logged verbatim in `reset-password`** (NEW)
  - `supabase/functions/reset-password/index.ts:129`
  - Evidence: `console.log("Reset email response:", emailRes.status, emailResBody);`
  - Full Resend response body written to logs on every reset. Resend responses carry the message id and can echo the recipient in error payloads; defense-in-depth minimization issue.
  - Spec: OWASP-ASVS V7.1.3, OWASP A09.
  - Fix: log only `emailRes.status` (and `emailRes.ok`) — parse the body for an id if needed and log that alone.

- **A11** — **Resend response body slice logged in `send-invoice` main path** (NEW)
  - `supabase/functions/send-invoice/index.ts:465`
  - Evidence: `console.log("Main email response:", mainRes.ok, mainRes.txt.slice(0, 200));`
  - First 200 bytes of the Resend body land in logs on every main-recipient invoice send. Provider errors sometimes echo the recipient into the body.
  - Spec: OWASP-ASVS V7.1.3, OWASP A09.
  - Fix: log `mainRes.ok` + status only; drop the body slice or gate it behind a debug-mode env flag.

- **E2** — **`pdf-lib@1.17.1` unmaintained (~4 years since last publish)** (carry-over)
  - `package.json:16`
  - Evidence: `"pdf-lib": "^1.17.1"` — last release Oct 2021. Used in `src/lib/payAppPdf.js:1`, called by `NewPayAppModal` + `PayAppDetailModal`.
  - No open CVEs. Called only on user-initiated pay-app PDF generation with server-derived data (proposal_wtc, invoice_lines) — no untrusted PDF is parsed. Risk = future undiscovered bug never patched.
  - Spec: OWASP A06.
  - Fix: track upstream; consider `@cantoo/pdf-lib` (maintained fork) or fold pay-app rendering into `jspdf`.

- **E6** — **`dompurify@3.4.1` XSS bypasses (optional transitive of jspdf; `.html()` path unused)** (carry-over)
  - `package-lock.json:1424` (marked `optional: true`)
  - Evidence: 10 advisories across IN_PLACE mode, hook pollution, custom-element handling (up to moderate, ≤3.4.12).
  - Grep of `src/` confirms zero calls to `jspdf.html(` — DOMPurify is loaded but never invoked. Not exploitable.
  - Spec: OWASP A06.
  - Fix: add `overrides: { "dompurify": "^3.4.13" }` to close the noise; keep `.html()` ban as a code convention.

- **E7 (nanoid)** — **`nanoid@3.3.11` infinite loop on non-positive size (NEW)**
  - `package-lock.json` (transitive of postcss → Vite)
  - Evidence: GHSA-28wg-ghj8-5hjv (loop on negative size) + GHSA-2v37-7h3g-55p8 (loop on zero size). Fixed in ≥3.3.18.
  - Transitive of postcss → Vite (dev/build-only). App code does not import nanoid; used inside PostCSS to generate map source ids. No attacker-controlled size argument reaches it. Zero runtime/build-time exposure.
  - Spec: OWASP A06.
  - Fix: bumped for free by the postcss/Vite upgrade in E3/E4; otherwise add `overrides: { "nanoid": "^3.3.18" }`.

- **C-stripe-webhook-reclaim** — **`stripe-webhook` re-process branch can double-record QB Payment + double-send emails** (carry-over)
  - `supabase/functions/stripe-webhook/index.ts:101`
  - Evidence: `// 'claimed' (or unreadable): a prior delivery did not finish → RE-PROCESS.`
  - If a prior delivery updated `invoices.status='Paid'` and `qb-record-payment` succeeded but died before `markDone()`, Stripe retries land here, see "claimed", re-process → `qb-record-payment` runs again (does NOT check `invoice.qb_payment_id` before creating a QB Payment — creates unconditionally at :170, overwrites `qb_payment_id` at :175). Net: duplicate QB Payment leaves a credit balance. Email sending has no idempotency guard, so the customer + rep also receive duplicate 'Payment Received' emails.
  - Spec: OWASP-ASVS V8.1.5, CWE-362.
  - Fix: age-gate the reclaim (only re-process claimed rows older than N minutes); in `qb-record-payment` early-return when `invoice.qb_payment_id` is set.

- **C-check-orphan-error** — **`check-orphan-users` returns raw `error.message`** (carry-over)
  - `supabase/functions/check-orphan-users/index.ts:33,60`
  - Cron target — caller is Supabase scheduler with cron secret, so external exposure is low. Included for parity with C9.
  - Spec: OWASP-ASVS V7.4.1, OWASP A09.
  - Fix: return generic error; log server-side.

- **C-deactivate-payment-link-cors** — **`deactivate-payment-link` catch omits CORS headers** (carry-over)
  - `supabase/functions/deactivate-payment-link/index.ts:99`
  - Catch returns `Content-Type` only — browser hides the body and the UI sees an opaque network error.
  - Spec: OWASP-ASVS V14.5.3, CWE-1021.
  - Fix: spread `...corsHeaders` in the catch response.

- **C18** — **`buildCorsHeaders` omits `Access-Control-Allow-Methods`** (carry-over)
  - `supabase/functions/_shared/cors.ts:49`
  - Preflight responses omit Allow-Methods. Chrome/Firefox default permissively so most POSTs still succeed; strict CORS clients refuse. Comment at :11-13 acknowledges out-of-scope.
  - Spec: OWASP-ASVS V14.5.3, CWE-1021.
  - Fix: add `"Access-Control-Allow-Methods": "POST, OPTIONS"`.

- **C-proposal-signed-error** — **`proposal-signed` returns raw `error.message` on the top-level catch** (carry-over)
  - `supabase/functions/proposal-signed/index.ts:108` (moved from 144)
  - Public endpoint — any unauth caller can trigger 500 (e.g. malformed JSON) and read raw Postgres/Deno error text.
  - Spec: OWASP-ASVS V7.4.1, OWASP A09.
  - Fix: return `{ error: "Sign failed" }`; keep detail in `console.error`.

- **C-invoice-existence-oracles** — **404-vs-403 oracle across four QB fns lets a caller enumerate cross-tenant invoiceIds** (carry-over)
  - `supabase/functions/deactivate-payment-link/index.ts:41`, `qb-void-invoice/index.ts:79`, `qb-sync-invoice/index.ts:127`, `qb-record-payment/index.ts:109`
  - Evidence (pattern): `if (!invoice) return 404 "Invoice not found"; if (!caller.isServiceRole && invoice.tenant_id !== caller.tenantId) return 403 unauthorized;`
  - Existence check happens BEFORE tenant check — 404 for nonexistent vs 403 for other-tenant. Lets an authenticated attacker enumerate valid invoiceIds across tenants.
  - Spec: OWASP-ASVS V4.2.2, OWASP A01, CWE-204.
  - Fix: return 404 uniformly for both branches across all four fns (or 403 uniformly).

- **C-qb-sync-invoice-svc-scope** — **`qb-sync-invoice` uses service-role client for reads/writes after user auth** (NEW)
  - `supabase/functions/qb-sync-invoice/index.ts:113`
  - Evidence: `const sb = createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY); const caller = await authenticateCaller(sb, req, SUPABASE_SERVICE_ROLE_KEY);`
  - Single service-role client is used to authenticate the caller AND to read invoices/proposals/call_log and mutate invoices. RLS is bypassed on every read, so tenant scoping depends entirely on explicit `.eq('tenant_id', caller.tenantId)` filters throughout the file — the invoice+proposal reads at `:126` and `:163` do NOT scope by tenant (the check comes after). If any future edit forgets to add the tenant filter it silently reads/writes across tenants.
  - Spec: OWASP-ASVS V4.1.1, OWASP A01.
  - Fix: use a user-JWT-scoped anon client for reads (RLS auto-scopes) and reserve the service-role client for writes that require bypass — mirror `invite-user:38-43`.

- **F-stray-fix-script** — **Stray Python here-doc script tracked under `src/pages/`** (carry-over)
  - `src/pages/fix_proposals_pdf.py:1`
  - Evidence: `cat > fix_proposals_pdf.py << 'ENDOFSCRIPT'`
  - Not JS/JSX, not imported anywhere, opens with a shell here-doc header meaning it was captured mid-heredoc and committed to a page directory. Dead code. Inert (never imported), so no runtime bug — but a copy-paste violates Rule 3 (calcs must use `calcMaterialRow()`) and it targets a stale path (pre-v52 `src/pages/Proposals.jsx`) so anyone re-running it would silently write nothing.
  - Spec: `CLAUDE.md` Workflow Rules (no orphan/half-baked artifacts), Data Integrity Rule 3.
  - Fix: `git rm src/pages/fix_proposals_pdf.py`.


## Diff vs last audit
_pending_

## Coverage

### Pass A — Secrets & config
- Hardcoded API keys/JWTs/service_role tokens grep across `.ts/.tsx/.js/.jsx/.mjs/.json/.toml/.html`.
- `service_role` usage confined to `supabase/functions/` (verified none under `src/`).
- `VITE_*` env var enumeration — only `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY` referenced.
- `.env*` gitignore coverage and tracked-file check (`git ls-files`) — none tracked.
- Log leakage of secrets/PII across every `supabase/functions/*/index.ts`, new client pages (`CheckoutPage`, `FeatureDetailPage`, `InvoicePaidPage`, `Import/*`, `components/followup/*`, `components/HistoryLocker/*`).
- Re-verification of prior A1, A2, A1b, A2b, A4, A5, A6, A7 at current line numbers.
- New edge functions in scope: `follow-up-reminders`, `leads-intake`, `proposal-approved`, `qb-reflect-payments`, `qb-webhook`.
- Cron/webhook auth surface (CRON_SECRET timingSafeEqual, QB HMAC verifier, LEADS_INTAKE_SECRET constant-time, Stripe HMAC verifier).

_pending — passes B, C, D, E, F_
