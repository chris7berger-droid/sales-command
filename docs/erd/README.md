# ERD (Expectation / Result / Delta)

The ERD methodology lives in its own repo: **https://github.com/chris7berger-droid/erd-loop**

This file is a stub — the methodology, per-task template, and chronological log are all in `erd-loop`. Loops run against work in this repo (`sales-command`) get logged there, not here.

See also `docs/plans/self_protection_layer.md` for the 100-point scorecard (companion lens — tracks what's built, while ERD tracks how predictions calibrate).
