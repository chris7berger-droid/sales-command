SC Handoff v113 — 2026-05-12
============================

SESSION SUMMARY
Audit + build session shipped Multi-GC Migration 1a to prod
(8 columns on proposals + 1 on proposal_wtc + proposal_clones
audit table + intro trigger), then mid-flight reversed the §5(c)
"block duplicates" resolution after build challenged the
domain-fact premise — multi-WTC-same-work_type is intentional
encoding for sub-area/time-phasing/crew splits, not an import
bug. UX guard reverted (never reached scmybiz.com); B17/B18/O5
closed; F16 filed as T1 blocker for §10 step 6. Cross-repo
collision with sch-command surfaced during cleanup: their Jobs
IA implementation (commit 2a286e9 on sch-command/main) shipped
DDL via dashboard/db-query bypass, leaving prod's job_wtcs
table live with zero migration ledger trace. Breadcrumb dropped
in sch-command's latest handoff with reconciliation recipes.
Two audit misses owned in AUDIT_LOG: ratifying §5(c) without
challenging the domain-fact premise; relying on session-old
sch-command state for the cross-repo directive. Numbering jumps
v112 → v113 because v112 was taken by the +Add CO wizard session
earlier today.

CHANGES SHIPPED
─────────────────────────────────────────────────────────────────
Commits on feat/multi-gc-1a (chronological), FF-merged to
feat/multi-gc-allocation at session close:

84edc1b — feat(migration): Multi-GC schema additions —
  Migration 1a (Touches O5; files B17, B18)
  8 columns on proposals (customer_id, markup_override_pct,
  cloned_from_proposal_id, lost_reason, lost_at,
  locally_edited_fields, rfp_number, bid_due_date) + 1 on
  proposal_wtc (locally_edited_fields) + indices + COMMENT ON
  COLUMN blocks for rfp_number/bid_due_date + proposal_clones
  audit table with full RLS block + proposals_track_local_edits
  trigger function + BEFORE UPDATE OF intro trigger.

ba747d3 — feat(ux): WTCCalculator work-type duplicate guard
  (Multi-GC §5(c))   [REVERTED in 44f7c59]
  Added usedWorkTypeIds state + sibling-WTC fetch in
  loadWorkTypes + disabled-options-with-suffix in BiddingTab.
  Reverted later when §5(c) premise overturned.

6b381cd — fix(migration): drop NOT NULL on
  proposal_clones.parent_proposal_id (audit M1)
  Audit M1: NOT NULL + ON DELETE SET NULL was self-contradicting.
  Dropped NOT NULL so cascade can complete on parent hard-delete.
  Plan doc fix paired + ratification note appended.

eadd93b — fix(migration): rename Migration 1a to 20260513000000
  (collision avoidance)
  Original timestamp 20260512120000 collided with sch-command's
  pre-reserved placeholder ledger rows. Renamed file via git mv
  + updated plan-doc filename references + filed O6/O7/O8 +
  refreshed BACKLOG last-updated.

44f7c59 — §5(c) reversal: multi-WTC-same-work_type is intentional
  (Closes B17, B18, O5; files F16)
  Restored WTCCalculator.jsx to pre-ba747d3 state. Closed
  B17/B18/O5 in BACKLOG Completed Log. Filed F16 (T1). Appended
  §5(c) Reversal subsection + §10 step 6 second-amendment to
  plan doc. Appended AUDIT_LOG row + notes section.

0f75a20 — docs: log Migration 1a prod-apply + cross-repo
  discovery (resolves O8)
  AUDIT_LOG row 15 outcome changed → applied. Notes section
  appended for prod-apply, phantom-row revert, post-revert
  cross-repo discovery. BACKLOG O8 Resolved.

Cross-repo commit on sch-command/main:

a258d23 — cross-repo note: sales-command Migration 1a
  reverted two placeholder ledger rows on prod (reconciliation
  needed before next db push)

DEPLOYED
────────
Migration 20260513000000_multi_gc_allocation applied to prod
(pbgvgjjuhnpsumnowuym) via `supabase db push --linked`.
PostgREST schema cache reloaded via NOTIFY pgrst. No edge
functions deployed. Vercel rebuilt feat/multi-gc-1a preview
after the §5(c) reversal (UX guard removed from preview;
guard never landed on main).

DECISIONS / CHOICES MADE
────────────────────────
§5(c) "block duplicates" REVERSED. Multi-WTC-same-work_type
is the intentional canonical encoding for sub-area splits,
time-phasing, and crew assignment. proposal_wtc.sub_areas
(jsonb) is an orthogonal within-WTC mechanism. V8 "dups" were
sub-area splits, not import bugs. Chris confirmed domain
assertion. UX guard + UNIQUE constraint + B17/B18/O5 all
dropped. F16 filed for the §5 sync identity re-plan
(cloned_from_wtc_id lineage column needed before §10 step 6
RPCs can be authored).

Phantom ledger row revert. Audit's prior "don't touch sch-command
rows" directive was overcautious. Both rows had has_statements=false
(no DDL), no local files existed in sch-command at the time of
check, and the rows actively blocked our db push. Reverted via
supabase migration repair --status reverted. Decision later
complicated by post-revert discovery that sch-command's actual
Jobs IA DDL HAD been applied to prod via dashboard/db-query
bypass — see Audit Miss #2 below.

Minimal-scratch over full dump-restore. Migration 1a was
smoke-validated against scratch project ibalavttrqjyijrnkwmd
with a hand-rolled minimal bootstrap (6 shell tables + 2
helper-function stubs) rather than a full prod schema dump.
Justification: migration is additive + IF NOT EXISTS guarded;
trigger semantics validation only requires the columns the
migration itself adds; full-dump required Docker or libpq
install or prod password rotation. Prod column-collision
precheck (C1/C2/C3) returned zero rows confirming no name
collisions. Scratch deleted post-validation.

Two audit misses owned:
1. §5(c) ratification miss. Audit ratified the Round-5
   planning resolution without challenging the load-bearing
   domain-fact premise ("duplicates are a bug"). The plan
   agent's reasoning chain was internally consistent given
   the premise, but the premise was wrong. Lesson: future
   audit ratification passes should explicitly challenge
   domain-fact premises, not just verify reasoning chains.
   New audit-miss pattern tag introduced.
2. Cross-repo state staleness. Audit's directive to drop a
   breadcrumb in sch-command was drafted from sch-command
   state observed early in our session (only one migration
   file present, latest handoff v8). By the time build
   executed, sch-command had shipped 4 new commits including
   the actual Jobs IA implementation. Push rejected; build
   correctly stopped. Lesson: cross-repo directives must
   re-verify remote state immediately before execution.

NEW BACKLOG ITEMS
─────────────────
- O6 (T2) — Migration history not replayable from zero (two
  2026-04-16 migrations in dependency-violating timestamp
  order; fresh-scratch db push fails). Filed during scratch
  bootstrap.
- O7 (T1) — Multi-repo migration coordination. sales-command +
  sch-command share Supabase project pbgvgjjuhnpsumnowuym with
  no timestamp coordination convention. Today's collision was
  silent (db push skips local file whose version is in ledger
  without applying DDL). Recommend pre-push hook +
  pre-draft-ledger-query convention. T1 because next collision
  could silently no-op a real DDL push.
- O8 (T2) — Two placeholder ledger rows reserved via repair
  without local DDL. Resolved 2026-05-12 via revert.
- F16 (T1) — Re-plan §5 sync identity using cloned_from_wtc_id
  lineage on proposal_wtc. Blocks all of §10 step 6 (clone +
  sync RPCs). Estimated one Plan-agent round.

CLOSED THIS SESSION
───────────────────
- B17 — Archive import creating duplicates (Not-a-Bug; closed
  in 44f7c59)
- B18 — Triage 17 dup pairs (Not-Applicable; closed in 44f7c59)
- O5 — Migration 1b UNIQUE constraint (Won't-Do; closed in
  44f7c59)
- O8 — Phantom ledger rows (Resolved; via revert + paperwork
  commit)

VERIFICATION
────────────
Scratch (ibalavttrqjyijrnkwmd, deleted post-smoke):
- Migration 1a applied clean via supabase db push.
- Smoke 1: proposal_clones exists + count = 0 ✓
- Smoke 2: parent intro UPDATE leaves locally_edited_fields = {}
  (trigger short-circuit on cloned_from_proposal_id IS NULL) ✓
- Smoke 3: sister intro UPDATE appends 'intro' to
  locally_edited_fields ✓
- Smoke 4: second sister intro UPDATE remains {intro}
  (idempotent) ✓
All four passed with ROLLBACK + no ASSERT errors.

Prod (pbgvgjjuhnpsumnowuym, applied 2026-05-12):
- Migration db push reported clean apply.
- PostgREST schema reloaded.
- DEFERRED to next session: read-path smoke on scmybiz.com
  (~30s) + trigger NO-OP smoke on real prod parent (~1min).
  Migration is additive + IF NOT EXISTS-guarded +
  scratch-validated; failure probability very low but smokes
  should still run for due diligence.

Cross-repo: sch-command's job_wtcs table verified present on
prod with full Jobs IA schema. Migration ledger has zero trace
of the DDL's apply path. Breadcrumb dropped advising
reconciliation before next sch-command db push.

NOT TOUCHED THIS SESSION
────────────────────────
- §10 implementation steps 3 onward. Step 2 (Migration 1a)
  shipped; steps 3-11 remain. F16 must land before step 6
  (RPCs) can be authored.
- O3 (H5 Migration B) — 48h gate from 2026-05-11 02:09 UTC
  passed 2026-05-13 02:10 UTC. Not addressed; remains Open.
- O6 (migration history replayability) — filed today, no
  remediation attempted.
- All T2/T3 backlog beyond items touched.

NEXT SESSION POINTERS
─────────────────────
1. Read order: docs/BACKLOG.md → this handoff (v113) →
   docs/plans/multi_gc_allocation.md.
2. Highest priority: F16 — §5 sync identity re-plan. One
   Plan-agent round to design cloned_from_wtc_id lineage column
   + clone RPC body changes + sync RPCs join key change +
   migration shape. Use the existing file-as-memory pattern on
   feat/multi-gc-allocation; durable artifact appended to
   docs/plans/multi_gc_allocation.md as a new §5 amendment.
3. Run deferred prod smokes from today: read path on
   scmybiz.com (open existing proposals, confirm clean load);
   trigger NO-OP on real parent (edit intro of a
   cloned_from_proposal_id IS NULL proposal, verify
   locally_edited_fields = {}). Both safe to run any time
   post-deploy.
4. O3 (H5 Migration B) — pg_stat_statements check now valid
   per 48h timer. Drop 1-arg mark_proposal_signed compat
   wrapper + legacy policy.
5. O7 cross-repo coordination — worth a separate planning
   session given the structural impact. Decide convention
   (pre-draft ledger query + pre-push hook recommended)
   before any further migration work in either repo.

FILES TO PROBABLY KNOW ABOUT NEXT SESSION
─────────────────────────────────────────
supabase/migrations/20260513000000_multi_gc_allocation.sql
  — Live on prod. Canonical reference for the schema shape
    we now build on top of.

docs/plans/multi_gc_allocation.md
  — §5(c) Reversal subsection + §10 step 6 second-amendment
    + §11 V8 results section all added this session.

docs/AUDIT_LOG.md
  — Row 15 (Migration 1a outcome=applied) + row 16 (§5(c)
    reversal) + new notes sections.

docs/BACKLOG.md
  — F16 (T1) is the immediate next blocker. B17/B18/O5/O8 in
    Completed Log.

~/sch-command/SCH_HANDOFF_v11.md
  — Carries our cross-repo breadcrumb (appended this session).
    sch-command keeps handoffs in repo root, not docs/handoffs/.

GIT STATE ON CLOSE
──────────────────
- sales-command branch feat/multi-gc-allocation: FF-merged
  from feat/multi-gc-1a at session close. Pushed to origin.
- sales-command branch feat/multi-gc-1a: deleted (local + origin).
- sales-command main: unchanged. Migration 1a lives only on
  feat/multi-gc-allocation pending the rest of §10 implementation.
- sch-command main: cross-repo breadcrumb commit pushed (a258d23).
- Working tree: clean.
- No open PRs touched this session.
- No scratch projects (ibalavttrqjyijrnkwmd deleted post-smoke).

END STATE
─────────
Migration 1a live in prod. §10 implementation phase resumes at
step 3 (client fallback rollout) — but step 6 (RPCs) is gated
on F16 re-plan. Two audit misses documented for future
ratification-pass discipline. Cross-repo collision with
sch-command resolved and bread-crumbed; their ledger
reconciliation is their next move. Sales-command paperwork
clean, FF merged, ready for fresh session.
