SC Handoff v142 — 2026-06-01
============================

SESSION SUMMARY
─────────────────────────────────────────────────────
Build terminal for ERD Loop #30 — per-invoice retention release. Implemented
the RELEASE side of retention (the withhold side already shipped): a "Bill
Retention $X" button on an invoice's detail screen spawns a release invoice for
the held amount, marks the source released, and syncs to QB so the held dollars
move out of Other Current Asset back into A/R. Built strictly to the converged,
3-round-audited plan at docs/plans/retention_invoice_process.md (§3 build order).
Migration is LIVE on the shared prod DB and qb-sync-invoice is REDEPLOYED to
prod; the frontend (the button) is on the feature branch + Vercel preview only,
NOT yet on main/prod. Smoke PASSED end-to-end on the preview against a TEST
customer under job #10021 — release invoice #10064 synced to QB with exactly the
right single positive retention line. Still on the feature branch awaiting merge.

═══════════════════════════════════════════════════════
1. /buildvsplan VERDICT + DEVIATION FROM §3
─────────────────────────────────────────────────────
/buildvsplan ran BEFORE smoke (read-only gate). Verdict:

  BUILD vs PLAN · docs/plans/retention_invoice_process.md
  🔴 Tier 1 blockers: 0   🟠 Tier 2 bugs: 0   🟢 deferred: 1 (F34, correct)
  SMOKE TEST: GO

Live-schema reality check (the differentiator): both new columns probed
HTTP 200 against PROD REST (retention_release_of, retention_released) — genuinely
deployed, not just file-present. vite build clean (544 modules). No new eslint
errors in edited regions (pre-existing errors only).

DEVIATION FROM §3 — DESIGN/CODE: **NONE.** Every code deliverable matches §2/§3
exactly (button visibility predicate, billing flag, UPDATE-source-first w/ rows-
affected check, INSERT field list incl. tenant_id, revert-on-failure w/ its own
rows-affected check + loud manual-recovery alert, void-copy one-liner, QB full-
assembly gate + positive release line + hard-fail fallback, no-optimistic-setInv
navigate, send-invoice verified unchanged). Per the audit's standing rule
(no round-4 unless the IMPLEMENTATION deviated), no round-4 is warranted.

DEVIATION FROM §3 — PROCESS (disclosed, end-state identical): ONE.
  • §3 step 1 specified `npm run db:push`. Actual: the additive DDL was applied
    via the Supabase SQL editor (Chris ran it), then recorded in the ledger with
    `supabase migration repair --status applied 20260601120000`. Reason: the
    wrapper is BLOCKED by sibling-repo ledger entries (see O10 / Surprises).
    End state is identical to a clean db:push — columns live (HTTP 200), ledger
    aligned (collision check: 64 local / 64 in ledger). Chris explicitly approved
    running the push directly when the wrapper failed.
  • Micro-note (not a deviation): §2.3 "add retention_release_of to the invoice
    SELECT" was already satisfied — qb-sync-invoice selects `*` (index.ts:115),
    so the column is present without an explicit edit.

═══════════════════════════════════════════════════════
2. SMOKE EVIDENCE (locked §4 success artifact)
─────────────────────────────────────────────────────
Setup: TEST customer "TEST Sales 2 Schedule connections", job #10021 (already
QB-linked). Source invoice #10063: gross $1,010.00, retention 10% → **R = $101.00**
held, net due $909.00. Synced to QB cleanly ("#10063 is in QB properly").

Release flow (on the Vercel preview):
  ✓ "Bill Retention $101.00" button appeared on #10063 detail.
  ✓ Click → release invoice #10064 spawned (next-free id), landed on it.
  ✓ Source #10063 flipped to released state ("Retention billed").
  ✓ #10064 Marked Sent → Synced to QuickBooks successfully.

PRIMARY ARTIFACT — QB invoice #10064 (saved: docs/handoffs/assets/
loop30_qb_invoice_10064_release.pdf):
  • Exactly ONE line: "1121- Retention %" / desc "Retention release" / Qty 1 /
    Rate $101.00 / **Amount +$101.00**. No Services line, no discount, no negative
    block — the full-assembly gate worked (a release row has no invoice_lines and
    would otherwise have pushed a +$101 Services line; it did not).
  • Note to customer: "Retention release for invoice #10063". Total $101.00.
  That single positive line IS the OCA→A/R movement: "1121- Retention %" maps to
  the Construction in Progress-Retent (Other Current Asset) account, so a positive
  invoice line debits A/R +$101 and draws that OCA account down −$101.

BALANCE-SHEET DELTA — **NOT captured in the locked form; honest gap.** The two
balance sheets pulled (saved: loop30_balance_sheet_cashbasis_before_1514.csv @
03:14 PM, loop30_balance_sheet_cashbasis_after_1520.csv @ 03:20 PM) were both
**Cash Basis** and are byte-for-byte identical — cash basis cannot show A/R or
the retention OCA account at all, so NO movement is visible:
    AR Retention:                    $0.00      →  $0.00      (no change)
    Construction in Progress-Retent: $28,730.10 →  $28,730.10 (no change)
    Accounts Receivable:             $435.44    →  $435.44    (no change)
The literal §4 artifact (OCA −R / A/R +R in figures on an ACCRUAL sheet) was
therefore NOT obtained. The #10064 invoice line is the equivalent/stronger proof
(it shows the exact mechanism, not just the net). To close the locked wording
verbatim, pull ONE Accrual balance sheet and Construction in Progress-Retent will
show the −$101 / A/R +$101 move. Recommended but not blocking — see Next Session.

Screenshot saved: loop30_invoice_10063_prod_detail.png (prod view of #10063 —
shows it has NO Bill Retention button on prod, which is correct; the button is
preview-only until merge).

CLEANUP STILL OWED BY CHRIS: void #10063 + #10064 **manually in QBO** to restore
HDSP's books to baseline. NOTE: in-app Pull Back/Void will NOT void them in QB
because the job name contains "test" (B41 asymmetry) — they must be voided
directly in QuickBooks (Sales → Invoices → open each → More → Void). The in-app
Pull Back already marked the SC DB rows voided. (Void-copy double-bill guard still
verifiable on the preview: after the SC-side pull back of #10063, the Bill
Retention button must NOT reappear on its replacement.)

═══════════════════════════════════════════════════════
3. SURPRISES / QUIRKS / NEW BACKLOG / [HYP]
─────────────────────────────────────────────────────
• MIGRATION CROSS-REPO LEDGER WALL (new backlog O10, T2). `npm run db:push` AND
  bare `supabase db push` BOTH refused this migration because sch-command's 5
  applied migrations sit in the shared prod ledger with no file in THIS repo:
  check-migration-safety.sh Check B (git log --all is repo-local) flagged them as
  divergent and wrongly suggested `repair --status reverted` (the 2026-05-18
  footgun); the supabase CLI refused with "Remote migration versions not found in
  local migrations directory" and suggested the same. Verified by hand that all 5
  (20260503190000, 20260512120000, 20260512120100, 20260528120000, 20260528133000)
  are legit sch-command files → NO revert. Worked around via SQL-editor apply +
  `repair --status applied`. THIS WILL RECUR on every future SC migration. Related
  to O7/O8/O9; filed as O10 with a concrete fix (teach Check B to scan sibling
  repos). [HYP worth promoting: the migration-safety guardrail now produces a
  false-positive that BLOCKS the happy path — the guardrail has outlived its
  single-repo assumption.]

• CASH-BASIS vs ACCRUAL for retention verification (project insight). Retention
  lives in A/R + an OCA account — both ACCRUAL concepts. A Cash-Basis balance
  sheet hides the entire withhold/release movement. Any future retention smoke
  MUST pull Accrual. Cost ~20 min of confusion this loop.

• "test" word only suppresses AUTO QB sync (Mark-Paid / Edit paths), NOT the
  manual "Sync to QuickBooks" button or qb-sync-invoice itself (those gate on
  qb_skip_sync + voided only). So a test-named job still pushes to QB on manual
  sync — which is what made the smoke possible without a real customer name.

• QB SYNC/VOID "test"-NAME ASYMMETRY (new backlog B41, T3). The flip side of the
  above bit the cleanup: Void/Pull Back DOES have the test guard
  (handleVoidConfirm Invoices.jsx:1398-1400) and SKIPS the QB void for test-named
  jobs, while manual Sync has NO guard and pushed them IN. Result: #10063 + #10064
  synced to QB, then Pull Back left BOTH live + Open in QB (DB marked voided, QB
  untouched). Chris had to void them by hand in QBO. Pre-existing, not from this
  loop's code. Filed B41 (fix: make the two paths symmetric — preferably skip QB
  for test jobs on BOTH sync and void).

• Sync button hides while invoice status == "New" (Invoices.jsx:1736) — must
  "Mark as Sent" first. Applies to both source and release invoice. Minor smoke
  gotcha, no code issue.

• NO QB fallback fired — the retention item "1121- Retention %" was found, so the
  positive-item branch ran (not the hard-fail). Hard-fail path is unexercised by
  this smoke (it only triggers on a tenant with no retention item configured).

• F34 already filed (full block/cascade void UX, deferred per round-2 scope-cut).
  F6 touched (per-invoice done, per-job/cumulative still open).

═══════════════════════════════════════════════════════
4. ACTUAL TIME (build → smoke-pass) vs 1hr TIME-fear lock
─────────────────────────────────────────────────────
Hard stamps (PDT): build commit 33dc4d8 @ 14:52:42 · balance sheets pulled
15:14–15:20 · smoke pass (invoice #10064 confirmed) ~15:20 · handoff ~15:30.
No precise session-START stamp was recorded, but code + the migration-ledger
detour landed by the 14:52 commit; estimated build→smoke-pass ≈ 1.25–1.5 hr.
TIME-fear DELTA: modest overage on the 1hr lock. The single overage driver was
the UNPLANNED migration cross-repo ledger wall (O10) — diagnosing that all 5
remote-only entries were legit sch-command files (not orphans to revert) and
routing around the blocked wrapper added ~20–30 min that the plan did not
anticipate. The code itself tracked close to estimate. Honest read: the loop did
NOT balloon (the locked fear), but it nudged past 1hr due to shared-DB tooling
friction, not scope growth.

═══════════════════════════════════════════════════════
5. SHIP STATE
─────────────────────────────────────────────────────
• Branch: feat/retention-invoice-process. NOT merged to main.
• Build commit: 33dc4d8 ("Touches F6: per-invoice retention release path").
• Handoff/backlog commit: <THIS COMMIT — see Git State below>.
• Frontend (the Bill Retention button) is FEATURE-BRANCH + Vercel PREVIEW ONLY.
  scmybiz.com (prod) does NOT have the button yet — confirmed (Chris's prod
  screenshot of #10063 has no button). Feature is not user-usable on prod until
  this branch merges to main.
• Merge to main NOT done — left for Chris's go-ahead.

═══════════════════════════════════════════════════════
6. DEPLOY STATE (precise — "file present" ≠ "deployed")
─────────────────────────────────────────────────────
• MIGRATION → PROD shared DB: **DEPLOYED & CONFIRMED LIVE.** Both columns probe
  HTTP 200 on the prod REST API (retention_release_of, retention_released).
  Applied via Supabase SQL editor (additive ALTER, IF NOT EXISTS) + recorded with
  `supabase migration repair --status applied 20260601120000`. Collision check:
  64 local / 64 in ledger, aligned. Migration file committed:
  supabase/migrations/20260601120000_invoices_retention_release.sql. (This is the
  SHARED project pbgvgjjuhnpsumnowuym — affects SC/SCH/FC; additive, no collision.)
• qb-sync-invoice → PROD: **REDEPLOYED with --no-verify-jwt.** Output: "Deployed
  Functions on project pbgvgjjuhnpsumnowuym: qb-sync-invoice". Edge functions are
  NOT branch-scoped — this is LIVE in prod now. Proven working by the smoke (the
  #10064 QB invoice was produced by this deployed function). Note: the gate only
  changes behavior for invoices with retention_release_of set; normal invoices
  take the unchanged else-branch, so prod invoices are unaffected.
• send-invoice: NO change (verified only).
• Frontend: preview only (see Ship State).

NET: backend (DB + edge fn) is LIVE in prod; frontend is preview-only. Safe
combination — the only way to create a release invoice is the preview button, so
no prod user can trigger the new path until merge.

NEW / CLOSED BACKLOG ITEMS
─────────────────────────────────────────────────────
• F6 (T3) — TOUCHED. Per-invoice release path shipped; per-job/cumulative still
  open. Row updated.
• F34 (T3) — already on file (full block/cascade void UX, deferred).
• O10 (T2) — NEW. check-migration-safety.sh Check B + bare supabase db push block
  legitimate SC migrations on sibling-repo ledger entries; workaround + fix noted.
• B41 (T3) — NEW. QB sync/void "test"-name asymmetry → orphaned live QB records on
  test-smoke cleanup (witnessed on #10063/#10064). Fix: make sync + void symmetric.
• Top-of-file "Last updated" line added for Loop #30.

VERIFICATION
─────────────────────────────────────────────────────
• /buildvsplan: 0 Tier-1, 0 Tier-2 (above).
• Live schema: HTTP 200 both columns on prod.
• vite build: clean.
• Smoke: PASSED — release invoice #10064 in QB with one +$101.00 "1121- Retention
  %" line (artifact PDF in repo). Balance-sheet delta NOT captured in accrual form
  (cash-basis pulls showed nothing — honest gap; invoice line is the proof).

NOT TOUCHED THIS SESSION
─────────────────────────────────────────────────────
• Merge to main (left for Chris).
• QB cleanup of #10063/#10064 (Chris to void).
• Accrual balance-sheet pull (optional, to satisfy §4 wording verbatim).
• All other open backlog (S4, B32, F5, F7, v140 audit triage, etc.).

NEXT SESSION POINTERS
─────────────────────────────────────────────────────
• /erd-close retention-invoice-process — artifact = the #10064 QB invoice PDF
  (and, if pulled, the accrual balance-sheet delta). TIME Delta: modest overage,
  driver = O10 migration wall, not scope.
• Merge feat/retention-invoice-process → main to ship the button to prod, then
  verify the button renders on scmybiz.com.
• Void #10063 + #10064 in QB; confirm the void-copy double-bill guard on #10063's
  replacement while there.
• O10 is the highest-leverage follow-up — it blocks the normal push path for
  EVERY future SC migration until fixed.

FILES TO KNOW
─────────────────────────────────────────────────────
src/pages/Invoices.jsx
  mintNextInvoiceId() helper (above InvoiceDetail); handleBillRetention(); Bill
  Retention button + released-state note in the action bar; retention_released
  copy on the void replacement insert; onNavigateInvoice now calls load().
supabase/functions/qb-sync-invoice/index.ts
  qbLines assembly gated on retention_release_of (release branch = one positive
  retention-item line; hard-fail if no item).
supabase/migrations/20260601120000_invoices_retention_release.sql
docs/plans/retention_invoice_process.md  (the converged plan)
docs/handoffs/assets/loop30_*  (smoke artifacts: invoice PDF, 2 balance sheets,
  prod screenshot)

GIT STATE ON CLOSE
─────────────────────────────────────────────────────
• sales-command: feat/retention-invoice-process. Build commit 33dc4d8 + this
  handoff/backlog commit (SHA below). Pushed to origin. NOT merged to main.
• Migration live on shared prod DB; qb-sync-invoice live in prod; frontend
  preview-only.
• sch-command / field-command / AR-Command-Center: untouched.

END STATE
─────────────────────────────────────────────────────
Per-invoice retention release built to spec (0 deviation in code; one disclosed
push-mechanism workaround, identical end-state). Backend live in prod, frontend
on preview, smoke PASSED via the #10064 QB invoice. ERD #30 ready to close on
that artifact. Outstanding: merge to main, QB cleanup, optional accrual sheet.
