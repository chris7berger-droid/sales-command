SC Handoff v192 — QuickBooks → Sales Command payment-status sync: FULLY SHIPPED (backend + frontend live)
Date: 2026-08-27
Branch: feat/qb-payment-status-sync — MERGED to main (merge 4d148bd) + branch deleted.
Plan: docs/plans/qb_payment_status_sync.md (2-round audited, build-ready).

═══════════════════════════════════════════════════════════════════════════════
PICK-UP (read first)
═══════════════════════════════════════════════════════════════════════════════
COMPLETE. QuickBooks → Sales Command payment reflection is fully live in prod — backend AND
frontend. A payment posts in QB → Intuit webhook → SC flips the invoice to Paid in seconds
(15-min cron sweep as backup). The one-time backlog (53 stuck invoices) is corrected. The UI
(QB badge, delay notice, hidden Mark-as-Paid on QB-owned invoices, H2 warn) is deployed via
the merge to main. qb-record-payment is locked to Stripe (service-role) only.

NOTHING REMAINS to build/deploy. Two soft follow-ups only:
  - Live-confirm (passive): the next real Stripe payment should still record a QB payment
    (tripwire + Stripe service-role pass-through are code-verified in T5/T6; not live-smoked).
  - F55 (docs/BACKLOG.md, T3): 3 non-blocking reflect-core hardening polish items.

DONE this session (full rollout):
  - CRON backup sweep LIVE. Migration command-suite-db 20260827160000; cron.job
    'qb-reflect-payments-sweep' active, */15 * * * *, targets qb-reflect-payments. No schema
    footprint (grants stayed 9171). command-suite-db main @ 09ee3e3.
  - FRONTEND MERGED to main (4d148bd) → Vercel prod deploy READY. UI live on scmybiz.com.
  - qb-record-payment TRIPWIRE deployed (Stripe-only; non-service-role → 403). Health: no-auth
    → 401 confirmed. Stripe's service-role call unaffected (verified T5/T6, code path unchanged).

All four review gates PASSED: build → buildvsplan → code-review → security-review. 0 blockers.

═══════════════════════════════════════════════════════════════════════════════
WHAT IS LIVE IN PROD RIGHT NOW
═══════════════════════════════════════════════════════════════════════════════
DB (shared project pbgvgjjuhnpsumnowuym):
  - invoices.qb_reflected_at (timestamptz, nullable) — migration command-suite-db
    20260827120000, pushed + ledger-recorded. Column confirmed NOT anon-exposed.
  - Same command-suite-db PR folded a rehearse-gate catch-up for yesterday's customers.notes
    drift (EXPECT_COLUMN_GRANTS 9004→9028→9039, baseline snapshot updated). Commits on
    command-suite-db main: 07c177c + 61aeaa5 (pushed).

Edge functions (deployed --no-verify-jwt):
  - qb-reflect-payments (NEW) — the 15-min sweep. Deployed + x-cron-secret gate verified
    (no/bad secret → 403). Inherits project-global CRON_SECRET (do NOT re-set it).
  - qb-webhook (NEW) — real-time reflection. Deployed. QB_WEBHOOK_VERIFIER_TOKEN secret SET.
    Smoked: no-token→500, bad-sig→401, valid-sig→200 (realm 9341452554176340 → tenant
    246f6551-60de-4965-bb97-9a52971bc05d, no-op on fake id, no data touched).
  - qb-record-payment — NOT yet redeployed. Still the OLD version (no tripwire). Held on
    purpose (see ORDERING HAZARD).

Intuit developer portal (Chris did this manually):
  - Sales Command app → Webhooks → PRODUCTION tab.
  - Endpoint URL = https://pbgvgjjuhnpsumnowuym.supabase.co/functions/v1/qb-webhook
  - Subscribed entities: Invoice + Payment.
  - Verifier token generated + set as QB_WEBHOOK_VERIFIER_TOKEN.

BACKFILL (one-time, done + verified against QB):
  - Ran the sweep once by hand via net.http_post in the Supabase SQL editor (the MCP role
    can't read the Vault cron_secret, so it must run from the SQL editor).
  - 111 candidates → 53 flipped to Paid, 58 left unpaid, 20 pre-existing Paid untouched.
  - Verified: all 53 flipped have a real paid_at (0 flipped without a payment — A1 guard held);
    3 spot-checked directly in QB all show Balance 0.00 (incl. 2 retention invoices); ALL 58
    left-unpaid have QB Balance > 0 (min $0.38) so none were wrongly skipped. No write-off/
    credit-memo existed in the data, so the "zero-balance-no-payment stays unpaid" case wasn't
    exercised against live data (covered by code review + the require-Payment invariant).
  - Note: 3 invoices have tiny residual balances (10059=$0.38, 10040=$0.40, 10050=$3.00) —
    underpayments that will never auto-flip (Balance≠0). Documented v1 partial-payment behavior.

═══════════════════════════════════════════════════════════════════════════════
ORDERING HAZARD — read before deploying the tripwire or merging
═══════════════════════════════════════════════════════════════════════════════
qb-record-payment's new tripwire (`if !caller.isServiceRole → 403`) makes it reject app-side
calls. But prod's CURRENT frontend (main) still has the OLD Invoices.jsx whose Mark-as-Paid
calls qb-record-payment with a user JWT. If you deploy the tripwire BEFORE the frontend merges,
a manual Mark-as-Paid on a QB-linked invoice silently stops recording in QB (the old .catch
swallows the 403). The tripwire + the button-hiding are two halves of one change.
  → DEPLOY qb-record-payment (tripwire) AT THE SAME TIME AS the merge to main, not before.

═══════════════════════════════════════════════════════════════════════════════
CRON backup sweep — DONE (was Remaining Step 1)
═══════════════════════════════════════════════════════════════════════════════
- LIVE. command-suite-db main caught up to prod (parallel session merged feat/material-
  confirmation → prod head 20260827150000), so the parked cron migration landed as
  20260827160000_qb_reflect_payments_cron.sql: rehearsed green (DDL-free, grants unchanged
  at 9171), pushed to prod, committed to main @ 09ee3e3.
- Verified: cron.job 'qb-reflect-payments-sweep' active, schedule */15 * * * *, targets
  qb-reflect-payments. Mirrors 20260805223000 (literal URL + Vault cron_secret + to_regclass
  guard). Inherits project-global CRON_SECRET (never re-set).

═══════════════════════════════════════════════════════════════════════════════
Frontend merge + tripwire — DONE (was the last remaining piece)
═══════════════════════════════════════════════════════════════════════════════
- Merged feat/qb-payment-status-sync → main (--no-ff, merge 4d148bd), pushed; Vercel prod
  deploy READY (frontend live on scmybiz.com). Clean merge (main had only the v191 handoff
  since branch point, no conflicts).
- Deployed qb-record-payment tripwire AFTER the frontend deploy went READY (frontend-first
  ordering honored — the new frontend no longer calls qb-record-payment, so no silent-403
  window). Health: no-auth → 401.
- Deleted the merged feat/qb-payment-status-sync branch (local + remote).
- Designer-as-user eyeball left to Chris (per solo-project norm): QB badge on a reflected-Paid
  invoice (e.g. #10145), Mark-as-Paid hidden on QB-linked, delay notice under "All Invoices."

═══════════════════════════════════════════════════════════════════════════════
NEW BACKLOG
═══════════════════════════════════════════════════════════════════════════════
- F55 (docs/BACKLOG.md, T3, Open) — QB→SC reflect core hardening, 3 non-blocking T5/T6
  findings: webhook work[] dedup, general IN-list escaping, paid_at noon-UTC vs literal local
  noon. All safe today; polish.

═══════════════════════════════════════════════════════════════════════════════
GIT STATE
═══════════════════════════════════════════════════════════════════════════════
sales-command feat/qb-payment-status-sync (pushed, NOT merged), commits since main:
  43b1c91  T5/T6 review response — fix gate comment, file F55
  4b1d9b7  steps 5+6: qb-webhook + QB badge + delay notice
  1d4d2f4  step 3: _shared/reflectPayments.ts core + qb-reflect-payments sweep + config.toml
  21950ab  step 2: local-only Mark-as-Paid + remove SC→QB pushes + tripwire + CLAUDE.md col ref
  (+ 7 prior plan/audit commits)
command-suite-db main (pushed): 61aeaa5, 07c177c (my migration + rehearse-gate work). LEAVE IT.

FILES TO KNOW
  supabase/functions/_shared/reflectPayments.ts   — the reflect core (all guards here)
  supabase/functions/qb-webhook/index.ts          — real-time webhook (HMAC, async)
  supabase/functions/qb-reflect-payments/index.ts — 15-min sweep
  supabase/functions/qb-record-payment/index.ts   — tripwire added (NOT yet deployed)
  src/pages/Invoices.jsx                           — local-only Mark-as-Paid, badge, notice
  scratchpad DRAFT_qb_reflect_payments_cron.sql    — the parked cron migration
