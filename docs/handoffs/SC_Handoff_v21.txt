SALES COMMAND — HANDOFF DOCUMENT v21
Generated: March 17, 2026
====================================================

HOW TO USE THIS DOCUMENT
Start every new Claude thread with:
"Continue building Sales Command. Here is the handoff doc v21 and the architecture doc."
Upload this file + HDSP_CommandSuite_Architecture.docx

====================================================
PICK UP HERE NEXT SESSION
====================================================

SC-30 end-to-end test:
- Open proposal > Generate PDF > Send to Customer > click send button
- Watch Network tab for fetch to supabase.co/functions/v1/send-proposal
- Check chris@hdspnv.com inbox for email
- Check Supabase dashboard > Edge Functions > send-proposal > Logs if no email
- After working: remove hardcoded chris@hdspnv.com fallback

====================================================
COMPLETED THIS SESSION (v21)
====================================================

- App.jsx: BrowserRouter added, public /sign/:token route added
- src/pages/PublicSigningPage.jsx: created (public, no auth)
  fetches proposal by signing_token, shows SOW + total,
  captures name, writes proposal_signatures, sets status to Sold
- ProposalPDFModal Send tab: wired to send-proposal Edge Function
- SendPlaceholder: wired to send-proposal Edge Function
- Test email hardcoded to chris@hdspnv.com
- All changes committed to git

KEY LESSON: always git commit immediately after Python scripts write files.
VS Code hot exit overrides disk writes with stale cache.
Recovery command: git checkout src/pages/Proposals.jsx

====================================================
COMPLETED PREVIOUS SESSIONS
====================================================

- proposals.signing_token column added
- proposal_signatures table created
- signed-proposals Supabase Storage bucket created (private)
- Resend domain hdspnv.com verified
- RESEND_API_KEY added to Edge Function secrets
- Supabase CLI installed and linked
- send-proposal Edge Function deployed
- react-router-dom installed
- PDF redesign: both modals match, white bg, teal header border
- CustomerSigningPage built inside WTCCalculator.jsx (internal preview)

====================================================
NEXT TASKS
====================================================

1. SC-30: Complete end-to-end test (pick up here)
2. SC-57: Proposal header UUID — verify still needed
3. Full end-to-end app test: customer > call > proposal > WTC > PDF > send > sign
4. SC-56 (backlog): Multi-tenant email settings

====================================================
SUPABASE COLUMN REFERENCE
====================================================

table: team_members
  columns: id, name (NOT full_name), email, phone, role, auth_id, active

table: proposals
  columns: id, total (NOT total_price), approved_at (NOT accepted_at),
           status, created_at, customer, intro_completed, attachments_added,
           recipients_assigned, wtc_verified, call_log_id, proposal_number,
           signing_token

table: call_log
  columns: id, display_job_number, stage, bid_due, follow_up, created_at,
           jobsite_address, customer_name, sales_name, job_number, job_name,
           is_change_order, parent_job_id, co_number, co_standalone,
           jobsite_city, jobsite_state, jobsite_zip,
           billing_address, billing_city, billing_state, billing_zip,
           billing_address_same

table: proposal_wtc
  columns: id, proposal_id, work_type_id (INTEGER 1-40), burden_rate,
           ot_burden_rate, tax_rate, prevailing_wage, regular_hours,
           ot_hours, markup_pct, materials (jsonb), size, unit,
           sales_sow, field_sow (jsonb), sub_areas (jsonb),
           travel (jsonb), discount, discount_reason, locked, created_at

table: proposal_signatures
  columns: id, proposal_id, signer_name, signer_email, signed_at,
           ip_address, pdf_url

====================================================
FILE LOCATIONS
====================================================

- src/pages/WTCCalculator.jsx
- src/pages/CallLog.jsx
- src/pages/Proposals.jsx
- src/pages/PublicSigningPage.jsx
- src/components/CallLogDetail.jsx (NOT src/pages/)
- src/lib/supabase.js
- src/lib/tokens.js
- src/App.jsx

NOTE: orphaned src/CallLogDetail.jsx in root — ignore it

====================================================
WTC CALCULATOR ARCHITECTURE
====================================================

- WTCCalculator.jsx lives in src/pages/ NOT src/components/
- CustomerSigningPage inside WTCCalculator.jsx (internal preview only)
- PDFPreviewModal inside WTCCalculator.jsx
- PublicSigningPage.jsx is the customer-facing signing page
- autosave: debounced 1.5s, upsert pattern
- work_type_id is INTEGER (1-40), not UUID
- 7 tabs: Bidding Info, Labor, Materials, Scope of Work, Travel, Discount, Summary
- On autosave: writes proposalPrice back to proposals.total

====================================================
PDF MODAL ARCHITECTURE
====================================================

1. PDFPreviewModal in WTCCalculator.jsx — single WTC, internal
2. ProposalPDFModal in Proposals.jsx — multi-WTC, customer-facing
   Has Send tab wired to send-proposal Edge Function

====================================================
SIGNING FLOW STATUS (SC-30)
====================================================

BUILT AND COMMITTED:
- PublicSigningPage.jsx
- App.jsx BrowserRouter + /sign/:token route
- ProposalPDFModal Send tab wired
- SendPlaceholder wired
- proposal_signatures table
- signing_token on proposals

NOT YET CONFIRMED WORKING:
- Actual email delivery
- Signature write to DB
- proposals.status = Sold on signing

====================================================
WORKFLOW RULES
====================================================

1. Never use drag and drop for files
2. Always provide code as text in chat
3. Never give half-baked solution mid-figuring-it-out
4. Supabase SQL Editor button: Create new snippet
5. Complex edits: Python scripts via terminal
6. git commit immediately after every Python script succeeds
7. Ask for screenshot before assuming cause of any error
8. One step at a time
9. Use cat > file.py << PYEOF pattern for Python scripts
10. After Python scripts: git add + git commit immediately

====================================================
DESIGN PHILOSOPHY
====================================================

- Fast obvious clicks over fewest clicks
- PDF white background — customers print
- No cost breakdown on customer PDF
- No teal text on linen — use dark #1c1814
- Teal for buttons, borders, badges only

====================================================
CURRENT APP STATE
====================================================

WORKING: Home, Call Log, Proposals, Invoices, Managers, Customers, Our Team
PLACEHOLDER: Sales Dash, Jobs, Feedback

====================================================
END OF HANDOFF v21
====================================================
