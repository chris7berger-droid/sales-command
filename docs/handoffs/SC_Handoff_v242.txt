SC Handoff v242 — CODEX — 2026-09-08

SESSION SUMMARY
Chris requested one copyable weekly itinerary per crew member, replacing the
proposed single-job image export. Built in an isolated sales-command worktree
on codex/weekly-crew-text. Ready for review; not merged to production.

CHANGES BUILT (NOT YET SHIPPED)
Schedules now shows an exact text preview, week picker, person picker, next-person
button and clipboard copy/manual fallback. Each Mon–Sun day lists assigned jobs,
trip title, address, lead, same-day/same-trip coworkers, vehicle/equipment/power,
and trip notes. Internal jobs.notes and billing fields are excluded.
Optional usual start/meeting instructions persist across people in the mounted
screen; saved job deferred_days/deferred_time take priority. No invented 6:30 AM.
Uses buildJobTrips and pickAllocField, preserving UUID assignment identity and
conservative legacy attribution. Parent dates never hide assigned return trips.
Missing jobs and ambiguous legacy staffing remain visible with confirmation text.
Rosterless/archived assigned names remain selectable; unassigned roster members
can copy an explicit no-work week. Sunday READ support only, not scheduling UI.
Crew Schedule's new Weekly crew texts button carries its displayed week and guards
unsaved trip edits/loading. Actions > Send Schedules opens the current-week screen.
Loads jobs/crew/assignments/trips as one usable snapshot; failures, refresh and
week changes remove copy controls until complete; stale requests cannot win.

DECISIONS
Text-only clipboard flow. No automatic messages, PDF/image, or SMS service.
No schema/RLS/edge-function/DB writes. Dates derive from assignments, not status
or job date filtering. No guessing lead/coworkers for ambiguous trip history.
No reusable-link persistence; texted copies are snapshots with an updated stamp.

BACKLOG
F61 added In Progress (built; pending review/release).
B107/B108 old print-path defects remain deferred; F60 Sunday scheduling UI stays open.

VERIFICATION
- Full Vite production build with non-secret fixture URL/key: PASS; existing large
  bundle warning remains. Env-free build is not meaningful here: missing-config
  throw allows Vite to eliminate app code. Vercel must use its normal environment.
- node scripts/check-crew-week-text-model.mjs: PASS (trip/day coworkers, inheritance,
  Sunday/DST, deferred start, missing jobs, overlap ambiguity, duplicates/multiple
  assignments, private job-note exclusion).
- scripts/check-crew-week-text.mjs intercepted React browser suite: PASS (exact
  clipboard/preview equality, person/week changes, desktop/390px phone layout,
  read errors/retry, clipboard denial fallback, Actions route and board week link).
- Existing crew-week-summary browser regression suite: PASS, including loading,
  rapid navigation, retry, draft guard, trip links and historical wording.
- Existing crew-week-summary and overlapping-crew-trips model checks: PASS.
- New screen and formatter ESLint clean. Six unchanged baseline unused-symbol
  errors remain in ScheduleLayout (1) / Schedule (5); no unrelated cleanup.
- git diff --check: PASS. All browser requests intercepted; GET-only DB assertion.
Browser tools: in-app node_repl tool unavailable; used existing Playwright script
pattern with local Chrome. PLAYWRIGHT_MODULE can point at the installed runtime's
Playwright index.mjs; existing regression script also accepts CHROME_PATH.
Screenshots: /private/tmp/weekly-crew-text-desktop.png and -mobile.png (fixtures).

NOT TOUCHED
Production, retired sch-command code, migrations, billing/printing, staff messages.
No real crew text sent; clipboard tested with fixture data only.

NEXT SESSION POINTERS
Review PR/preview then merge when authorized. Normal hosting config supplies the
Supabase environment; do not deploy fixture environment values. Once released,
mark F61 complete and record release SHA. Prefer the board's Weekly crew texts
button to carry the selected week; Actions opens a week-selectable screen.

FILES TO KNOW
src/schedule/lib/crewWeekText.js — pure formatter / day and trip attribution.
src/schedule/views/Schedules.jsx — read-only preview and clipboard UI.
src/schedule/views/Schedule.jsx — 4-line week-preserving launch button.
src/schedule/ScheduleLayout.jsx — Send Schedules placeholder replaced by routing.
scripts/check-crew-week-text-model.mjs and check-crew-week-text.mjs — focused proofs.

GIT STATE
Base main/origin/main: 7503a7c. Feature branch codex/weekly-crew-text.
Main checkout /Users/chrisberger/sales-command is clean and unchanged by this build.
Earlier stale B103 backlog draft preserved in named stash
preserved-before-reconciliation-2026-09-08-stale-B103-backlog; do not reapply over
current backlog (old report is already superseded by the released B103 fix).
