SC Handoff v242 — CODEX — Trip controls, 2026-09-08

SESSION SUMMARY
Work isolated at /Users/chrisberger/sales-command-bug-squash on codex/bug-squash.
The weekly crew text build is in a different worktree and was not edited.

CHANGES BUILT (NOT SHIPPED)
Expanded trip SOW is capped at 180px with keyboard-accessible scrolling.
Trip editor textareas are capped too. Job-level Delete now says Delete job.
The trip editor exposes Delete trip; confirmation states job/other trips remain.
Existing pull-ticket hard block retained. Linked crew assignments block deletion.
Delete uses job_id + trip id and verifies returned rows; failures stay visible.

DECISIONS
10227 screenshot has original Job schedule dates plus two saved trip entries.
Parent date display is derived, not a third saved trip; no deletion action for it.
No production writes, data repairs, or deletion of 10227 trips were performed.

INVESTIGATION / OPEN WORK
Read-only live queries verified 10262 -> job 1279, start Sep14, and 10227 ->
job 1280, start Sep28. Both active/unmerged Parked (displayed as Staged).
199 visible jobs rules out the 1000-row cap. September positions 14/22 rule out
25-row display cap for default September/All Stages; precise UI cause unproven.
Crew Schedule static jobs/trips load on mount/retry, not live updates/tab return.
Home/Jobs have realtime but no visibility/focus/reconnect catch-up. Refresh fix
is still unimplemented; protect unsaved editor state when addressing it.

VERIFICATION
npm run build, check-job-trips-model.mjs, git diff --check: PASS.
check-job-trips.mjs with installed Chrome and intercepted fake data: PASS.
Covers SOW overflow, deletion cancel, staffed-trip blocking, successful scoped
trip deletion, parent retained, no job writes, existing trip editing/regressions.
No authenticated production browser smoke. Pull-ticket block retained but not
newly simulated; assignment precheck is not an atomic database constraint.

NEXT SESSION / FILES
User preview/acceptance and production deployment pending.
TripsPanel.jsx/.css, MobsModal.jsx, StageJobCard.jsx, queries.js,
scripts/check-job-trips.mjs. No refresh or unrelated scheduling implementation.

GIT STATE
Local codex/bug-squash from main 7503a7c. Not merged or deployed.

DATE AUTHORITY CORRECTION (user approved)
Tentative bidding dates must not schedule work. ProposalDetail send now writes
null job/job_wtcs scheduling dates and null day dates in both SOW copies. Scope,
order, mobilization tags, and the mobilizations' own dates remain intact. This
prevents a synthesized parent schedule from bidding estimates on new sends.
No historical cleanup: origin of existing Schedule-edited dates is unproven.
check-send-schedule-dates.mjs executes the real send handler with an in-memory
DB boundary. Dated/undated mobilizations, dates_tbd on/off, preserved scope,
unchanged Sales inputs, no phantom parent and later Schedule edits: PASS.
Build and diff check PASS. PR42 extends to include this handoff correction.
Earlier proposed original-date separation/direct trip row controls have not
been implemented; discussion shifted to correcting the source of the dates.

10227 DATA CLEANUP / CARD FOLLOW-UP
User explicitly requested clearing the extra job schedule. Guarded live UPDATE
cleared only jobs1280/call_log3803 start/end/scheduled_start/scheduled_end after
verifying Sep28-Oct2. Readback confirmed N-DOT Polish trip UUID
e55a4fce-9d33-4074-9f66-58795189852e retained Sep28-Oct30.
Later read-only crew check: 84 assignments, 3 people, 28 dates; job and trip
crew_needed both null. User directed to Trips > Edit trip > Crew needed.
Planning DAYS and compact/Planning CREW now use jobCardSchedule helper with
saved trips, not proposal fallback. Union worked days avoids bridging trips
or double counting overlap. Weekend assignment exception retained. Null crew
shows ?, zero stays zero, mixed targets show varies. Trip DAYS opens Trips.
Model cases and real React test of trip-only 28d + 3/3 PASS; full existing
trip UI suite, build and diff check PASS. No crew requirements were written.
Readiness/baseChecklistPasses and StageBanner still use job-level dates: known
follow-up, not represented as fixed by the scoped DAYS-card change.

CREW NAVIGATION FOLLOW-UP
CREW now opens the earliest dated saved trip's start week and passes its UUID
in the trip query parameter. Board highlighting/scroll target matches job AND
trip when supplied; old job-only links keep their behavior. Explicit per-trip
Open Crew Schedule links also pass the UUID. With multiple dated trips, the
job-card CREW button chooses earliest start, then sequence; no date => existing
job-date/current-week fallback. No data writes. Model, full card UI, and full
overlapping-board UI tests PASS, including future-week exact-trip highlighting.

Back-to-job correction (preview): Crew Schedule now routes directly to
/schedule/jobs?job=<id> instead of browser-back, so the existing Jobs deep-link
logic expands/includes the intended card. Regression starts from a generic Jobs
history entry with fromCard:true and verifies the job-specific return. Full
overlapping-board UI suite, build and diff checks PASS.
