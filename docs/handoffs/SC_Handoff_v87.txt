SC_Handoff_v87
Date: 2026-04-28
Author: Chris Berger (with Claude assistance)
Topic: Security audit sweep — close all Critical, High, Medium, and Low items

================================================================
WHAT WAS DONE
================================================================

Closed 15 of 17 findings from the 2026-04-26 security audit in a single
session. Work spanned RLS hardening, dependency upgrades, CORS tightening,
rate limiting, environment hygiene, and repo cleanup.

Items closed by prior sessions (v83):
  - Critical #1: anon UPDATE on any signable proposal
  - Critical #2: anon SELECT on every signable proposal + children
  - High #5: anon INSERT into proposal_signatures

Items closed this session:

  High #3: tenant_config_public_read exposed business financials to anon
    - Created SECURITY DEFINER RPC get_public_tenant_config() returning
      only branding fields (company_name, tagline, logo_url, phone, email,
      website, license_number, proposal_validity_days)
    - PublicSigningPage + PublicInvoicePage switched to RPC
    - Old USING(true) anon policy dropped via 6-gate deploy
    - PR #7 merged, migrations applied in Supabase SQL Editor

  High #4: team_members_public_read exposed employee directory to anon
    - Created SECURITY DEFINER RPC get_rep_contact(rep_name) returning
      only name, email, phone for a single active rep
    - PublicSigningPage (2 spots) + PublicInvoicePage switched to RPC
    - Old USING(active=true) anon policy dropped via 6-gate deploy
    - Same PR #7, same migration batch

  High #6: xlsx 0.18.5 — Prototype Pollution + ReDoS
    - Upgraded to 0.20.3 via SheetJS CDN tarball
      (npm registry has no fix; used https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz)

  High #7: vite 7.3.1 — path traversal, fs.deny bypass, WS file read
    - Upgraded to 8.0.10 via npm i -D vite@latest

  High #8: flatted + picomatch transitive CVEs
    - Resolved via npm audit fix --legacy-peer-deps

  Medium #9: CORS Access-Control-Allow-Origin: * on 4 edge functions
    - Added ALLOWED_ORIGINS allowlist + Vercel preview suffix check to:
      proposal-signed, create-billing-session, stripe-webhook, scc-stripe-webhook
    - Same pattern already used by delete-user, invite-user, send-proposal, etc.

  Medium #10: reset-password unauthenticated and unbounded (DoS surface)
    - Added in-memory rate limit: 3 requests per email per 15 minutes
    - Rate-limited requests return 200 success (no state leakage)

  Medium #11: dompurify / postcss / brace-expansion moderate CVEs
    - Cleared by the dependency upgrades above; npm audit now reports 0

  Medium #12: Hardcoded Supabase URL + anon key fallback in supabase.js
    - Removed fallback values; env vars now required
    - Throws clear error if VITE_SUPABASE_URL or VITE_SUPABASE_ANON_KEY missing

  Medium #13 + Low #17: Console logs leak rep emails/IDs to public DevTools
    - Removed all 25 console.log/warn/error calls from src/
    - QB sync .then() chains replaced with .catch(() => {})
    - Error conditions that need user visibility converted to alert()

  Low #14: .gitignore doesn't block .env / .env.production
    - Replaced .env*.local + .env.prod with .env + .env.* (catches all variants)

  Low #16: Repo root clutter (72 handoff files, 9 fix scripts)
    - Moved all SC_Handoff_v*.txt + HANDOFF_*.md to docs/handoffs/
    - Deleted 9 obsolete fix_*.py scripts (preserved in git history)

================================================================
WHAT IS STILL OPEN
================================================================

  Low #15: qb_connection stores refresh tokens as plaintext
    - This is a Supabase-side storage concern, not a client-side fix
    - Proper fix requires Supabase Vault (pgsodium) or a server-side
      encryption layer in the edge functions that read/write tokens
    - Recommend using /ultraplan to architect this before implementing —
      it touches qb-auth, qb-sync-invoice, qb-record-payment,
      qb-create-job, qb-void-invoice, and the token refresh flow
    - Low urgency: tokens are only accessible to authenticated users
      with the service_role key (edge functions), not exposed to anon

================================================================
FILES TOUCHED
================================================================

New:
  supabase/migrations/20260429180000_add_public_rpc_functions.sql
  supabase/migrations/20260429190000_drop_public_read_policies.sql
  sql/rollback_drop_public_read_policies.sql
  docs/handoffs/ (directory — all handoff docs moved here)

Modified:
  src/pages/PublicSigningPage.jsx — config + rep lookups via RPCs
  src/pages/PublicInvoicePage.jsx — config + rep lookups via RPCs
  src/lib/supabase.js — removed hardcoded fallback values
  src/pages/Invoices.jsx — removed console.* calls
  src/pages/QBCallbackPage.jsx — removed console.log
  src/pages/Settings.jsx — removed console.error
  src/pages/Team.jsx — removed console.error
  src/App.jsx — removed console.warn
  src/components/ProposalDetail.jsx — removed console.* calls
  src/components/CallLogDetail.jsx — removed console.* calls
  src/components/NewInquiryWizard.jsx — removed console.error
  src/components/NewPayAppModal.jsx — removed console.error
  src/components/HistoryLocker/ImportToLiveWizard.jsx — removed console.error
  supabase/functions/proposal-signed/index.ts — CORS allowlist
  supabase/functions/create-billing-session/index.ts — CORS allowlist
  supabase/functions/stripe-webhook/index.ts — CORS allowlist
  supabase/functions/scc-stripe-webhook/index.ts — CORS allowlist
  supabase/functions/reset-password/index.ts — rate limiting
  .gitignore — block all .env* files
  package.json — xlsx 0.20.3, vite 8.0.10
  package-lock.json — updated dependency tree

Deleted:
  fix_approuter.py, fix_handoff.py, fix_jobinfo.py, fix_pdfmodal_send.py,
  fix_proposals_pdf.py, fix_sendproposal.py, fix_sendreset.py,
  fix_testemail.py, fix_wtc_tokens.py

================================================================
DEPLOY STATUS
================================================================

Vercel (frontend):
  - Auto-deployed on push to main
  - All client-side changes are live

Supabase (database):
  - Migration 20260429180000 (add RPCs) — applied via SQL Editor
  - Migration 20260429190000 (drop old policies) — applied via SQL Editor
  - Rollback available: sql/rollback_drop_public_read_policies.sql

Supabase (edge functions) — NOT YET DEPLOYED:
  - proposal-signed (CORS fix)
  - create-billing-session (CORS fix)
  - stripe-webhook (CORS fix)
  - scc-stripe-webhook (CORS fix)
  - reset-password (rate limiting)
  Deploy with: supabase functions deploy <function-name>
  Or deploy all: supabase functions deploy

npm audit: 0 vulnerabilities

================================================================
CROSS-REPO IMPACT
================================================================

RLS changes (tenant_config + team_members):
  - sch-command and field-command query these tables via authenticated
    client only — verified via grep, no impact
  - AR-Command-Center not found locally; verify at next deploy

Edge function CORS changes:
  - ALLOWED_ORIGINS includes salescommand.app, scmybiz.com,
    schedulecommand.com, schmybiz.com + any *.vercel.app preview
  - All Command Suite domains covered

================================================================
AUDIT SCORECARD
================================================================

  Critical:  2/2 fixed (v83)
  High:      6/6 fixed (3 v83, 3 this session)
  Medium:    5/5 fixed (all this session)
  Low:       3/4 fixed (1 open — qb_connection plaintext tokens)

  Total: 16/17 closed | 1 open (Low)

================================================================
