
# S6 — check-orphan-users is a CRON target, not a user-facing function. There is
# no caller JWT to forward, so the project's blanket `--no-verify-jwt` deploy
# convention is deliberately OVERRIDDEN here: deploy this function WITHOUT
# --no-verify-jwt so verify_jwt=true stands. The platform gate rejects requests
# with no valid JWT/service-role key; the handler's x-cron-secret check is the
# sole gate that distinguishes the scheduled invoker from any logged-in user.
[functions.check-orphan-users]
enabled = true
verify_jwt = true

# follow-up-reminders is a CRON target with no human caller, same as
# check-orphan-users. It is deployed WITH --no-verify-jwt (unlike that one) so
# the scheduled invoker doesn't need a service-role key: the handler's
# x-cron-secret check is the gate, and that secret is what the pg_cron job
# sends. See the notify_follow_up_reminders migration in command-suite-db.
[functions.follow-up-reminders]
enabled = true
verify_jwt = false

# proposal-approved is called by a signed-in user from the proposal screen.
# Auth is the handler's own authenticateCaller + tenant check.
[functions.proposal-approved]
enabled = true
verify_jwt = false

[functions.qb-auth]
enabled = true
verify_jwt = false

[functions.send-proposal]
enabled = true
verify_jwt = true
import_map = "./functions/send-proposal/deno.json"
# Uncomment to specify a custom file path to the entrypoint.
# Supported file extensions are: .ts, .js, .mjs, .jsx, .tsx
entrypoint = "./functions/send-proposal/index.ts"
# Specifies static files to be bundled with the function. Supports glob patterns.
# For example, if you want to serve static HTML pages in your function:
# static_files = [ "./functions/send-proposal/*.html" ]
